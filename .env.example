# Server Configuration
PORT=3000

# JWT Secret - Change this to a random string in production
JWT_SECRET=your-super-secret-jwt-key-change-this

# Database Configuration (MySQL 8.0)
DB_HOST=localhost
DB_PORT=3306
DB_USER=your_database_user
DB_PASSWORD=your_database_password
DB_NAME=magicalstory

# Storage Mode: 'file' or 'database'
# Use 'file' for local development with JSON files
# Use 'database' for production with MySQL
STORAGE_MODE=file

# API Keys (These will be stored in data/config.json via admin panel or database)
# ANTHROPIC_API_KEY=your-anthropic-key
# GEMINI_API_KEY=your-gemini-key

# Story Generation Batch Size (for rate limit management)
# Set to 0 or omit to generate entire story in one API call (requires high rate limits)
# Set to 5-10 for lower API tiers (e.g. Claude Tier 1 with 8K tokens/minute)
# Recommended: 0 for Tier 2+, 5-8 for Tier 1
STORY_BATCH_SIZE=0
