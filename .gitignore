# Temp/debug scripts
tmp-*.js
tmp-*.json
fetch-prompt.js

# Node modules (if any)
node_modules/
**/node_modules/
client/node_modules/

# Python virtual environment
venv311/
__pycache__/
*.pyc

# OS files
.DS_Store
Thumbs.db

# Editor files
.vscode/
.idea/
*.swp
*.swo

# Vite SSR bundle (build-only intermediate, not deployed — only used by scripts/prerender.mjs)
client/dist-ssr/

# Test files
test-simple.html

# Temp files
nul

# Secrets and API keys
API key.txt
*API key.txt
*.key
.env

# Claude Code (ignore all except skills)
.claude/*
!.claude/skills/

# Server data (sensitive - don't commit to Git)
/data/
/data/uploads/
/data/files.json
server/data/*
!server/data/swiss-cities.json
!server/data/swiss-story-ideas.json
!server/data/swiss-sagen.json

# User content and uploads
# Images/ - now included in git for Railway deployment
# But keep originals and unused out (large files)
images/originals/
images/not used/
images/*.mp4
!images/Boy to pirat to book.mp4
!images/landing-why-it-works.mp4

# Pre-optimization backups produced by scripts/optimize-landing-images.js
# (kept locally as a safety net; regenerable from git history if needed).
images/*-orig.*

Magical-Story-images/
Geschichten, Prompts, Inputs/
MagicalStory/
*.zip

# Logs
*.log
npm-debug.log*
logs/*.log

# Firebase service account (secrets)
*.firebase*.json
*-firebase-adminsdk-*.json

# Playwright
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
/playwright/.auth/
.auth/

# Migration scripts (one-time use)
run-migration*.js
test-output/
tmpclaude-*
*.bak

# Test files with potential secrets
test-gemini*.js

# Inpainting test outputs
test-*.png
test-*.jpg
test_*.png
test-scene*.jpg

# Per-page debug images dropped in repo root by analysis scripts
# (e.g. p2-v3.jpg, p10-debug.jpg, page3-debug.jpg, etc.)
/p[0-9]*.jpg
/p[0-9]*.png
/page[0-9]*-debug*.jpg
/page[0-9]*-debug*.png

# Local scratchpad — analysis scripts, dump files, ad-hoc PDFs
tmp/

# Manual test artifacts (screenshots etc. dumped during testing)
tests/manual/*.png
tests/manual/*.jpg

# Temp photos (debug/testing)
temp_photos/

# Test fixtures (large test data)
tests/fixtures/
test image inputs/

# Baden landmarks cache (local testing)
baden-landmarks/

# Output folder (story extractions, etc.)
output/

# Historical location images (local editing)
historical-location-images/
historical-location-images-uploaded/

# Miscellaneous temp/debug files
availableLandmarks
userLocation
C:*
*.tflite
_local

# Claude Code task tracking (local working files)
tasks/

# React Email browser previews (regenerable via npx tsx scripts/preview-email.ts)
emails-preview/

# Local creative drafts (not committed)
drafts/
