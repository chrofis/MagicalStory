# Full Code Review — 2026-07-25

> Deep review of the entire codebase, run in parallel waves by subsystem.
> Findings are recorded per area with severity, file:line evidence, and a
> proposed fix direction. Verified findings are marked; unverified ones are
> hypotheses from a single reviewer pass.
>
> Severity scale: **P0** data loss / security / money — fix now · **P1** real
> user-visible bug · **P2** correctness edge case / robustness · **P3** code
> health, perf, maintainability.

## Status

- [ ] Wave 1 — backend core (security, payments, pipeline, images, entity, text/PDF, regeneration, avatars/trial)
- [ ] Wave 2 — client, DB layer, prompts↔parsers, providers, python analyzer, composites, email/sharing
- [ ] Wave 3 — adversarial verification of P0/P1 findings + synthesis
- [ ] Final summary + ranked fix list

## Scope inventory

| Area | ~Lines | Files |
|---|---|---|
| server.js (monolith) | 8,280 | server.js |
| server/lib | 65,145 | images.js (17k), storyHelpers (6k), entityConsistency (3k), … |
| server/routes | 25,002 | regeneration (6.2k), stories (3.8k), avatars (3.6k), trial (2.7k), print (2.1k), … |
| server services/config | 8,456 | database.js (3.2k), models.js, prompts.js, middleware |
| client/src | 73,746 | StoryWizard (6.2k), StoryDisplay (7k), AdminDashboard (2k), … |
| prompts | 71 templates | |
| python | 3,439 | photo_analyzer.py |
| email | 1,108 | email.js |

---

## Findings

### 1. Security — routes, middleware, auth (Wave 1) ✅

**Verdict: no P0/P1.** Ownership checks (IDOR) are systematically correct across
stories/regeneration/jobs/characters/avatars. The real issues are abuse-cost
and hygiene class.

- **[P2] Unauthenticated file serving with weak non-crypto IDs exposes user photos (incl. children's faces)** — `server/routes/files.js:105` + `:43` — `GET /api/files/:fileId` uses `optionalAuth` and deliberately no ownership check ("unique random IDs, so public access is acceptable"), but IDs are `` `file-${Date.now()}-${Math.random()...substr(2,9)}` `` — predictable timestamp + non-crypto random. Uploaded face/body photos and print PDFs are fetchable by anyone with/guessing an ID; IDs leak in `fileUrl` responses. **Fix:** `crypto.randomUUID` ids + ownership gate or signed expiring URLs.
- **[P2] Global API rate limiter bypassable with a forged (unverified) JWT** — `server/middleware/rateLimit.js:28-42,55` — `apiLimiter` skips when `jwt.decode` (not verify!) yields `role==='admin'` or `impersonating===true`. Any unsigned token with `{"role":"admin"}` skips the 100/min cap; expensive unauthenticated endpoints (`sharing.js:653` og-image sharp resize, text-overlay render, `/api/user/location` outbound fetch) lose their only throttle → CPU DoS. **Fix:** skip based on verified `req.user` only.
- **[P2] AI proxy = free uncharged compute for any registered user** — `server/routes/ai-proxy.js:39,117` — `POST /api/claude` / `POST /api/gemini` deduct **no credits**, accept arbitrary prompts, and honor caller-supplied `max_tokens` (line 78, no clamp). Open registration → 60 calls/min of operator-paid Claude/Gemini. **Fix:** credit-charge or admin-gate; clamp `max_tokens` and prompt size.
- **[P3] Login email enumeration** — `auth.js:178` vs `:201` — `EMAIL_NOT_REGISTERED` vs `Invalid credentials` distinct responses defeat the dummy-bcrypt timing equalization. **Fix:** identical generic 401.
- **[P3] Legacy admin endpoints: `ADMIN_SECRET` in query string + timing-unsafe compare** — `admin.js:284,339,387` — secret lands in access logs/Referer; `/job-input` returns full job input/result data behind it. **Fix:** header + `timingSafeEqual`, prefer JWT-admin.
- **[P3] API-key prefixes logged** — `ai-proxy.js:49,127` — first 6 chars of live keys in logs. **Fix:** log set/unset boolean only.
- **[P3] Stateless 7-day JWTs: demotion/logout/password-change don't revoke** — `middleware/auth.js:39-51`, `auth.js:1260` — demoted admin keeps admin for up to 7 days; stolen tokens irrevocable. **Fix:** token-version column checked on sensitive routes.
- **[P3] OAuth redirect trusts Host headers when `FRONTEND_URL` unset** — `auth.js:424-429,587` — token fragment could be steered to attacker origin if env var missing. **Fix:** fail closed on missing `FRONTEND_URL`.
- **[P3] `/api/user/location` interpolates client-spoofable IP headers into outbound URL** — `user.js:19-37` — path injection into ip-api.com call (not full SSRF). **Fix:** regex-validate IP.
- **[P3] Mass assignment: `create-story` spreads whole `req.body` into `story_jobs.input_data`** — `jobs.js:101` — unaudited fields (model overrides, skip flags) ride along. **Fix:** allowlist.

**Checked and clean (defenses verified at line level):** story/regen/job/character/draft/avatar IDOR scoping (`WHERE user_id=$2` everywhere, any-story reads gated by `canReadAnyStory`); admin gating incl. impersonation re-check; parameterized SQL throughout (one dynamic-column case uses a hardcoded allowlist, `stories.js:779`); share tokens 64-hex `crypto.randomBytes(32)` + HMAC signed-key path with `timingSafeEqual`; password reset (1h expiry, generic response, cleared on use); trial admin-bypass HMAC (purpose-scoped, 5-min TTL); staging basic-auth constant-time; JWT_SECRET fail-closed at boot; Stripe webhook raw-body signature before json parser; Gemini proxy model allowlist; no user-controlled path traversal.

---

### 2. Background job pipeline — lifecycle, watchdog, credits (Wave 1) ✅

**Verdict: the credit-refund topology has holes.** The atomic-claim refund
pattern exists and is correct where used — but three failure paths skip it, and
"killing" a job doesn't actually stop it.

- **[P1] Watchdog + stale-detection mark jobs `failed` WITHOUT refunding reserved credits** — `server.js:8214-8227`, `server/routes/jobs.js:460-469`, `:684-691` — the 15-min sweep and the status-poll/my-jobs stale transitions flip status but leave `credits_reserved` untouched and never credit `users.credits` back. Contrast: create-story stale path (`jobs.js:206-250`) and boot zombie cleanup (`server.js:8078-8098`) DO refund. Compounding: `cleanupOldCompletedJobs` (`jobs.js:54-58`) **deletes failed rows after 1 hour**, destroying the `credits_reserved` evidence — the debt becomes undiscoverable. **Fix:** apply the cancel-endpoint's atomic-claim refund inside `sweepStaleJobs` + the poll stale transition.
- **[P1] Credit reservation leaks if the job INSERT fails** — `jobs.js:330-360,395-398` — credits debited atomically (line 330), then job INSERT (line 356); if INSERT throws (e.g. two concurrent submits with same `idempotencyKey` → second hits the partial unique index), outer catch returns 500 — debited credits never refunded and **no job row exists to carry the reservation**. **Fix:** one transaction, or refund-in-catch.
- **[P1] Double-submit without idempotency key double-charges** — `jobs.js:177-279` — active-job check is SELECT-then-INSERT, no lock, no DB constraint (idempotency key optional). Two near-simultaneous POSTs → both reserve, both insert, two full pipelines. **Fix:** partial unique index `ON story_jobs(user_id) WHERE status IN ('pending','processing')` or mandatory idempotency key.
- **[P1] "Killed" jobs keep running, keep spending, and can overwrite `failed`/`cancelled` with `completed`** — `server.js:7473-7485,7242-7248` — `checkCancellation()` only aborts on `'cancelled'` and is NOT threaded into `runUnifiedRepairPipeline` (whole repair phase never checks). Completion write is **unconditional** (`UPDATE ... SET status='completed' WHERE id=$5`). Scenarios: stale-refunded job finishes anyway → refund + finished story; user cancels during repair/finalize window → refund + `'cancelled'` overwritten by `'completed'` → free story. **Fix:** completion UPDATE `WHERE status='processing'`, reconcile credits on mismatch.
- **[P1] Status-poll can false-fail a healthy job (heartbeat gaps)** — `jobs.js:441-469` vs `server.js:5462,5663,5879,6747` — three inconsistent thresholds (10 min poll / 15 min sweep / 60 min total; comments still say "5 minutes"). Phase 5a/5a-pre heartbeats fire only on image *completion* — first completion >10 min (provider backoff at concurrency 50) → job failed while working. Repair loop already fixed this with a 30s interval ticker (`images.js:8488`); 5a and finalize (no heartbeat at all after progress 73, during R2 upload of every image) didn't. **Fix:** interval-ticker pattern in 5a/5a-pre/finalize; unify thresholds.
- **[P2] Boot zombie cleanup: non-atomic refund + rolling-deploy race** — `server.js:8053-8102` — refunds from stale SELECTed values (not atomic-claim); overlapping deploy containers can double-refund, and the new container can fail jobs an old draining container then completes.
- **[P2] Job "completes" at full price with most pages failed** — `server.js:6062-6083,7099-7119` — per-page catch converts failure to `{imageData:null}` and continues; no minimum-success gate before completion. Also warn-and-continue on: null scene expansions (whole pages dropped, 4909), empty VB (3109), coverless after 3-min timeout (6613), vantage plates, scale repair, shared bbox, checkpoints, cover typography, styled-avatar persistence. **Fix:** success-ratio gate + proportional refund; surface degradations in `result_data`.
- **[P2] ~0.5–1 GB peak per repair-heavy job, no global concurrency cap** — `server.js:5790-6079,7203-7241` — all base64 held in memory whole-job (~15–25 MB/page × 15 + covers + versions + retry history); `dropInlineBase64` comment confirms a real 256 MB jsonb overflow happened. Only per-user cap exists; 5–8 concurrent repair-heavy jobs threaten the 8 GB heap. **Fix:** global job semaphore; stream versions to R2 as produced.
- **[P2] Retry-storm surface: pLimit(500) bbox + evalConcurrency 500 per job** — `server.js:6337-6364` — 500 simultaneous Gemini calls manufacture the 429/503s the retries then amplify; two jobs → 1000.
- **[P2] Row stuck `pending` forever if the failure handler's own DB write throws** — `jobs.js:381-383`, `server.js:7969-7974` — falls to the sweep, which doesn't refund (see first P1).
- **[P3] Progress written at task start** (races to ~30 with zero images done); "95%" comment writes 73; completed rows deleted after 1 h → late poll gets 404.
- **[P3] Polling bandwidth: without optional `?knownPages=`, every poll re-downloads all completed pages (~10–20 MB).**

**Checked and clean:** atomic-claim refund correct in cancel/create-story-stale/pipeline-catch paths; credit reservation race-safe (`UPDATE ... AND credits >= $1`); `JobCancelledError` vs `failed` distinction; checkpoint lifecycle (upsert, delete on completion, CASCADE); throttled in-flight-guarded heartbeat helper; repair-loop 30s ticker; cover `Promise.all().catch()` + 3-min race at both await sites; `AbortSignal.timeout` on every external fetch checked (Grok 120s, Runware 60–120s bounded retry, Gemini 120–300s, Anthropic ~5min/25min streaming cap); `savePartialStoryFromCheckpoints` clobber guard; per-job `runInCacheScope` isolation.

---

### 3. Image generation + evaluation — `images.js`, scoring, validators (Wave 1) ✅

**Verdict: retry/score/aspect machinery is largely sound; the rot is in cost
accounting (four no-op tracker calls) and dead eval fields consumers pretend
exist.**

- **[P1] Repair/bbox/consistency/re-eval usage silently dropped from cost accounting** — `images.js:13533,13627,13795,13818` — `usageTracker` contract is `(provider, usage, functionName, modelName)` (`server.js:2869`), but four call sites inside `generateImageWithQualityRetry` misuse it: `13795` uses the removed legacy 5-arg signature (provider receives a usage *object* → junk `[object Object]` bucket, 0 tokens); `13533/13627/13818` pass `(null, usage, null, model)` → **no-op that still stamps `usage.__accounted=true`**, blocking later legitimate accounting. Compounding: Grok repair usage shape is `{model, cost}` — no tokens — so even correct plumbing would count $0. Every auto-repair Grok edit, bbox-detect, incremental-consistency and post-repair re-eval is invisible in per-story cost. Same family as the recent "cost NaN" fixes; these instances remain.
- **[P1] `evaluateImageQuality` never returns `object_matches` / `expectedText` / `actualText` — three consumer features are dead** — `images.js:1981-2001` vs consumers at `4794,4909,5441,6422,6468,6482,13455-13467,13582` — `object_matches` read in every branch, always `[]` (P1 visual inventory produces it at `710` but the merge at `1864-1876` drops it). The cover "NO TEXT expected" carve-out (`13455`) can never fire (`expectedText` always undefined; `textIssue` never `'MISSING'`) — textless-by-design covers go to full regen. **Fix:** return the fields or delete the dead features.
- **[P2] Last-attempt generation error throws away a usable earlier attempt** — `images.js:13434-13436` — `if (attempts >= MAX_ATTEMPTS) throw` even when `bestResult` holds a scored image from attempt 1-2; transient outage on attempt 3 fails the page instead of shipping the 55% image. **Fix:** `if (bestResult) break;`.
- **[P2] Falsy-zero severity bugs** — `issueExtractor.js:39,504`: `SEVERITY_ORDER[a.severity] || 2` with `critical: 0` → critical ranks as minor in dedup; overlapping major can dominate-drop a critical. `images.js:16078`: `severityOrder['critical'] || 1` → `minSeverityToFix:'critical'` behaves as `'major'`. `moderate`/`catastrophic`/uppercase not in tables. **Fix:** `??` + complete case-insensitive tables.
- **[P2] Runware branches hardcode 1024×1024, ignoring `MODEL_DEFAULTS.pageAspect` (3:4)** — `images.js:4748-4754,5160-5166,5585-5591,5840-5846` — square pages in an A4 pipeline; breaks text-overlay geometry + PDF layout on any Runware/dev config. **Fix:** one aspect→dims helper.
- **[P2] Runware backend branch drops `imageUsage`/`qualityUsage`** — `images.js:4781-4802` — exact bug class documented as fixed for the Grok branch (`4818-4821` comment); Runware never got the fix → untracked cost.
- **[P2] Out-of-scope `finishReason` → ReferenceError sabotages bbox truncated-JSON repair** — `images.js:3484` (declared inside `for` at 3369) — throws on every successful truncated-JSON repair; regex fallback then overwrites the good parse with figures-only → `objects[]`/missing-object flags silently lost whenever eval JSON truncates. **Fix:** hoist declaration.
- **[P2] STEP 2C proportion check disabled on the production-default backend** — `images.js:4882-4891` — the `imageBackend==='grok'` branch (prod default) omits `sceneCharacters` from the eval call; Runware/model-routed/Gemini branches all pass it. Head-to-body proportion block empty on the main path.
- **[P2] Eval failures fail OPEN in the quality-retry path** — `images.js:13441,13499-13514` — `null` eval (API error, safety block, unparseable JSON…) → image accepted with `score:null`, no `evaluated:false` flag (batch path has one; covers/iterate/regeneration path doesn't).
- **[P2] No timeout on repair-verification Gemini calls** — `repairVerification.js:171,295` — missing the `timeout:120000` that `sceneValidator.js:30` added after the 2026-07-07 stuck-at-51% incident; same hang class available in grid-repair verification.
- **[P2] Post-repair return pairs repaired bytes with pre-repair bbox overlay** — `images.js:13863-13874,13941-13952,13975-13983` — fails safe thanks to the `sourceImageFp` invariant (verified enforced at all consumers), but persists mismatched overlays and forces redundant re-detection.
- **[P3]** (abridged; full detail in agent report) `imageFingerprint` hashes full data-URI not payload (false stale-bbox); legacy `parseFloat("8/10…")` → 8/100 misread + unclamped `score*10` paths; cover `TITLE_ERROR` regex matches "texture" (no trailing boundary) and best-by-score can ship the title-error cover; Grok 7.5k truncation blind tail-chop cuts the trailing no-text/copy-space rules + dead ternary defeats backend override; batch-index→page mapping ambiguity; consistency parse failure shape (`consistent:false, issues:[]`); `wasRegenerated:true` on single-attempt; stale hardcoded prices in sceneValidator (~2-4× under-report); `repairScene` newline-escape corrupts multi-line JSON; mime declarations wrong both directions (benign today); double-count guard bypassed in post-three-stage merge; eval cache key ignores model overrides (dev A/B confusion); `evaluateImageBatch` default concurrency 100.

**Checked and clean:** retry loop keeps BEST not last (`13491-13497`); bounded retries everywhere, no retry↔repair recursion (`maxPasses:3` + deliberate ping-pong terminators); scoring.js triplication intentional and consistent, `capEntityPenalty` idempotent, tie-breaks correct; no `null>=60` coercion paths; bbox normalize/round-trips consistent `[ymin,xmin,ymax,xmax]`; `editImageWithPrompt` aspect-snap exemplary (log-ratio nearest preset; Gemini fallback swaps model IDs correctly at all 3 sites); markdown-fence/balanced-brace/MAX_TOKENS-retry eval plumbing correct; LRU caches have real eviction, bbox cache content-hashed.

---

### 4. Client — StoryWizard, trial flow, polling, dev mode (Wave 2) ✅

**Verdict: one hard P0 crash on a conversion-critical path, and the dev-mode
"admin gate" turns out to be client-side decoration.**

- **[P0] `require()` in browser ESM bundle — white screen for theme-page arrivals** — `client/src/pages/StoryWizard.tsx:5656` — the pre-selected-topic banner does `const {...} = require('@/constants/storyTypes')` inside a render IIFE. Vite builds pure ESM; `require` is a ReferenceError at runtime. Render condition matches exactly the state produced by landing on `/create?category=…&topic=…` from a theme page — and there is **no ErrorBoundary anywhere in client/src**, so the throw unmounts the whole app: blank page for every theme-page conversion click. **Fix:** static import (module already imported at line 35).
- **[P1] Dev-mode model overrides NOT server-gated — any user can set them via devtools** — client gate `StoryWizard.tsx:3982` is decorative; server (`server.js:7787-7805`) applies `inputData.modelOverrides` for **every** user (comment says "admin only" but `isAdmin` at 7782 only gates debug images). Also honored from anyone: `skipImages`, `skipCovers`, `enableFullRepair`, `layoutOverride` (`server.js:7774-7776`). Any user can pick expensive models or silently degrade their own paid story. Ties into the security agent's mass-assignment finding (`jobs.js:101` body spread). **Fix:** server drops overrides unless `isAdmin`.
- **[P1] Duplicate concurrent polling; one poller re-downloads all page images every 1.5s** — `GenerationContext.tsx:178` polls without `knownPages` at 1.5s (full base64 of every completed page per poll — hundreds of MB per generation), while `generateStory()`'s own loop polls the same endpoint at 2s (correctly filtered). Two pollers per tab; cross-tab sync starts more. **Fix:** single poller, always pass `knownPages`.
- **[P1] `generateStory()` poll loop not tied to component lifecycle** — `StoryWizard.tsx:4025-4275` — "Continue in Background" navigates away but the while-loop keeps polling from the unmounted component (~15 dead setStates/tick) and on completion force-`navigate`s the user out of whatever page they're on minutes later. **Fix:** break loop on unmount/route change; let GenerationContext own background completion.
- **[P2] Trial generation hard-fails on a single non-OK poll** — `TrialGenerationPage.tsx:382-385` — one 502 during a deploy → permanent "failed" → retry hits `TRIAL_USED` wall while the job still completes server-side. **Fix:** treat 5xx like network errors (bounded retry).
- **[P2] Trial photo re-upload after prewarm silently dropped** — `TrialCharacterStep.tsx:433-440,471-501` — PATCH syncs only text fields; a changed photo never reaches the server → story renders the first photo. **Fix:** photo-dirty check → PATCH photos or re-create account.
- **[P2] Double-submit window in `generateStory`** — guard flag set only after two awaits (`StoryWizard.tsx:3792-3831`); second click triggers the confusing "already generating — cancel it?" dialog against the user's own fresh job. **Fix:** synchronous ref guard.
- **[P2] Cross-account localStorage leakage** — `storage.ts:138-149` clears only auth keys; `story_details`, `story_dedication` (child's name — privacy), `wizard_step`, `mystories_selected` survive logout→login as another user. The recent banner fix covered GenerationContext only. **Fix:** extend `clearAuthStorage` or key by user id.
- **[P2] TrialWizard destroys session token on transient network error** — `TrialWizard.tsx:309-313` — flaky mobile connection discards a valid trial session → second anonymous account → fingerprint cap lockout. **Fix:** clear only on confirmed 401/403.
- **[P2] F5 during trial generation loses the progress view** — `TrialGenerationPage.tsx:217-240` — session token persisted only in the Google-sign-in path; plain refresh → `/try` → "already created your free story" wall while the job runs. **Fix:** persist token when generation starts.
- **[P3]** (abridged) No client-side photo type/size validation + `resizeImage` falls back to the raw original on decode error (40MB POST); hardcoded `pages * 10` credit price in 3 buttons (constant exists); raw `localStorage.getItem('auth_token')` in 5 places bypasses the Safari-private-mode storage wrapper; stale closure defeats the pageTexts update guard (heavy re-render every poll); 60-min orphan cleanup can hide a legitimately long job; ~20 `as any`/`@ts-expect-error` casts marking real type drift between `storyService` types and wire responses; render-time side effects in TrialGenerationPage; check-then-set cross-window guard; un-awaited admin cancel; silent fallback to uncropped photo when analysis fails.

**Checked and clean:** all setInterval/setTimeout have cleanups except the flagged while-loop; all pollers stop on terminal states + handle 404/abandoned; `?jobId` re-adoption guard correct (server uses `storyId: jobId`); legacy localStorage normalized + age-capped; photos never persisted to localStorage; trial consent genuinely blocks upload (incl. a11y); trial steps can't skip forward; BookBuilder double-submit guarded synchronously; api.ts centralizes res.ok/401 handling; progress bars have backwards guards in all three pollers.

---

### 5. Story-mutation & avatar routes — races, versions, transactions (Wave 1) ✅

**Verdict: the single worst structural problem in the codebase — full-blob
read-modify-write with zero concurrency control, plus transactions that
aren't transactions.**

- **[P1] Full-blob RMW, last-write-wins, across ~15 handlers** — `database.js:1902-1905` — `saveStoryData` is a whole-blob `UPDATE ... SET data=$1` with no version column, no `FOR UPDATE`, no CAS — and the read→write window contains a 10s–3min AI call. Concrete: character-repair (reads at `regeneration.js:5014`) racing frontend autosave (`stories.js:3021`) — loser's `imageVersions` metadata, eval results, `finalChecksReport` erased. `saveScenePageData` (jsonb_set, page-scoped) exists and is used by regenerate/iterate/edit/repair-image — but ~15 handlers still full-blob write (full table in agent report; worst: all repair-workflow steps, cover paths, autosave, trial character RMWs). **Fix:** `data_version` CAS column, or extend the targeted-write pattern to covers/finalChecksReport/sceneDescriptions; never write back a blob read before an AI call.
- **[P1] `BEGIN`/`FOR UPDATE`/`COMMIT` issued through the POOL — transactions are fictional** — `avatars.js:1275,2245-2497`, `characters.js:455`, root cause `database.js:47-56` — each statement may run on a different connection: the `FOR UPDATE` lock protects nothing, `BEGIN` leaves a connection in-transaction back in the pool where unrelated requests join it, and stray `ROLLBACK`s can roll back other requests' writes — silent, non-deterministic corruption. **Fix:** dedicated `client = await getPool().connect()` per transaction (helper already exported for this).
- **[P1] Non-atomic `getNextVersionIndex` (MAX+1) + upsert `saveStoryImage`** — `database.js:2388-2399,2444-2459` — two concurrent mutations on one page both allocate N+1; second **overwrites** the first's bytes (R2 key collides too); blob ends up with two entries pointing at one image. **Fix:** allocate inside INSERT with conflict-retry, or advisory lock on (story, type, page).
- **[P2] Repair-workflow steps save in background AFTER responding** — `regeneration.js:4049,4878,4997,4555,4736` — `res.json()` then unawaited `saveStoryData`; the chained next step reads before the save commits, then its own full-blob save erases step N's results. Systematic lost-update **within one user's sequential workflow**. **Fix:** await saves; page-target the writes.
- **[P2] DB version_index used as array index in six places** — `regeneration.js:3834,3971,4121,4537,4732,5688` — `arrayIndexForDb` exists and is used correctly elsewhere (`227,4793,4957`); these six skip it → evals/bbox stamped onto the wrong version on lazy-migrated stories; `recomputeAllActiveVersions` then ranks on corrupted scores.
- **[P2] artifact-repair pushes versions with no `dbVersionIndex` stamp** — `regeneration.js:5921-5951` — the exact row-collision failure the codebase's own comments warn about (`2729-2734`); every sibling endpoint was fixed, this one missed.
- **[P2] Cover iterate with `blackoutIssues` → ReferenceError AFTER credit deduction** — `regeneration.js:2485` — `previousImage` never declared (variable is `previousImageData`, line 2264). User pays, generation succeeds, response building throws → 500, result invisible.
- **[P2] Trial hard cap consumed before generation, never refunded on failure** — `trial.js:1111-1125` — job-failure path refunds credits but not `stories_generated`; a deploy killing a trial job burns the user's only trial forever. **Fix:** guarded decrement in the refund path.
- **[P2] Avatar jobs: 10-min in-memory Map expiry + no restart reconciliation** — `avatars.js:153-166,3594-3639,2543-2561` — >10-min jobs deleted mid-flight (poller 404s, wizard stalls); deploy/crash → `avatars.status='pending'` forever, no sweep. Same stuck-pending class the Grok migration fixed for safety refusals, still open for process death. Trial claim path same.
- **[P2] Character-repair Phase-2: full-blob save per page in a loop with eval awaits between** — `regeneration.js:5581-5811` — each iteration widens the stale window and rewrites the whole story.
- **[P3]** (abridged) `upsertStory` TOCTOU ownership flip via `ON CONFLICT ... SET user_id` (`stories.js:3011`, `database.js:2051`) — fix in the conflict branch's WHERE; shell row `data='{}'` visible/orphanable during persist; repair endpoints don't check `story_jobs.status` (generation's final save clobbers mid-flight repairs); story deletion mid-repair → R2 orphans + FK 500s; 50MB `express.json` on ALL mounts incl. unauthenticated trial routes; dead duplicate route `server.js:2659` shadowed by router; `/edit/cover` missing rate limiter, both edit endpoints charge no credits (decision or oversight? → decisions.md); trial admin-bypass HMAC not bound to user/IP.

**Checked and clean:** ownership filtering everywhere incl. impersonation gating; trial stories not enumerable (server-signed session tokens); no per-version delete endpoint (no index-shift hazard); trial cap enforcement atomic + pre-spend (correct except refund); consent/deferred-email coherent; styled-avatar cache scoping (`runInCacheScope`) leak-free; merged save core (`persistStoryToDatabase`) v0 single-writer/firstSave/`_alreadySaved` semantics correct, `created_at` preserved; BILL-1/2 atomic credit decrements correct; `impersonating` not user-forgeable.

---

### 6. Client — StoryDisplay, repair workflow UI, viewers (Wave 2) ✅

- **[P1] Full-workflow inpaint hardcodes version index 0 to the parent** — `useRepairWorkflow.ts:1280` — the standalone path was explicitly fixed to use `result.activeVersion` (line 1015, with a comment explaining why); the automated-workflow sibling still sends `0` → repaired image masquerades as "Original", repair invisible in history. Textbook fixing-sibling-paths miss.
- **[P1] Aborted/thrown workflow leaves steps stuck `in-progress` forever** — `useRepairWorkflow.ts:1181-1187,1535-1545` — catch never `failStep`s the current step; `isRunning` derives from any-in-progress → all buttons disabled, spinner forever, only the tiny Reset icon recovers. **Fix:** sweep in-progress → failed in finally.
- **[P1] "Stop" stops nothing server-side + enables overlapping runs** — `useRepairWorkflow.ts:242,313-317,1544` — AbortController signal never passed to fetches (server keeps generating + persisting billed versions); Stop immediately re-enables "Run Full Workflow" while the old run drains; first run's finally nulls the second run's controller ref → second run unabortable, interleaved writes.
- **[P1] Array-index vs `versionIndex` confusion in active-version logic** — `ImageHistoryModal.tsx:233,241,278,469,803`, `StoryDisplay.tsx:223-263,4470,5356,6045` — server explicitly supports sparse version arrays (clamp comment `stories.js:2329-2346`); every active-check compares raw array index → wrong "Aktiv" badge, "Use this image" on the visually-active card switches versions, dev panels show wrong bbox/refs. **Fix:** one `findIndex(v => v.versionIndex === activeVersion)` resolver.
- **[P2]** (abridged) ImageHistoryModal snapshots versions at click time (repairs completing while open invisible); `completeStep('re-evaluate')` replaces instead of merges (partial evals erase full evals → auto-identify works on truncated subset); eval failure in automated workflow → "No bad pages" → green "complete" run that repaired nothing; `rejectRepair` reverts to `versionIndex-1` (may not be pre-repair version); workflow state 100% component-local (navigate away = desync, second workflow can race the first server-side); duplicate repair entries + duplicate React keys on retry (manual appends, auto replaces); **"Text auf Bild" toggle is a visual no-op** that fires 15 sequential server-side Sharp renders into state nothing renders (overlay display removed 2026-05-16, toggle stayed); whole book base64 in DOM at once + versions with bytes in state (50-100MB+ on mobile); cover lazy-loading only for front cover (initial/back with `hasImage` but deferred bytes render NOTHING — the next thumbnail-style regression); displayed per-issue penalties (-30/-20/-10) disagree with real model (25/15/2); BookViewer monkeypatches `EventTarget.prototype.addEventListener` globally; Italian page-marker split breaks text editing (`parseStoryPages` knows `Pagina`, `handlePageTextChange` doesn't → edits land on wrong page) + Italian UI strings missing app-wide; BookBuilder price display not authoritative + hardcoded "CHF 10" label vs dynamic discount; dev-mode gate = localStorage (server-side authz of dev endpoints is the only real defense — cross-ref finding §4 P1); concurrent same-step ops share one status slot.
- **[P3]** (abridged) "Figur reparieren" button styling deviation (`min-h-[52px]` twice + `w-full`); `window.location.reload()` after dev inpaint; `initialTextOverlay` computed once pre-data; JWT in img query strings (logs/history — `?key=` mechanism exists); shared-viewer overlay warm-up storm + gate drift vs BookViewer; book-spread inversion when frontCover absent; no SSR/OG for shared viewer; server `normalizeImageData` lacks https passthrough (exact bug class client fixed — latent) + mixed `toImgSrc` conventions (how the thumbnail regression happened); `IMAGE_CONCURRENCY=50` unbounded; console.warn in render; ~80-line duplicated cover feedback block (already drifting); MyStories payment poll no unmount cancel; debug console.log page 1.

**Checked and clean:** top/bottom action bars fully compliant with the mandated className standard; `versionScore` mirrors server chain correctly; `toImgSrc` correct; share payload exposes no PII/eval internals; TestModelsPanel/StyleLab render-gating consistent; trial/checkout guards consistent; BookViewer parity-blank page mapping correct; reading-mode position sync sound; `pageSlotCount` guard correct; MyStories cache userId-keyed with sound invalidation.

---

### 7. Eval-threshold coherence — prompts vs scoring code (Wave 2, sub-audit) ✅

**Verdict: the canonical redo gate (scoreThreshold 60, SEVERITY_POINTS
50/25/15/5/2) is genuinely single-sourced and coherent — but the prompts
teach the models a DIFFERENT rubric than the one that charges them.**

- **[P2] `image-evaluation.txt` states two different rubrics both labelled "§2"** — line 16 (deduct-from-100: 50/25/15/5/2 — matches `scoring.js:99-105`, correct) vs line 359 (legacy 0-10 table mislabelled as "§2, what the pipeline computes finalScore with" — false since 2026-07-10 single-scale change).
- **[P2] Prompt claims the model's score is ignored; code blends it** — `image-evaluation.txt:359` "the pipeline ignores it" vs `images.js:1783/1252` — model's audit score can lift computed quality by up to 3 points (=30 on 0-100). Comment at `images.js:1727` repeats the false claim.
- **[P2] `image-semantic.txt` penalties (30/20/10) ≠ redo-gate charges (25/15/5)** — prompt matches only the legacy path (`images.js:1938-1941`); canonical path charges `SEVERITY_POINTS`. A MODERATE semantic issue: 10 by prompt, 5 by the gate that decides redo.
- **[P2] Avatar fail bar: prompt says <5 fails; code retries <7** — `avatar-evaluation.txt:13` vs `avatars.js:148` (`MIN_BASE_AVATAR_SCORE=7`, raised 2026 with comment "alongside F3 prompt tightening" — prompt never updated; still teaches the model 5-6 passes).
- **[P2] Avatar eval field path dead + invariant unverified** — `avatars.js:519-520` reads `faceMatch.finalScore` which the prompt never puts there (always undefined → fallback); `||` not `??` (a 0 falls through, then discarded by range check → null); "finalScore MUST equal LOWEST of three" never recomputed in code.
- **[P2] sheet-2x4 `valid ≥ 6` rule never enforced** — prompts state it; `character2x4Sheet.js:652,667,787,804` trust the model's `valid` boolean, never cross-check (`{finalScore:3, valid:true}` accepted); parse failure assigns `score:5` (below the prompt's own bar) and competes for bestAttempt; the numeric gate was deliberately removed 2026-05-17.
- **[P2] Client and server disagree on the iterate gate (50 vs 20)** — `repairLogic.js:209` (`visualScore<50`) vs `repairDefaults.ts:11` (20) — a page at 35 iterates on server, inpaints on client. Root cause documented-but-unfixed at `models.js:459-464` ("DEAD CONFIG … values DISAGREE").
- **[P2] `feedback-consolidator.txt` deduction table (30/15/7/2) + ship/redo bands (70+/≤55) ≠ code (25/15/5/2, gate 60)** — the prompt's grey zone straddles the real gate; `CATASTROPHIC` allowed in output but missing from its own table (code charges 50).
- **[P3] `scoring.js:18-19` header docstring stale on entity penalties** (says 30/20/10; real: 25/15/2 — client mirror is correct).
- **[P3] Three different PASS bars on the same 0-10 scale across the three eval prompts** (5+/5+/7+) — display-only but same quality reads PASS in one panel, FAIL in another.

**Confirmed coherent:** semantic & compliance 0-10→×10 conversions; severity vocabularies match `SEVERITY_POINTS` keys; repair-verification six-field contract + 0.7 confidence gate; entity/incremental/final/text consistency scales + gates; sheet-2x4 nested-vs-flat two-shape split parsed correctly on both sides; bbox 0-1000 convention matches denormalizer; `scoreThreshold=60` single-sourced across 6 consumers incl. client mirror; entity penalty cap 40 applied at all 4 sites; three-stage double-count guard correct.

---

### 8. Money paths — Stripe, credits, referral, Gelato (Wave 1) ✅

**Verdict: one true P0 (free printed books) and a cluster of referral-balance
concurrency leaks; the core webhook/credit-spend idempotency is genuinely
solid.**

- **[P0] Unauthenticated-of-payment Gelato print order — free printed books** — `print.js:77-295` — `POST /api/print-provider/order` requires only `authenticateToken`, then submits a **real** Gelato order (`orderType = isUserTestMode(req.user) ? 'draft' : 'order'` → `'order'` for any non-admin) with no credit check, no Stripe session, no `orders` row. Frontend labels it "Developer mode: direct print" but the server has no gate. Any logged-in user POSTs `{storyId, shippingAddress}` → physically printed, shipped book at operator cost. **Fix:** admin-gate (like the retry endpoint at :1212) or require a paid orders row.
- **[P1] Referral CHF balance spendable N times via concurrent checkouts** — `print.js:1816-1902` — discount baked into the Stripe session BEFORE `holdPending()`; concurrent sessions each carry the full discount; losers' sessions never decrement balance on completion (`noPending`). Comment dismisses as "small loss" — unbounded under concurrency. **Fix:** hold before session creation; release on failure.
- **[P1] `payment_status='paid'` is transient — first-time-buyer gate and cashout query a value that no longer exists** — `orders.js:25-26`, `referralBalance.js:421`, `server.js:1051` vs `gelato.js:192,436` (overwrites to `'processing'`/`'completed'` within seconds). Consequences: `hasPaidOrder()` false for real customers → "referral codes only for first-time customers" rule non-functional (CHF 20 leak per redemption, limited only by the one-code-per-user claim); `getRefundableAmount()` finds nothing → cashout-to-card silently broken for everyone. **Fix:** standardize terminal status; use `gelato_status` for fulfillment.
- **[P1] Cashout double-refund under concurrency + ignored spend result** — `print.js:1668-1704` — Stripe refund BEFORE `spendForRefund()`, return value discarded; two concurrent cashouts → refunds up to 2× balance (partly masked today by the status bug above).
- **[P2] Trial cap farmable** — fresh anon account needs only Turnstile + an in-memory fingerprint map that resets on deploy and **skips itself when full** (`trial.js:416-447`); no email needed before cost-bearing generation.
- **[P2] Buffered webhook events never auto-replayed** — `server.js:1191-1218` — money-taken-no-goods until an operator manually notices; admin route only lists/dismisses.
- **[P2] Book-purchase token reward granted at payment, never clawed back** on fulfillment failure or refund (`server.js:960-992` vs `gelato.js:443-457`).
- **[P3]** Credits webhook metadata fallback is a latent trust-the-client path (`server.js:752-758`); zero-amount Stripe session reachable only if shipping ever becomes free (invariant note).

**Checked and clean:** webhook signature verification ordering; credits double-grant defense (FOR UPDATE + in-lock re-check + partial unique index); book double-fulfillment defense (unique session id in-tx); atomic credit spends everywhere; atomic-claim refunds; referral balance conditional updates + CHECK constraints + per-session unique indexes; self-referral & one-code-per-buyer TOCTOU-safe; server-side price derivation for credits AND books; Gelato webhook constant-time secret; refund routed via persisted `stripe_mode`; integer-cent arithmetic, floors round against the user.

---

### 9. Periphery — email, SEO/SSR, R2, landmarks, health (Wave 2) ✅

- **[P1] Anonymous-account cleanup orphans stories + R2 objects — GDPR gap** — `trial.js:1560-1613` + `server.js:1828-1834` — `stories.user_id` has **no FK/CASCADE** (unlike characters/jobs/orders/story_images); the 48h anon sweep deletes users but never their `stories` rows, `story_images`, or R2 artefacts. A purged child's story (title, dedication, likeness-derived illustrations) persists forever with a dangling user_id. Admin user-delete does it correctly — the sweep must mirror it. **Fix:** delete stories + `deleteStoryArtefacts` in the sweep; add the missing FK.
- **[P2] Stored HTML injection into story-completion email** — `email.js:153-165` (`fillTemplate` no escaping) + user-controlled title (`stories.js:3277`) → markup injection into emails (also order emails). **Fix:** escape in `fillTemplate`.
- **[P2] Reflected XSS in `/s/:shareToken` redirect body** — `sharing.js:794` — OG tags escape the title; the visible anchor uses un-escaped `rawTitle` → script executes for anyone landing on a crafted shared link. **Fix:** use the escaped title.
- **[P2] Admin-targeted HTML injection in failure-alert emails** — `email.js:897-910,949-967` — `${userName}`/`${errorMessage}` unescaped (the daily-summary builder next door has `esc()` and uses it).
- **[P3]** `emailSent` always truthy (structured result treated as boolean, `jobs.js:153-159`); PII (emails) logged on every send/auth event across ~10 files (Railway retention outside GDPR cascade); unauthenticated debug endpoint `/api/debug-landmarks/:city`; `/api/health` is a static stub (checks nothing — can't catch analyzer/R2 outage; drives Railway healthcheck).
- **Remaining base64-assuming consumers (the "thumbnails broke" class):** `admin/users.js:577` (hasThumbnail false for URL-only), `admin/testlab.js:585-586` (`test-image` 404s — sibling `baseline-cover` was fixed, this one missed), `admin/database.js:408` (story-list flag ignores image_url).
- **Notes:** SEO/SSR has NO user content (hardcoded dictionaries + escaping — clean); **Italian is entirely absent from SSR/hreflang/sitemap** (`SUPPORTED_LANGS = {de,en,fr}`); R2 is public-CDN by design (no expiry risk), delete-prefix guard good; landmark photo **attribution stored but never surfaced in viewer/PDF** — licensing-compliance gap for CC-BY images; trial deferred email idempotent and safe; storyIdeas rate-limiting sound.

---

### 10. Repair/blend/composite — SAM, DINO, geometry (Wave 1) ✅

- **[P1] Stratified composite crashes: `preset.name` undefined variable** — `sceneComposite.js:2086` — step 2 references `preset.name`; only `presetName` exists in scope → ReferenceError on every run reaching the back-stratum fill (the DEFAULT strategy for 3+ character depth scenes) — whole composite fails after paying for empty-scene + anchor-plate Grok calls. **Fix:** `aspectRatio: presetName`.
- **[P1] `/repair-workflow/artifact-repair` calls `gridBasedRepair` with a completely wrong signature — silent no-op** — `regeneration.js:5896` — passes `(scene, {retryHistory})` vs real `(imageData, pageNum, evalResults, options)`; throws immediately on missing outputDir, per-page catch swallows, endpoint returns 200 with `issuesFixed:0`. "Repair artifacts" in the UI always reports success while doing nothing. (Cross-ref §5's artifact-repair version-stamp finding — this endpoint is broken twice over.)
- **[P1] "Face repairs REQUIRE SAM head mask — no rembg" NOT enforced on the production fallback** — `images.js:11455-11461,11563-11564,11820` — face-insert enforces `requireMobilesam`, but its catch falls back to legacy blended, which degrades to rembg twice. Exactly the SAM-outage scenario the rule was written for silently ships rembg-clipped repairs; testlab throws instead. **Fix:** restrict fallback to non-mask failure classes.
- **[P2]** (abridged) `gridBasedRepair` pushes to never-initialized `history.errors` → TypeError masks the real failure; production face-insert documented "FEATHER-ONLY" but `bgBorderMatch` defaults true (testlab A/Bs a knob production runs blind — decide + log in decisions.md); production has NO style gate while its own comment claims style-drift falls back (testlab has the gate — added for a real 85/100 style-flip); hand-rolled `idx*3` RGB loops assume no alpha (one PNG intermediate away from colour-noise blends); fullScene feather blurs OUTWARD (no erode-then-feather — the exact bug commit 4c20610c fixed, only face-insert path got it); repairVerification force-resize `fit:'cover'` would silently crop on dimension drift; samBlend bg-material shift has no minimum-sample guard (3-pixel "material" → speckled fringe).
- **[P3]** (abridged) swapped `bboxW`/`bboxH` names; inclusive containment off-by-one; `featherPx:0` still blurs 1.2; inconsistent stride handling (half-defended); production blended/cutout paths still stack 3-4 JPEG generations (testlab's lossless-PNG fix didn't reach them; cutout's own comment claims otherwise); PNG bytes in `data:image/jpeg` URIs; testlab `fit:'fill'` stretch; fullScene missing registration guard.
- **testlab ↔ production drift inventory:** 7 near-copy blocks between `runQwenInsertStage` and `grokFaceInsertRepair` (square-crop, box mapping, whiteout, head-pose, prompt, round-2 re-detect, maskFetcher) + a third orchestration copy in `runCharRepairStage`; behavioral drift: colorCorrect default, headClipMode, PNG vs JPEG final, style gate presence.

**Checked and clean:** `computePresetAlignedExtract` clamp complete (Jul 9 fix verified); `maskBlurThreshold` correctly fixes the blur/threshold-erode bug at all routed sites; colour-correction division/clamp guards thorough; coordinate spaces consistent (SAM decode at explicit dims, DINO px↔norm clamps, square-face-crop fix mirrored in production); island-dropping/IoU/white-card/salvage geometry sound; coverComposite defringe/chroma-key math verified; repairGrid padding/inset bounds-safe.

---

### 11. Entity consistency, Visual Bible, story parsing (Wave 1) ✅

- **[P1] Entity-check cell-letter→page mapping diverges from the grid the evaluator saw** — `entityConsistency.js:1003-1057` vs `1926-1932` — `cellToPage` built by collection order; grid assigns letters after SORTING by pageNumber; three divergence causes (covers appended last but sorted first; failed crops silently dropped; multi-grid letters restart per grid while cellToPage indexes across the group). Consequence: entity issues stamped onto the WRONG pages → consolidator pipes findings into the wrong page's Grok repair. **Fix:** build from `gridResult.manifest.cells` (carries true letter→page, verified).
- **[P1] Mid-stream retry replays into a stateful progressive parser — mixed-generation stories** — `textModels.js:257-258,385` + `server.js:4461-4477` — `withRetry` wraps request+stream; attempt 1's chunks already fired into the parser (`emitted.pages` suppression), so images generate from attempt-1 text while the final parse stores attempt-2 text. Title/cover can come from the aborted attempt. **Fix:** fresh parser per attempt.
- **[P2]** (abridged) TDZ bug silently disables the VB relevance filter (`storyHelpers.js:3858` uses `draftSceneDescription` declared at `:3938`; catch swallows the ReferenceError → every page carries the full VB list, defeating the documented ~500-token optimization); `cachePrefix` silently dropped for google/xai providers → consolidator runs with ZERO instructions on those models (`textModels.js:539-641` vs `feedbackConsolidator.js:303`); `visual-bible-analysis` template placeholder never substituted (double-brace key bug — every VB image-analysis call runs with an empty elements list); duplicate fallback IDs (`length` evaluated before assignment → all id-less entries get XXX001; ANI/ART/LOC have no dedupe); empty-scene prompts are the ONE image path never VB-id-sanitized (ids become paintable text — the exact failure the sanitizer prevents); prompt-length truncation amputates the tail where EXACT POSES/EXPRESSIONS were deliberately placed (cross-ref §3's Grok truncation finding — two agents independently); scene-count mismatch is warn-only + `stop_reason` never checked (max-token truncation ships a shorter book silently); two incompatible cover page-number conventions (−1/−2/−3 vs 0/−1/−2 — cover queries off by one slot); cascade face merge missing its best depth signal in the entity-check path (5th arg omitted; other site passes it).
- **[P3]** (abridged) `fillTemplate` strips user brace tokens + order-injectable; templates cached at process start (local-dev footgun); delimiter markers not line-anchored (name-injection theoretical); sanitizer ID pattern lacks word boundaries + mangles dotted variants; English-only stopwords in object canonicalization + same-label distinct objects always merge; it/es language gaps in 4 places (regional codes get English headers — branch dead for most real traffic); `updateVisualBibleWithExtracted` latent crash + vehicle skip (legacy); VB-id resolution call-order dependent; Gemini streaming ignores options / fallback drops system; `cachePrefix`+`prefill` trap armed; clothing backstop always fires for one-term outfits.

**Checked and clean:** bbox conventions consistent across all entity paths incl. pad/unpad round-trips; parser robustness (balanced-brace, fences, three-tier recovery, draft-header tolerance) genuinely good; `$`-escaping + regex-metachar hardening present; scene-metadata consumers all null-guard with sensible fallbacks; VB grid 9-cap with loud overflow; avatar/clothing fallback chains coherent and logged.

---

### 12. Python photo analyzer — memory, concurrency, contracts (Wave 2) ✅

- **[P1] Cross-request photo mix-up: temp file keyed by PID under multi-threaded waitress** — `photo_analyzer.py:868` — all threads share one PID → concurrent `/analyze` requests share `input_{pid}.jpg`; thread A can read **user B's photo** → face thumbnails/avatar reference from the wrong child. Silent cross-user data leak + delete races. **Fix:** `cv2.imdecode` in memory (like every other endpoint) or uuid filenames.
- **[P1] `MAX_IMAGE_DIM` is dead code — the decompression-bomb cap does not exist** — defined at `:47-49`, referenced nowhere; 16MB body cap doesn't stop a 20000×20000 PNG → 1.2GB allocation + full-res rembg; analyzer OOM takes the whole shared container down. **Fix:** apply the downscale after every decode.
- **[P1] `/analyze` returns HTTP 500 for "no face detected" — Node's graceful branch unreachable** — `:1735-1738` vs `avatars.js:1184-1212`, `trial.js:1655-1683` — users get "service error" instead of "upload a clearer photo"; the exact 200-vs-500 muddle the `sam_no_mask` rename fixed for `/figure-mask`, still present here.
- **[P1] Shared ML models not thread-safe under waitress's thread pool** — MobileSAM (ultralytics documents not-thread-safe; concurrent calls can compute masks against the other request's prompts), Haar cascades, MTCNN dnn Nets — no inference locks anywhere (only the GDINO *load* is locked). **Fix:** per-model inference locks or thread-locals.
- **[P2]** (abridged) `_release_memory()` hygiene only on SAM/DINO endpoints — `/analyze` (highest traffic, full-res rembg) will reproduce the SAM-500 RSS creep; zombie-compute fix incomplete (30s aborts on `/analyze`/`/remove-bg` still stack work; `fetchMaskWithRetry` ×5 re-fires are exactly the forbidden pattern); u2net weights downloaded from network at every cold start (failure → silent MediaPipe-quality degradation); `waitress==3.0.0` pinned to a version with CVE-2024-49769 + heavy ML deps float while numpy hard-pinned (the exact combination that already broke staging once); `os.cpu_count()` = host cores not container quota + torch intra-op unconfigured → N×M oversubscription (credible contributor to the original 150s incident); `/analyze` 500s on transparent PNG (RGBA→JPEG save).
- **[P3]** (abridged) health probe wired to nothing + `no_mask` doesn't flip status + RSS never thresholded; `min_confidence` silently ignored in MTCNN branch (~6× strictness variance by import luck) + no `detector` field in response; last user photo persists on disk indefinitely (debug write missed by the cleanup commit); sceneComposite's `/split-reference-sheet` call has no timeout and throws (sibling has 10s + fallback); input-validation nits (negative `selected_face_id`, unvalidated `cached_faces`, base64 errors → 500); GDINO reaper skips `malloc_trim` and mutates globals unlocked; DeepFace imported but not in requirements (3 endpoints 500 in prod, no callers); local-dev binds 0.0.0.0 with CORS `*`.

**Checked and clean:** models load once; `/figure-mask` is the exemplar endpoint (memory discipline + clean 400/503/200-fail/500 contract that Node fully honors); 16MB byte-cap works; temp cleanup on all 4 exits; no debug=True; 150s/300s timeouts coherent with measured durations; MediaPipe per-request instances thread-safe; every production analyzer call has a fallback path (Python outage degrades, doesn't block); Dockerfile torch/numpy ordering correct. Full Node↔Flask timeout table in agent report.

---

### 13. Database layer — pools, transactions, migrations, growth (Wave 2) ✅

**Verdict: SQL-injection surface is CLEAN (all 11 dynamic-SQL sites verified
safe). The disease is transactional: fake pool-level transactions (P0,
independently found by two agents) and a dangerously unconfigured second pool.**

- **[P0] Fake transactions: `BEGIN`/`COMMIT`/`ROLLBACK` through the pool** — `characters.js:455-795`, `avatars.js:1275-1378,2245-2497`, root `database.js:47-56` — each statement checks out an arbitrary connection: `FOR UPDATE` locks release instantly (the races these blocks were written to prevent are fully open), connection A returns to the pool inside an open transaction, unrelated requests silently join it, and a stray `ROLLBACK` discards *their* writes. Works "by luck" under low concurrency. **Fix:** dedicated-client pattern (already used correctly in `referralBalance.js`, jobs cancel, Stripe webhook) + a grep guard forbidding `dbQuery('BEGIN')`.
- **[P1] Second unconfigured Pool in server.js** — `server.js:521-534` — serves webhooks + the entire pipeline: **no `pool.on('error')`** (idle-client error = process crash killing every in-flight generation), no statement_timeout (multi-MB JSONB UPDATE can hang forever), connectionTimeout 0 (webhooks queue unbounded behind pipeline traffic inside Stripe's 10s window), SSL config diverges from the modular pool. **Fix:** one shared configured pool.
- **[P1] Reserve+INSERT non-atomic (confirms §2)** — debit, ledger, job INSERT are three pool statements; idempotency pre-check is check-then-act; failed INSERT = debited credits with NO job row → **no refund path can ever recover it** (all refund paths start from a story_jobs row).
- **[P1] Watchdog no-refund + 1h evidence deletion (confirms §2)** — additional detail: the `/status` refund path stops matching once the sweep flips status to `failed`, so the two mechanisms actively defeat each other.
- **[P1] Dead schema-init path already caused a prod incident** — `database.js:64-670` exported, called by NOTHING (verified) — commit c97974fb added `trial_completion_email_sent_at` to the dead list; never reached prod; trial claim emails threw for days (2026-06-10). Dead copy also disagrees with live schema (story_images missing is_test; pricing seeds differ). **Fix:** delete it; the skills warning exists because the code still needs it.
- **[P1] Story persistence non-transactional + `data='{}'` window** — `database.js:1659-1906,2038-2067` — N image upserts → R2 → recompute → final blob UPDATE, no transaction; crash mid-sequence corrupts the dbIndex↔array mapping used everywhere; readers can see the empty blob; statement_timeout on a >100MB JSONB write is a plausible trigger, not just crashes.
- **[P2]** (abridged) MAX+1 version-index race + ON CONFLICT silent overwrite (confirms §5); full-blob RMW last-writer-wins (confirms §5); deletion cascade incomplete (admin path misses `story_drafts`; anon sweep misses stories/files/R2 — confirms §9's GDPR finding; no FK on stories.user_id/characters.user_id to catch what code misses); `consolidator_calls` unbounded orphan growth (full prompts + raw LLM responses, zero DELETE anywhere — verified); `logs` table unbounded + ZERO indexes, seq-scanned by every admin dashboard load; pricing seed drift (fresh DBs get pre-reprice prices; the two repricings live only in the dead init path — need `009_pricing_current_model.sql`); JSONB TOAST pressure quantified by the code's own audit trail (~150MB pre-strip; one story blew the 256MB limit; V8 string cap hit; defense is three best-effort strip layers that DISCARD data when R2 is down).
- **[P3]** migration runner lacks advisory lock (concurrent boots double-apply); `/api/debug-landmarks/:city` queries a table migration 001 DROPS (permanently 500s, unauthenticated); three regen paths record stale `balance_after` in ledger + may ledger-insert on 0-row deduction; `schema_migrations` leftovers break two admin endpoints on staging; duplicate `dbQuery` wrappers + two pools = same duplication disease as the dead init; boot zombie cleanup zeroes reservations BEFORE refunding (crash between = lost refunds).

**Checked and clean:** zero injectable queries (full 11-row dynamic-SQL table in agent report — all validated identifiers/numeric indices); `referralBalance.js` transactions genuinely well-built; layered webhook idempotency (partial unique indexes); hot-path indexes present except `logs`; `persistStyledAvatar` path validation exemplary; no error-swallowed-to-empty-result cases in the core layer.

---

### 14. `server.js` monolith — middleware, webhooks, lifecycle (Wave 2) ✅

- **[P0] Stripe webhook: inner catch swallows ALL order/credit processing errors — the retry buffer is dead code for exactly the failures it was built for** — `server.js:1156` — the entire money path (credits grant, order+token tx, referral) sits in a try whose catch only logs and does NOT rethrow; control falls to `res.json({received:true})`. The outer catch (1191) that buffers to `stripe_webhook_retry` can only see errors thrown outside that try — essentially nothing. DB blip during order INSERT → swallowed → 200 ack → Stripe never retries → nothing buffered → monitor silent → **customer charged, no order, no credits, no alert**. **Fix:** rethrow from the inner catch.
- **[P1] Inline `PATCH /api/stories/:id/page/:pageNum` is dead (shadowed) — admin image-revert broken in production** — `server.js:2659` vs `stories.js:3086` (mounted earlier, wins) — the client's `updateSceneImage()` PATCHes `{imageData}` for "revert repaired image" → stories.js rejects it with 400 `'Provide text or sceneDescription'` **today**; scene-description edits silently lose summary re-extraction. **Fix:** single owner; port the imageData+summary logic into stories.js.
- **[P1] No SIGTERM handler + zombie-sweep overlap race, reconstructed line by line** — new container's boot sweep (8053) fails/refunds jobs the OLD container is still processing (Railway overlaps); old container keeps burning spend (`checkCancellation` ignores `'failed'`), may set `completed` → refund + story (confirms §2's P1 chain with the deploy as trigger); then SIGTERM kills the old container with no draining, no checkpoint flush. **Fix:** SIGTERM handler + sweep claims only heartbeat-stale jobs.
- **[P1] Parked promises without `.catch` → unhandled rejection = process crash killing ALL jobs** — `server.js:3353` (scene expansion, awaited minutes later), `4112`, `4646` (a `.finally`-derived promise with NO handler) — `startCoverGeneration` has the no-op `.catch` with a comment describing exactly this crash class; siblings didn't get it. No process-level `unhandledRejection` handler either.
- **[P2]** (abridged) CORS allows any `*.railway.app` tenant with credentials:true; legacy static fallback serves the PROJECT ROOT (`data/config.json` with API keys — latent, one broken build away); watchdog no-refund (3rd confirmation); `apiLimiter` registered BEFORE the webhooks (Stripe's shared IP pool can 429 → dropped `checkout.session.completed` unrecoverable per the P0); legacy `STRIPE_WEBHOOK_SECRET` interpreted as TEST secret (live-only legacy config silently makes every real order a Gelato draft); `POST /api/landmarks/discover` unauthenticated (external-fetch amplification + unbounded Map growth); ~250 lines dead code inventoried (shadowed routes, dead functions/imports/env knobs, ~110 lines dead usage scaffolding, base64-heavy `characterPhotos` computed per job and passed to a parameter that's never read); no error middleware/API 404 handler (CORS rejection → HTML 500); heap profile confirms §2's memory finding.
- **[P3]** `savePartialStoryFromCheckpoints` costume projection can never run (typeof-guard on a name not in scope); PII at info level in webhook logs; staging URL fallback silently points trial-claim emails at PROD; `dropInlineBase64` mutates structures shared with the returned resultData (footgun); generationLog kept forever in the blob; redundant per-mount json parsers + global 50MB parser runs before auth.

**Checked and clean:** raw-body-before-json ordering; credits/order/referral idempotency layers; timing-safe secrets; malformed-URL + prerender traversal guards; ALS-keyed usage sink + logger (no cross-job leakage); checkpoint lifecycle; deliberate no-cancel-after-completion documented; migrations fail loud; keep-alive 65s > proxy 60s; DI seam for processStoryJob clean; no route-path overlaps between stories.js and regeneration.js. Full section map + inline-endpoint auth table in agent report.

---

### 15. Prompts ↔ parsers — story/scene side (Wave 2, sub-audit) ✅

- **[HIGH] `scene-iteration-free.txt` never asks for `sceneIntent`; `images.js:9551` hard-requires it** — every free-iterate burns a forced retry (+16k tokens) and logs ERROR, because the prompt never requested the field it's punished for omitting.
- **[HIGH] Trial cover spec discarded wholesale** — `story-trial.txt:139-173` emits `---COVER SCENE---`; final parser matches only `---COVER SCENE HINTS---` (`unified.js:264`); the streaming `onCoverScene` callback is wired to NOTHING; and the "no hints" fallback is unreachable (extractCoverHints always returns truthy stubs) → trial covers generate from `{hint:''}`.
- **[HIGH] `scene-expansion.txt` dead params lose real information** — `SCENE_CONTEXT` (setting/time/weather) built and discarded (sibling iteration template carries it); 7 dead params total.
- **[MEDIUM]** (abridged) `checks` vs `draftValidation` mismatch → `checksRun` always `{}` on strict iterate; `textPosition` referenced 4× but not in the strict schema (siblings disagree on the same contract); `shot` required by a rule but absent from the expansion schema (4 readers silently fall back — the exact bug the code comments say was fixed for unified); expansion schema omits 7 fields the parser reads (`sceneIntent` → eval prompts ship "THIS IMAGE DEPICTS: (none declared)" on Haiku-expanded pages); `landmarkContext` requested twice, read nowhere; **`story-unified.txt`'s worked example omits its own required fields** (trains the model to skip `sceneIntent`/`expression` — and the unified path has no retry guard); consolidator self-contradicts on `scene_fix.instruction` vs `fix_instruction` (primary parsers read `instruction`; the rule text teaches `fix_instruction`); deduction-table mismatch (confirms §7); `scene-repair.txt` dead behind `if (false)` with a drifted schema that would drop interactions/emptyScenePrompt/textPosition if re-enabled.
- **[LOW]** `castChanges`/`bbox` unread; `CATASTROPHIC` missing from `SEV_RANK` (sorts below CRITICAL in Grok fix ordering); dotted `LOC002.1` truncated to `LOC002` on covers; cover parser reads `gazesAt`/`perspective`/`depth` the prompt bans; trial `setting.weather`/`lighting` orphans (parser reads `weatherVisibility` nobody asks for); `aerial` outside the shot enum; guides files (all 5) verified clean — 184 sections, 0 regex failures, 0 duplicates.

---

### 16. Prompts ↔ parsers — image/repair/avatar side (Wave 2, sub-audit) ✅

- **[HIGH] `{ELEMENTS_LIST}` never filled (independent confirmation of §11)** — braces-inside-key bug; every VB analysis ships without its element list + a WARN fires every call.
- **[HIGH] Same bug in `avatar-retry-prompt.txt`** — `avatars.js:2987` — the safety-block retry ships "wearing ." — the entire outfit is stripped from the one path that runs after a safety block.
- **[HIGH] `art-styles.txt` vs `sceneComposite.js` hardcoded copies — `comic` is a DIRECT contradiction** — art-styles/storyHelpers: "no halftone dots, no CMYK"; `sceneComposite.js:906`: "halftone/Ben-Day dots, vibrant CMYK colours" — the exact bug the art-styles header claims was fixed. Steampunk gears-on-people guard and watercolor opacity guard also missing from the composite copies. Plus `realistic` missing entirely → photorealistic stories' blend pass requests **watercolor** (`sceneComposite.js:948` fallback).
- **[MEDIUM]** (abridged) bbox truncated-JSON recovery regex can NEVER match (requires field order/absences the template forbids — the salvage path is dead code; complements §3's finishReason ReferenceError on the same path); `character-repair-inpaint.txt` missing `{regionDesc}` (template path never tells Grok where the magenta region is; only the fallback does); visual-inventory `scene_summary`/`main_action` requested with dedicated RULES lines, dropped on the floor; avatar eval TASK 2 (`ageCheck`) scored but never parsed + `faceMatch.glasses` ignored (confirms §7's field-path finding); semantic parser reads 3 fields the template forbids (always `[]`, zero consumers); admin re-evaluate builds a divergent semantic prompt (no `buildSemanticPrompt` chokepoint); `styled-costumed-avatar` templates test-only; `image-inspection.txt` dead + would pass 0-1000 bbox through unnormalized if wired.
- **[LOW]** lowercase placeholders (`{figuresSummary}`, repair-template tokens) invisible to the unfilled-detector (typo ships literal braces with no warn); third brace convention (`{{X}}` manual replace); `{CLOTHING_CATEGORY}` dead param; sheet style-eval bare-word replace produces "REQUESTED_STYLE: REQUESTED_STYLE: label" (the exact pattern fixed for its sibling); textless cover variants make `STORY_TITLE` a dead param; `cleanScore` logged not recorded pass-1; character-analysis salvage regex keys mostly don't exist in the template.

**Chokepoints verified clean:** `buildEmptyScenePrompt` (all 7 callers), `buildEvaluationPrompt` (all 3), `applyRepairStyleGuard` (all 6 repair templates, both loaders). ~30 templates fully verified clean on both sides (table in agent report).

---

### 17. Provider libs — grok/runware/textModels/models.js (Wave 2) ✅

- **[P1] The style-transfer NaN fix is incomplete — `$NaN` totals still reachable on the DEFAULT Gemini avatar backend** — the landed fix stamps `usage.cost`, but **neither `addUsage` sink reads it** for provider `'gemini_image'`; `calculateCost` on a per-image-priced model does `(tokens/1e6)*undefined = NaN`. `prepareStyledAvatars` runs this exact path in the unified pipeline. **Fix:** root-cause in `calculateCost` (handle `perImage`) + fold `usage.cost` into `direct_cost` for all providers. (Same family as §3's four no-op tracker calls.)
- **[P1] `callGeminiTextAPI` never retries HTTP failures** — `textModels.js:554-581` — `!response.ok → throw` sits OUTSIDE the `withRetry` wrapper (fetch only rejects on network errors); a Gemini 429 burst during parallel evals fails pages outright instead of backing off. Anthropic/xAI paths do it correctly.
- **[P1] Gemini text fallback chain terminates in a RETIRED model** — fallback order ends at `gemini-2.0-flash`, which models.js itself documents (2026-07-18) as retired/404. When Grok is unavailable the "last resort" is a guaranteed 404.
- **[P2]** (abridged) Grok output-aspect drift correction is dead code (`skipOutputPadding` defaults `true`, no caller passes `false` — wrong-aspect pages ship uncorrected while the code claims the invariant); Runware 3000-char limit unenforced at lib boundary + the two backend-override branches send raw prompts before the truncating logic (and hardcode 1024² — confirms §3); no Grok safety-refusal taxonomy (callers substring-sniff `'400'`; avatar loop retries deterministic moderation failures ×3, up to 6 fetches per category); duplicate disagreeing Grok aspect-preset lists (`editImageWithPrompt` snaps to `4:5`/`5:4` which `grokAspect.js` says Grok rejects — resurrects the 422 class the helper was built to fix); **`qualityModelOverride` from req.body flows unvalidated into the Gemini URL path** (non-admin endpoint; arbitrary expensive models or URL path injection — pairs with §4's ungated overrides); Gemini API keys in query strings across ~40 call sites (proxy/APM/error-message exposure; Grok+Anthropic use headers); six Gemini image-gen fetches with NO timeout (up to ~15 min stall inside retry); Runware model-ID and pricing tables disagree across files (`runware:100@1` vs `runware:5@1`; miss → $0.035 default = 58× real cost; "SDXL" backend actually points at FLUX Dev); `getImageDimensions` cache key = first 100 chars of data-URI (identical across same-encoder JPEGs — collision returns wrong dimensions → mispositioned overlays).
- **[P3]** (abridged) `b64_json` unvalidated (`data:...;base64,undefined` propagates); unguarded `data.content[0].text`; Grok 429 never retried (Pro = 30 RPM); `withRetry` substring heuristics on untruncated bodies (runware truncates for this exact reason); mid-stream retry double-delivers chunks to `onChunk` (cross-confirms §11's parser-replay P1); semaphore slots held through backoff sleeps (head-of-line blocking in brown-outs); Qwen PNG-as-jpeg mislabel; Grok cost table duplicated in lib; dead ternary; half-supported `_role` reference objects.

**Invariant table verdicts:** input-aspect coercion ✅ enforced; aspect-snap-all-callers 🟡 partial; output-drift correction ❌ dead code; Runware limit 🟡 partial; ACE++ root-level ✅; 2.5-flash-for-eval 🟡 convention only (override unvalidated); aspect centralization 🟡 (Runware square, avatar `'9:16'` matches default by luck); timeouts 🟡 (libs yes, six images.js fetches no); safety-terminal 🟡 (Gemini yes, Grok no); cost-never-NaN ❌; dev-override validation 🟡. **Checked and clean:** packReferences slot budget + padding; magenta-prefix cap math; Runware retry discipline; text-usage single chokepoint with `__accounted` guard; TEXT_MODELS↔pricing coverage complete; streaming watchdog timer cleanup; no hazardous module state (ALS-keyed usage safe).

---

### 18. Text overlay + PDF + cover typography (Wave 1) ✅

- **[P1] Overlay-failure fallback renders text OFF-PAGE and corrupts print page count** — `pdf.js:557,612-627,640-648` — overlay mode computes `imageHeight = pageHeight`; when a per-page overlay render throws (R2 hiccup — exactly what `resolveImageBuffer` logs), the fallback places text at `bleed + pageHeight` = below the trim edge; PDFKit auto-paginates → extra pages → declared Gelato pageCount wrong + spread parity flips for the whole rest of the book. **One transient R2 failure = misprinted book.** The combined-PDF sibling is correct; only `addPictureBookPages` is broken.
- **[P1] `POST /print-provider/order` builds the Gelato PDF WITHOUT rehydrating images** — `print.js:82-104` — post-R2 migration the raw blob has no imageData → covers skipped, every scene hits the missing-image branch (which also writes text off-page per the previous finding) → essentially blank book shipped. Siblings at :721 and :1128 rehydrate; this one missed. (Also the endpoint with §8's P0 — broken *and* free.)
- **[P1] Version switching / score recompute can serve an UNBAKED (title-less) cover** — `coverTypography.js:462-493` + `stories.js:3443-3501` + `database.js:1884-1895` — post-persist bake stamps ONLY the active version row and is permanently idempotent; initial-generation alternates are never stamped; the version picker and `recomputeAllActiveVersions` can flip to them → cover without title/dedication/branding in viewer, share, PDF, print. **Fix:** stamp all versions or restamp lazily on activation.
- **[P2]** (abridged) `languageLevel` not threaded to `generateTextOverlay` in the PDF path + owner preview (small/medium/large mask drift vs share viewer — 1st-grade books print with the wrong-size text zone on busy art); fixed 11px font is pixel-relative while Grok (896×1280) and Gemini (1024×1536) pages coexist → visibly different printed text sizes in one book; `iterateCover` feeds the TITLED cover as model reference (double/ghost title; images.js sibling correctly uses `${key}Art`); bake seed inconsistency (initial `seed: jobId`, restamp `seed: title` → title font/layout silently changes after first repaint); `${key}Art` lockstep inconsistent (repair saves art at v0, edit at lockstep → later edit can revert a repair to stale art); overflow at max stage silently DROPS words from the printed page (console.error only); single over-long German compound overflows the polygon horizontally (no clip — `applyClipPath` is dead code); shared-viewer overlay cache never invalidates on text edits (key lacks text hash); cover typography margins vs print-spread crop — title can land ~3mm from trim / inside hardcover wrap (needs verification against real Gelato dims).
- **[P3]** (abridged) `client/src/utils/textOverlay.ts` is 100% dead code AND materially drifted (full 14-row constants diff in agent report — 9 rows mismatch; delete it to make the "preview right, PDF wrong" class structurally impossible); ~150 lines dead renderer code (`buildBlurLayer`/`applyClipPath`/`drawGradient` — the latter contradicts the client twin); **`docs/text-overlay.html` describes a pipeline that no longer exists** (CLAUDE.md points people at it — actively misleading); PDFKit Helvetica = WinAnsi only (emoji in a dedication aborts the whole PDF; DE/FR chars are safe); bake partial-failure leaves a cover permanently untitled (art row saved before titled row, idempotency keys on art); title-font glyph coverage unverified with `loadSystemFonts:false`; strip-fit throw can kill overlay-mode PDFs it doesn't apply to; pdf.js has its own `parseStoryPages` (drift risk); calm-region output computed per page but unused by the renderer.

**Checked and clean:** spread parity applied consistently at all 5 sites (incl. the dead client twin); preview⇄share⇄PDF all rasterize through the same `generateTextOverlay` (pixel-identical given same options — languageLevel gap is the one exception); canvas-measured wrapping (French apostrophes, multi-paragraph correct); no-calm-region can't crash; margin clamps bound every stage; mm↔pt and BOOK_FORMATS math verified; `drawImageCovering` clips; combined-PDF parity bookkeeping consistent; cover edit path exemplary; "title garbling" fix verified correct; "single-baker" claim HOLDS under the default flag (all title text funnels through `composeCover`); SVG escaping clean, no injection.
