# Prompt Inventory — every template, its consumer, and its pipeline stage

> Complete inventory of all files in `prompts/` plus the hardcoded prompt builders that
> live in JS. Generated from a full-code audit on 2026-07-17. **Keep this file updated
> when adding/removing/renaming a prompt template** — it is the single source of truth;
> CLAUDE.md only lists the most-touched templates.

Three loading mechanisms feed these templates:

1. **`server/services/prompts.js` → `loadPromptTemplates()`** — builds `PROMPT_TEMPLATES{}`
   from a ~50-entry `FILES` list. Derived keys: `sceneDescriptions` (alias of
   `sceneIteration`), `frontCoverTextless`, `backCoverTextless`.
2. **`server/lib/images.js` `LOCAL_PROMPTS{}`** — direct `fs.readFileSync` at module load
   (bbox-refine-overlay, iterative-placement-pass1/2, inpaint-grok-regions,
   style-transfer).
3. **Direct reads in feature modules** — the `*-guides.txt`, `art-styles.txt`,
   `generate-story-idea*`, `story-idea-requirements-*`, `grid-repair.txt`.

**Maintainer notes are a `#!` block (2026-09-21).** A template may open with notes for whoever
edits it — which pipeline sends it, what parses its output. Write them as consecutive lines
starting with `#!` at the very top of the file, ended by a blank line: `loadPromptTemplates()`
strips that block so it never reaches the model. A bare `#` heading is prompt structure and is
left alone. (`docs/decisions.md`, 2026-09-21.)

**What a call sees and is blind to** — per call, with the recorded reason or "reason not recorded", and the list of prompts/replies NOT stored in the DB: see the section *What each call sees / is blind to* at the end of this file (2026-09-23).


## Story text & ideas

| Template | Consumer | Stage |
|---|---|---|
| outline-analysis-imagefirst.txt | promptBuilders.js `buildOutlineReviewPrompt` | ANALYSIS instruction body for the split outline reviewer — its ONE consumer since 2026-09-21. `buildTextRefinePrompt` sliced its criteria out of it until then (A7); the refiner now carries its own, in text-refine.txt. Fed the deleted unified writers too until 2026-09-15 |
| outline-review.txt | storyHelpers.js `buildOutlineReviewPrompt`; server.js split-review seam | External outline review (split mode, default ON): Opus receives the writer's full output + the same analysis instructions + REVIEW HINTS (deterministic scene-consistency findings) and emits ANALYSIS + FIXES REQUIRED + STORY PAGES patches; owns all SEMANTIC scene-consistency judgment (decisions.md 2026-07-31) |
| story-beats.txt | promptBuilders.js `buildBeatsPrompt` (:7453; storyHelpers.js re-exports) | Beats-first pipeline step 1 (`pipelineMode: 'beats'`) + Test Lab `beats_scenes`: the PAGE PLAN, one plan line per page (no beat prose since 2026-09-02) |
| plan-check.txt | promptBuilders.js `buildPlanCheckPrompt` | Beats-first step 2: the ONE model call over a page division. **Sixteen numbered questions** (Q14 over-the-shoulder 2026-09-24; Q15 exciting start `EXCITING_START_DEF`, Q16 the ending's cast safe and together `HAPPY_ENDING_DEF`, both shared with story-beats.txt and must-fix, 2026-09-25) plus five output blocks the code reads — `ROSTER` (who is in frame per page, read from the who column alone; a fourth field `unlisted` names characters only the instant names → counter `CAST_NOT_IN_WHO_COLUMN`), `OBSTACLES`, `PEOPLELESS`, `WANTED` (Q4, each act's picture by arc sentence and page) and `ACTION` (Q12, each child's own action by sentence and page). Shared definitions (NAMING, WANTED_PICTURE, ENDING_EVENT, EXCITING_START, HAPPY_ENDING, DEED_AND_EFFECT, PAGE_CHANGE, TWO_HEIGHTS, act spans, `castActionRule`) are filled from code. Reads the arc hints since 2026-09-23 (Q13 checks page order and hint placement). Reads the planner's CAST block since 2026-09-25 (`{CAST_TABLE_SECTION}`, `castCoverage.castTableBlock`); answers still come from the plan lines, and the counter `CAST_PROMISE_BROKEN` holds each line to the block. Counters (server/lib/planCounters.js) do the arithmetic and are NOT shown to the checker. The same builder runs the post-replan recheck (`plan_recheck`, prompt stored since 2026-09-23). Inputs: see *What each call sees* below |
| arc-create.txt | promptBuilders.js `buildArcCreatePrompt` | Arc machine step 1 (beats pipeline, 2026-08-30): since 2026-09-24 the creator writes ONE arc, logic first — a STORY LOGIC block (`{ARC_LOGIC_SPEC}`, `arcLogicSpec()`: want and stakes, opposition, tagged figure facts, central figure, because/but chain), then `ARC:` and the numbered sentences, then a critique that checks logic first and counts nothing (`parseArcCreate`, `parseStoryLogic`). Two arcs + "Stronger: Arc N" before that |
| arc-panel.txt | promptBuilders.js `buildArcPanelPrompt` | Arc machine step 2: each panel model (arcPanelModels) proposes EXACTLY ONE solution on the committed arc + critique; advisory, parallel. Carries `{AVAILABLE_LANDMARKS_SECTION}` since 2026-09-21 so the critic sees the list the creator got. SENSE lens since 2026-09-23. Since 2026-09-23 (arc audit): "Answer in ENGLISH."; ENTRANCE / ASSUMED / SENSE read the creator's own telling-rule strings; CAST states `COMMISSIONED_CAST_DEF`; an ACTION lens (`EVERY_CHILD_ACTS_RULE`); landmark list without DESCRIPTION lines. A PLACE lens since 2026-09-24 (`ARC_PLACE_RULE`, the creator's own inside/outside telling rule). Deliberately NOT given (reasons mostly unrecorded): age mode, budgets, telling rules as a block, story shape, topic guide, challenge draw, `{CHARACTER_SOURCE_RULE}` — see *What each call sees* below |
| arc-retell.txt | promptBuilders.js `buildArcRetellPrompt` | Arc machine step 3: the SAME creator re-tells the story whole (FINAL ARC + Challenges taken + Used + fresh CRITIQUE); the critique feeds story-beats.txt `{ARC_WEAK_POINTS}`. Carries `{AVAILABLE_LANDMARKS_SECTION}` since 2026-09-21. The critique shape is ONE constant, `arcCritiqueSpec()`, shared with arc-create; since 2026-09-24 it writes the updated STORY LOGIC first (`{ARC_LOGIC_SPEC}`, the same spec as arc-create; the figure lists it replaced were `PREMISE_FIGURES_SPEC` / `INVENTED_FIGURES_SPEC`). Raw reply stored since 2026-09-23 (`rounds[].retellRaw`) |
| arc-hints.txt | promptBuilders.js `buildArcHintsPrompt`; called from beatsPipeline.js (`usageLabel: 'arc_hints'`, model `MODEL_DEFAULTS.arcHintsModel` = grok-4.6) | Arc machine step 4, the GROK HINT PASS (owner 2026-09-01): ONE outside look at the approved arc, emitting `ISSUE (s<N>): … → CHANGE: …` lines (sentence anchor since 2026-09-23) parsed by `parseArcHints`. Never another re-telling round — the hints travel forward into story-beats.txt and story-text-from-beats.txt as `{ARC_HINTS}`. Advisory: any failure clears the hints and the run proceeds. Fills `{CHARACTER_SOURCE_RULE}` (master `arc`, since 2026-09-20). Prompt and raw reply stored since 2026-09-23 (`arcReviewReport.hintsPrompt` / `.hintsRaw` / `.hintsModel`) |
| story-arc-review.txt | promptBuilders.js `buildArcReviewPrompt`; testlab.js `runArcRoundsStage` | Lab-only arc review round: the arc plus optional audit findings in, a revised arc out (`parseArcReview`). The production arc stage is the arc machine (arc-create / arc-panel / arc-retell / arc-hints). Check 11 carries `{ARC_PLACE_RULE}` (2026-09-24) |
| challenge-catalogue.txt | read directly (`fs`) by promptBuilders.js `buildChallengeIdeasSection`, storyIdeas.js `buildIdeasPromptContext`, testlab.js | A DATA file, not an instruction template: the obstacle catalogue, sampled at random per run (~5x oversupplied, filtered by age band / peril / category cap) so the same brief rerun does not reach for the model's default obstacle. Column 5 holds the age bands |
| premise-shapes.txt | read directly (`fs`) by storyIdeas.js `loadPremiseShapes` / `pickPremiseShapes` → `{PREMISE_SHAPE}` (generate-story-idea-single.txt) and `{PREMISE_SHAPE_1}` / `{PREMISE_SHAPE_2}` (generate-story-ideas.txt) | A DATA file, not an instruction template: twelve premise shapes (`id\|name\|definition\|min age`). One shape per idea arm, picked in code — deterministic from the same seed `buildVariantInstructions` uses, never the same shape on both arms, never one above the youngest character's age, and the two-mains shape withheld from a one-main cast. Added 2026-09-21 (round 8 of the story-idea rating series) |
| pattern-seeds.txt | read directly (`fs`) by `server/lib/patternSeeds.js` `loadPatternSeeds` / `pickPatternSeeds` → `{PATTERN_SEED}` (generate-story-idea-single.txt) and `{PATTERN_SEED_1}` / `{PATTERN_SEED_2}` (generate-story-ideas.txt) | A DATA file, not an instruction template: ten pattern books for a main character aged two or under (`id\|name\|pattern\|variation\|the one that resists\|refrain`). One seed per idea arm, picked in code with the same determinism as the premise shapes, never the same seed on both arms. World-neutral: the world seed supplies the creature, this file the mechanism. Fills the SAME template slot as `{PREMISE_SHAPE}` — exactly one of the two is ever non-empty. Added 2026-09-21 (round 23) |
| do-not-write-list.txt | promptBuilders.js `buildDoNotWriteSection` → `{DO_NOT_WRITE_LIST}` | The ONE banned-category list every narrative prompt carries. Owns the list outright since 2026-09-03 — it was previously sliced back out of the unified writer template at runtime, so deleting that dead template would have stripped the list from production silently |
| story-arc-audit.txt | storyHelpers.js `buildArcAuditPrompt` | Blind arc audit: commission + arc only, exhaustive fault list for the reviewer ledger. Lab only since 2026-08-30 — the production arc stage is the arc machine |
| story-text-audit.txt | promptBuilders.js `buildTextAuditPrompt` (:9201) | ARC-INFORMED text audit, first of the two parallel audits: the final ARC + the page plan + per page TEXT and THE PICTURE SHOWS = the compact spec the writer read (`buildTextStagePictureSpecs` via `extractRefinablePages`: WHERE / WHAT HAPPENS / WHO with outfit in short form / ALSO IN VIEW, from METADATA + VB + wardrobe contract), `{ARC_HINTS}`. Fourteen questions since 2026-09-23: LOADBEARING scoped to what the plot turns on (an age or a colour never is), LIMIT covers deadlines, ASSUMED covers a named feeling never shown, CAUSE covers a refusal let go, new CONTRADICTION and ENDING; 15. HINT (only when the story has hints, `{HINT_QUESTION}`): a hint no page follows is a finding. No back cover, no commission. Prompt and raw reply stored in `textRefineReport.audits[]` (`prompt` since 2026-09-23) |
| story-text-audit-blind.txt | promptBuilders.js `buildTextAuditBlindPrompt` (:9183) | BLIND text audit, second of the two parallel audits (owner 2026-09-03): the page text and nothing else, seven reader-side questions (CONFUSION, CONTRADICTION, IDLE, TRANSITION, PAYOFF, ENDING and STYLE since 2026-09-23; STYLE checks every sentence against the injected `STYLE_RULEBOOK` and quotes each breach), same `FAULT[...]` line format so the two lists merge in code. The picture exemption is narrowed (2026-09-23): only what a picture shows at that instant (where someone stands, how something looks, an object in view) is exempt |
| story-text-proofread.txt | promptBuilders.js `buildTextProofreadPrompt` (:9116) | THE LECTOR, last step of the text chain: objective language faults in the final text, judged at the reading level (`{READING_LEVEL}`, short form, since 2026-09-23: plainest word, never a change of meaning), emitted as `PAGE n: '<quoted>' → '<corrected>'` lines. The corrections are applied IN CODE (`applyLectorFindings` in textRefine.js) — no apply model call; a finding whose quote is not verbatim on its page is dropped, which is the hallucination guard. The separate apply pass (story-text-lector-apply.txt) was deleted 2026-09-03 |
| story-text-diff.txt | promptBuilders.js `buildTextDiffPrompt` | THE GRAMMAR CHECK after the repair (was the diff pass; owner 2026-09-23: "No new sentences, the last pass is a grammar check"): only the pages the repair rewrote, BEFORE and AFTER as NUMBERED SENTENCES (`numberedSentences` / `pageSentences`, never cut inside a quotation) plus `FINDINGS THE REWRITE ANSWERED`. Answers only `PAGE n FIX A<k>: <sentence>` or `PAGE n RESTORE B<j> AFTER A<k>` (`parseDiffEdits`); `applyDiffEdits` refuses a FIX that is not one sentence or changes more than `GRAMMAR_EDIT_MAX_WORDS` (4) words, and inserts a RESTORE verbatim. Own contract since 2026-09-23 — no longer the lector's. Model `MODEL_DEFAULTS.textDiffModel` = gpt-5.6-luna-pro |
| book-audit.txt | bookAudit.js `auditStoryBook` (via `PROMPT_TEMPLATES.bookAudit`) | Final-book audit — the reader's-eye pass. Runs after covers, before persist: each page's TEXT then its SHIPPED IMAGE, interleaved as vision parts in reading order, ~6 pages per Gemini flash call. Emits `FAULT[IMG]:` / `FAULT[TEXT]:` routed by which artefact would fix it. Word/picture BALANCE is never a fault. Since 2026-09-24 each page also carries a BRIEF line between text and picture (`auditPageBrief`/`briefLine`: the moment drawn = `sceneIntent`, drawn with = the evaluator's ONE roster, `resolveExpectedCastNames`) — a character the words name but the brief did not draw is no fault, or `FAULT[TEXT]` — and an IMG line carries a type, `FAULT[IMG][<WEIGHT>][<TYPE>]`, from `{FINDING_TYPES}` = `evalBuckets.CONSOLIDATED_TYPES` (the consolidator's closed list) |
| story-child-critic.txt | storyHelpers.js `buildChildCriticPrompt` | Child critic of the arc, in PARALLEL with story-arc-audit: role-plays the youngest main character's age, retells the arc, then emits `FAULT[CHILD]:` lines for comprehension/boredom/fear only. Lab only since 2026-08-30 — the production arc stage is the arc machine |
| scene-review.txt | promptBuilders.js `buildSceneReviewPrompt` (:9553; storyHelpers.js re-exports) | Beats-first step 4: ONE review over ALL scene briefs, **37 tagged checks** (scene-review.txt:39, :47-84 — page repetition, visual arc, continuity, clothing, cast, gaze, props, scale, VB citation, required text and more) plus the code findings `CLOTHING_FINDINGS` / `BRIEF_FINDINGS`. It REWRITES briefs, and its rewrites ship verbatim. `{CLOSEUP_KEPT}` (7b, 10 — `shotVocabulary.CLOSEUP_KEPT_RULE`, shared with the Art Director's 11c and the `shot_widened` finding): a close-up the plan line asks for is restaged, never widened (2026-09-24, Lab 1433). Not shown the arc, page text, art style, full VB entries, cover hints, landmark photos, height order or age band — see *What each call sees* below (corrected 2026-09-23) |
| scene-hazard-audit.txt | testlab.js `runSceneHazardCountStage` (via `PROMPT_TEMPLATES.sceneHazardAudit`) | Lab-only measurement for the hazard-reduction loop: counts render hazards per page across a book's briefs (or beat SCENE lines) in 12 stable classes (CROWD, MULTIACT, GAZE, CONTACT, FORCE, ELEV, UNHELD, NEG, SCALE, TEMPORAL, LOC, SHOT); emits `HAZARD[<CLASS>]: p<N>` lines + `HAZARDS: <count>`. Report-only, nothing in the pipeline consumes it |
| story-bible-from-beats.txt | promptBuilders.js `buildStoryBibleFromBeatsPrompt` | Beats-first step 3: the WARDROBE contract and nothing else. Emits `---CLOTHING REQUIREMENTS---`. The Visual Bible and the cover scene hints moved OUT to scene-expansion-all.txt on 2026-09-11 (owner); clothing stayed because the styled avatars start the moment this call returns |
| scene-expansion-all.txt | promptBuilders.js `buildSceneExpansionAllPrompt` | Beats-first step 4, ONE call over ALL pages: `---VISUAL BIBLE---` ahead of page 1, then a brief per page — the book's covers included, as pages −1/−2/−3 briefed from code-written cover beats (`coverBeats.js`, 2026-09-24; the cover-hints section is gone). One author owns what is in each picture and what each thing looks like, so no page can cite an id nobody declared. Parsed by `extractBibleSections(raw, AD_BIBLE_MARKERS)` + `parseRefinedText(raw, pages, 'SCENES')`. 11c carries `{CLOSEUP_KEPT}` (the plan's close-up is restaged, never widened — the reviewer's 7b / 10 state the same constant, 2026-09-24). `{PLACE_SEPARATE}` + `{PLACE_INSIDE_OUTSIDE}` before the vantage paragraph say what counts as one location (2026-09-24; no scene-review counterpart, decisions.md) |
| clothing-review.txt | promptBuilders.js `buildClothingReviewPrompt` (storyHelpers.js re-exports) | Beats-first step 3b: wardrobe review over the bible's clothing contract, BEFORE the styled-avatar kickoff. Emits `---ANALYSIS---` + `---CLOTHING---`, parsed by `parseClothingReview` |
| story-text-from-beats.txt | promptBuilders.js `buildStoryTextFromBeatsPrompt` (:9704) | Beats-first step 5: page TEXT written from the FINAL ARC + `{ARC_HINTS}` + the locked plan lines + each page's compact picture spec as ILLUSTRATION (`buildTextStagePictureSpecs`, sceneMetadata.js — where, what happens, who wears what in short form from the contract version the page selects, what else is in view; the audit and the refine read the same) + `{PARAGRAPH_SHAPE}` (`paragraphShapeRule()`, the counter's number). Emits `---ANALYSIS---`, then `---STORY TEXT---`, then `---TITLE---` (template :78, :81, :90; corrected 2026-09-23, the order was listed title-first) so `parseRefinedText` reads it |
| text-refine.txt | promptBuilders.js `buildTextRefinePrompt` (:5465) | Post-image text refinement (both pipelines). Carries its OWN review criteria (sections A-E) since 2026-09-21; rewrites are scoped to the pages AUDIT FINDINGS names, and since 2026-09-23 so are the checks (other pages are scanned for the five extra cases only). Gets `{PAGE_MEASURES}` (words / sentences / paragraphs per page, `measurePageText`) and `{PARAGRAPH_SHAPE}` |
| story-trial.txt | storyHelpers.js `buildTrialStoryPrompt` | Trial story call |
| vb-label-repair.txt | (new) Visual Bible label repair | One fed-back round to fix Visual Bible label faults |
| trial-idea.txt | trial.js `POST /generate-ideas-stream` | Trial idea generation |
| — | — | **Bible `text` field, AUTHORING contract (2026-09-21):** story-trial.txt, scene-expansion-all.txt and scene-review.txt all carry `{REQUIRED_TEXT_AUTHORING}` — the ONE `REQUIRED_TEXT_AUTHORING_RULE` from `server/lib/requiredText.js` saying WHEN an element declares `text` (and when it must not). scene-expansion-all's `text` schema note now allows a run of letters or digits, story-trial's artifacts schema gained `text`, and scene-review gained check 9g `[required_text_undeclared]` with a `# VISUAL BIBLE — DECLARED TEXT` block and a `text` lane in `applyReviewBibleCorrections` |
| — | — | **`timeOfDay` / `weather`, ONE module (2026-09-24, `server/lib/sceneLight.js`):** the four brief-authoring templates, story-trial.txt and scene-review.txt check 3a `[light_fields]` carry `{SCENE_LIGHT_FIELD}` (`SCENE_LIGHT_FIELD_RULE`; story-trial also `{TIME_OF_DAY_ENUM}` / `{WEATHER_ENUM}`). image-generation.txt `{LIGHT_NOTE}` and empty-scene.txt `{LIGHT_NOTE}` are the fixed `**LIGHT:**` line built from the page's fields (page: protected tail, `PROMPT_NEVER_CUT`); empty-scene-qc.txt `LIGHT_CHECK`, image-semantic.txt `{DECLARED_LIGHT}` and the visual-flow grid (styleConsistency.js) judge against the same fields. A plate in another light is re-lit from the vantage base (`buildPlateRelightInstruction`). Empty on briefs written before the fields |
| — | — | **`emotion` field, ONE list on both sides (2026-09-23):** the four brief-authoring templates (scene-expansion.txt, scene-expansion-all.txt, scene-iteration.txt, scene-iteration-free.txt) get `characters[].emotion` through `{EXPRESSION_FIELD}` (`EXPRESSION_FIELD_RULE`), and image-inventory-unified.txt gets `figures[].emotion` through `{EMOTION_ENUM}`, filled at template load (prompts.js). Both read `EMOTION_ENUM_PHRASE` from `server/lib/emotionVocabulary.js`; `emotionCheck.js` compares them in code. image-semantic.txt no longer judges emotion |
| — | — | **Over-the-shoulder near figure (2026-09-23):** the four brief-authoring templates carry `{OTS_NEAR_FIGURE}` (`OTS_NEAR_FIGURE_RULE`: the near figure gets `perspective: over-the-shoulder`, is a head-shoulder-upper-arm crop, and its prose names nothing below the shoulders) and story-beats.txt carries `{OTS_NO_CONTACT}` (`OTS_NO_CONTACT_RULE`: no over-the-shoulder on a page whose near figure touches what they face). Both from `server/lib/shotVocabulary.js`, which also feeds the crop into the OTS shot definition and the image builder's perspective directive |
| — | — | **`{SIZE_LOOK_RULE}` (2026-09-23):** `SIZE_LOOK_RULE` in promptBuilders.js, ONE string for every stage that writes the story: inside `{TELLING_RULES}` (arc-create, arc-retell; the arc also keeps a size the commission gives, once), `{SIZE_LOOK_RULE}` in story-beats.txt, and the size line of `STYLE_RULEBOOK`. A size or look only where the plot turns on it, as the plot fact; the Visual Bible author decides every size (`scaleClass`, SCALE_CLASS_SPEC) |
| — | — | **`{STYLE_RULEBOOK}` and `{MOTIVE_AT_THE_ACT}` (2026-09-23):** ONE style rulebook, `STYLE_RULEBOOK` in promptBuilders.js, filled into every template that writes page prose: story-text-from-beats.txt, story-trial.txt, text-refine.txt (also its repetition_fix / length_fix rounds), story-text-diff.txt and story-text-proofread.txt. Complete sentences, no set-pieces or paired negations, no meaning/summary sentence, no narrator justifying an action, sizes and looks only where the story uses them, the last page ends on the concrete act. The writers and the repair write to it; the diff pass and the lector keep it in their corrections and stay out of style otherwise. The hand copies it replaced were deleted. `MOTIVE_AT_THE_ACT_RULE` (a reason revealed later gets a glimpse at or before the act) reaches both writers and the repair. story-text-audit.txt gained question 13 ENDING, a widened INFERRED, an unanswered-demand clause under PAYOFF and a size/look exclusion under LOADBEARING |
| — | — | **Bible `label` field (2026-09-13):** story-trial.txt and scene-expansion-all.txt both author a `label` beside each element's `id` — the one English name every prompt uses for that element |
| age-band-routine.txt | promptBuilders.js `buildAgeModeSection` → `{AGE_MODE}` in arc-create, arc-retell, story-arc-review, story-beats, story-trial, trial-idea, generate-story-idea(s) | Plot shape when the oldest MAIN character is 0–1: a day's rhythm, no plot, naming and repetition |
| age-band-quest.txt | as above (`{AGE_MODE}`) | Oldest MAIN aged 2: one tiny goal, one search place per page, a repeated phrase, cosy close |
| age-band-tries.txt | as above (`{AGE_MODE}`) | Oldest MAIN aged 3: one problem, try-fail / try-fail / try-succeed by the child's own doing |
| age-band-fear-choice.txt | as above (`{AGE_MODE}`) | Oldest MAIN aged 4: something scary faced, resolved by the hero's brave, clever or kind choice |
| age-band-journey.txt | as above (`{AGE_MODE}`) | Oldest MAIN aged 5: mini hero's journey with a required low point the hero's own idea turns |
| generate-story-ideas.txt | storyIdeas.js `buildIdeasPromptContext` | Wizard story ideas (multi) |
| generate-story-idea-single.txt | storyIdeas.js `buildIdeasPromptContext` | Wizard story idea (single) |
| story-idea-requirements-adventure-1/-2.txt | storyIdeas.js `buildIdeasPromptContext` | Idea requirements (adventure) |
| story-idea-requirements-historical-1/-2.txt | storyIdeas.js `buildIdeasPromptContext` | Idea requirements (historical) |
| adventure-guides.txt | storyHelpers.js `parseTeachingGuideFile` | Teaching guide injected into story call |
| educational-guides.txt | storyHelpers.js `parseTeachingGuideFile` | Teaching guide |
| life-challenge-guides.txt | storyHelpers.js `parseTeachingGuideFile` | Teaching guide (SEL/therapeutic) |
| historical-guides.txt | storyHelpers.js; historicalEvents.js; trialCostumes.js | Teaching guide + historical locations |
| swiss-sagen-guides.txt | storyHelpers.js; storyIdeas.js | Teaching guide (Swiss legends) |
| ~~text-consistency-check.txt~~ | — | **DELETED 2026-07-26** — `evaluateTextConsistency` was dead (imported, never called); removed in the Pt 10 cleanup (decisions.md) |

## Scene expansion

| Template | Consumer | Stage |
|---|---|---|
| scene-expansion.txt | storyHelpers.js `buildSceneExpansionPrompt` | Art Director: outline hint → illustration brief. METADATA carries `wornItems[]` (per-page worn/off state for `wornAs` elements). 11c carries `{CLOSEUP_KEPT}` (2026-09-24). The LOC citing rules carry `{PLACE_INSIDE_OUTSIDE}` (2026-09-24) |
| scene-iteration.txt | storyHelpers.js `buildSceneDescriptionPrompt` (via `sceneDescriptions` alias) | Scene re-description on iterate |
| scene-iteration-free.txt | storyHelpers.js `buildSceneDescriptionPrompt` | Free-form scene iteration |
| scene-repair.txt | sceneValidator.js `repairScene` | Scene-description repair |
| rewrite-blocked-scene.txt | images.js `rewriteBlockedScene` | Rewrite scene after provider safety block |

## Empty scenes & page images

| Template | Consumer | Stage |
|---|---|---|
| empty-scene.txt | prompts.js `buildEmptyScenePrompt` (→ images.js, server.js, coverIterate.js) | Background-only scene generation. `{PLATE_EDGE}` = shotVocabulary `PLATE_EDGE_RULE` (edge to edge, no signature/monogram, no margin/mat/deckle or torn-paper edge/vignette/border; 2026-09-25). `{PLATE_NO_PEOPLE}` = shotVocabulary `PLATE_NO_PEOPLE_RULE` (no person on any plate, 2026-09-26). `{CHARACTER_SPACE}` = shotVocabulary `buildPlateSurfaceNote` (band surfaces only, never a character or a count) |
| image-generation.txt | storyHelpers.js `buildImagePrompt`; testlab.js | Page illustration prompt (single unified template). Every character face text in it and in the cover's CHARACTERS IN THIS IMAGE line comes from ONE builder, `buildFaceDescription` (2026-09-24: midpoint descriptors dropped, same on pages, covers, detector and repair). `{REQUIRED_CAST}` = promptBuilders `buildRequiredCastRule(population)` (2026-09-26): the page draws the setting's own passers-by / crowd from the brief's `population`, since plates hold no people |
| image-system-instruction.txt | images.js (~212) | System instruction for image calls |
| art-styles.txt | styledAvatars.js `loadArtStylePrompts`; avatars.js | Art-style descriptor per style. ⚠ duplicated hardcoded copies exist in sceneComposite.js (~893) and character2x4Sheet.js (~395) — keep aligned |
| iterative-placement-pass1.txt | images.js `generateWithIterativePlacement` (LOCAL_PROMPTS) | Iterative placement pass 1 (dev/test-models path) |
| iterative-placement-pass2.txt | images.js `generateWithIterativePlacement` | Iterative placement pass 2 |
| illustration-edit.txt | images.js `editImageWithPrompt` | Targeted illustration edit (never a plate derive) |
| plate-derive.txt | prompts.js `buildPlateDerivePrompt` ← images.js `editImageWithPrompt(..., { plateDerive: true })` (storyJobPipeline.js derive step, testlab.js `edit_image` plate modes) | Camera-move / re-light edit of a vantage's base plate (2026-09-25): `{EDIT_INSTRUCTION}` (shotVocabulary `buildPlateDeriveInstruction` / sceneLight `buildPlateRelightInstruction`), `{ART_STYLE}`, `{PLATE_EDGE}` (`PLATE_EDGE_RULE`, shared with empty-scene.txt), `{PLATE_NO_PEOPLE}` (`PLATE_NO_PEOPLE_RULE`, shared with empty-scene.txt, 2026-09-26). No "keep identical / do not resize" |

### Image prompt length: the Grok cap and the cut order

Rendered from `PROMPT_CUT_ORDER` / `PROMPT_NEVER_CUT` in `server/lib/images.js`; edit the code, then run `node scripts/admin/sync-prompt-cut-order-docs.js`.

<!-- BEGIN prompt-cut-order (generated by scripts/admin/sync-prompt-cut-order-docs.js from images.js PROMPT_CUT_ORDER; do not edit by hand) -->
Grok image cap: **7,900 characters** (`server/config/models.js` `maxPromptLength`). Over the cap, duplicated bullet bodies are merged first; then `shrinkPromptForModel` removes these blocks **in this order**, stopping as soon as the prompt fits. The generation log names each removed block with its step number and size.

1. **COUNTS** (~235 chars) — the "three or fewer" counting rule. *Why here:* binds only a page whose prose states a number of like things; never built on a cover.
2. **Composition: size** (~305 chars) — the vessel / building / vehicle true-size bullet. *Why here:* the REQUIRED OBJECTS scale riders and DEPTH AND SIZE ("a size stated for an element always wins") state it per element.
3. **Composition: facing** (~367 chars) — the "faces the target, not the camera" bullet. *Why here:* yields to every declared facing, and EXACT POSES / EXPRESSIONS AND EYES declare one per figure; never built on a cover.
4. **DEPTH AND SIZE** (~647 chars) — the foreground / midground / background definitions. *Why here:* defines the brief's depth words; the prose still places every figure.
5. **Composition: ground** (~600 chars) — the feet-on-the-ground bullet (the header goes with it). *Why here:* the one feet-on-the-ground rule for pages and covers; kept longer than the generic rules above.
6. **HEIGHT ORDER** (~140 chars) — the shortest-to-tallest line. *Why here:* a page fact: the relative size the height judge compares.
7. **AGE & PROPORTIONS** (~485 chars) — the per-age head-count proportions. *Why here:* a page fact: without it an infant is drawn as a preschooler.
8. **reference-photo rule** (~512 chars) — which attached photo is a place and which is a person. *Why here:* a page fact: the plate-vs-identity binding of the attached images.
9. **single-illustration rule** (~503 chars) — one full-bleed picture, no lettering, ids are not painted. *Why here:* the frame rule; the very last block the cut may spend.

**Never cut. Once every step has run, only the text before the protected tail (from the first of REQUIRED OBJECTS / SEASON / LIGHT / COMPOSITION GUIDELINES / ART STYLE) is trimmed, at a sentence boundary; if the tail alone leaves no room, the render fails loudly.**

- **NO MARKS** — generator half of D-24 (sibling-registry page-image-generator-vs-critics parity anchor)
- **HANDS** — generator half of D-16b (same parity anchor set)
- **REQUIRED TEXT** — the baked cover title and any lettering a Visual Bible element must carry (SETTLED: baked title)
- **REQUIRED CAST** — the generator half of D-03 / D-04b: every named character in frame, exactly one of each, nobody added
- **REQUIRED OBJECTS** — the commissioned elements of the page
- **SEASON** — the book-wide season, even against a reference photo from another season
- **LIGHT** — the page's declared time of day and weather, which wins over the plate's light (sceneLight.js)
- **COMPOSITION GUIDELINES** — a cover's own composition: title-safe top third, group, bottom margin
- **ART STYLE** — the style the book is commissioned in
- **SHOT** — the page's declared framing (a cover carries none)
- **EXACT POSES / EXPRESSIONS AND EYES** — the declared pose and gaze per figure
<!-- END prompt-cut-order -->

### Repetition removed from every image prompt (owner, 2026-09-24)

Measured on staging job_1790100385959_1nitlympp's front cover (four children); chars are what that cover saved. Shared builders, so pages and covers stay identical (decisions.md 2026-08-26); see decisions.md 2026-09-24 "Image prompts say each thing once".

| # | Removed | Chars saved | Where |
|---|---|---|---|
| 1 | The cover's own ground/feet bullet, merged into the one shared Composition ground bullet (which gained "no ground is invented under" a swimming/floating/flying figure) | 243 per cover (the shared bullet grew ~42 on every render) | cover-composition.txt; `COMPOSITION_GROUND_BULLET` |
| 2 | The `**CHARACTER REFERENCE PHOTOS:** [Name], [Name]…` line; REQUIRED CAST now says "Every named character" | 118 | `buildCharacterReferenceList`; image-generation.txt |
| 3 | The age twice in each character line ("(Looks: X)"), "NNN cm tall", and the midpoint "average build" | ~190 | `buildCharacterPhysicalDescription` (also the entity judge, consolidator and detector rich line) |
| 4 | The age a third time in the cover prose, and "eyes on the viewer" once per figure (now one sentence) | ~155 | `buildCoverSceneFromHint` |
| 5 | The cover "Each character has 2 hands / at most 2 items" bullet (the HANDS anchor carries it) | 110 per cover | cover-composition.txt |
| 6 | The front "ALL MAIN CHARACTERS are prominently featured…" bullet, shortened (REQUIRED CAST carries the cast) | ~123 | cover-composition.txt |
| 7 | The Composition facing bullet and COUNTS on covers: not built (gaze is code-owned at the viewer; cover prose states no number) | ~600 built, 0 sent (the cut took them first anyway) | `buildCompositionBlock({cover})`, `COUNTS_RULE` |

Rebuilt through the real builders + shrinker (7,900 cap), before → after: front cover 10,129 → 8,641 built, REQUIRED CAST lost → kept; initial page 9,329 → 7,975 and back cover 9,280 → 7,907, DEPTH AND SIZE lost → kept; page 12 (four children) 10,022 → 10,043, REQUIRED CAST kept both times.

## Evaluation

> **SIZE WARNING (measured 2026-08-08, re-measured 2026-08-09).** These templates grow into
> incident logs — a paragraph per false positive somebody once hit. `image-evaluation.txt` had
> reached **36,321 chars / 406 lines**, with 129 bullet rules and 119 instances of "never / do
> not deduct"; filled for one page that was **46,019 chars ≈ 11,500 tokens**, and a 10-page
> story spent **862,221 input tokens across 109 eval calls — ~86k input tokens PER PAGE**.
> The 2026-08-09 overhaul restructured it into four sections (A never-deduct N-01…N-15,
> B severity, C defects D-01…D-28, D output) and cut it to **18,511 chars / 180 lines** — 49%
> smaller. Rules inside these templates are demonstrably not applied reliably ("missing
> glasses" came back MAJOR five times in one story despite a rule capping it), and two
> identical runs at temperature 0 produced 0 of 6 identical issue sets. Treat length as a
> defect: see `docs/decisions.md` 2026-08-08 "Eval prompts are too long".
>
> **A lean rewrite is not automatically better.** The first lean overhaul measured *worse*
> (21.3 → 7.5/16.5) and was reverted before the owner overrode and shipped the current one.
> Measure over a corpus, never on the page that motivated the change.
>
> **RULE when an eval misfires: do not append another carve-out paragraph.** First check whether
> a rule for it already exists and is being ignored. If it does, the fix is consolidation or
> removal, not addition — adding text to a template this size measurably buys nothing and makes
> the next person's rule less likely to be read. Prefer deleting or merging over appending.

Sizes measured 2026-08-09.

| Template | chars | lines | Consumer | Stage |
|---|---|---|---|---|
| image-evaluation.txt | 18,511 | 180 | images.js `evaluateImageQuality`; regeneration.js evaluate-single | Quality eval (fix_targets need gemini-2.5-flash) |
| image-semantic.txt | 16,061 | 151 | images.js `evaluateThreeStage`; sceneValidator.js `evaluateSemanticFidelity` | Semantic fidelity eval |
| absence-second-look.txt | 786 | 15 | evalPipeline.js `evaluateImageQuality` via absenceCheck.js `secondLookAbsenceClaims` (image + claims only) | Independent second look at every CRITICAL/MAJOR absence claim before it may take a repair slot; unconfirmed → advisory (2026-09-23) |
| image-vision-inventory.txt | 2,039 | 32 | images.js `evaluateThreeStage` | Three-stage eval: vision inventory |
| image-prompt-compliance.txt | 15,268 | 155 | images.js `evaluateThreeStage` | Three-stage eval: prompt compliance (never sees the image) |
| image-inventory-unified.txt | 6,644 | 81 | evalPipeline.js `runVisualInventory` (:98; image only, blind by design) | Per-figure inventory the blind compliance judge consumes; its `emotion` / `gaze` / `lettering` also feed the code checks (emotionCheck.js, gazeCheck.js, letteringCheck.js). `{EMOTION_ENUM}` filled at load |
| cover-evaluation-notes.txt | 1,353 | 17 | evalPipeline.js `evaluateImageQuality` (cover path, :2319-2364) | Cover-only preamble: COVER_NOTE / TEXT_NOTE_APP_OVERLAY / COVER_TEXT. Extracted from JS string literals 2026-09-15. Since the 2026-09-23 fix (decisions.md, "A painted cover title is REQUIRED TEXT on every side"), the title STRING is no longer in this file: it is a required-text item (`requiredText.coverRequiredTexts`) in every judge's structured `{TEXT_RULES}`, the consolidator input (`result.requiredTexts`) and the inpaint clause. COVER_TEXT (what that string is; missing/misspelled = `required_text` CATASTROPHIC) is prepended to the quality USER_PROMPT and appended to the semantic judge's cover reference. |
| empty-scene-qc.txt | 3,473 | 24 | evalPipeline.js `buildEmptySceneQcPrompt` | Plate judge: BODY / STYLE_CHECK / CAMERA_CHECK / LIGHT_CHECK (2026-09-24, the plate's declared time of day and weather) / ERA_CHECK / PLACEMENTS_CHECK / LANDMARK_CHECK. Extracted from JS string literals 2026-09-15; figure rule aligned with the generator and medium + camera checks added 2026-09-23; LANDMARK_CHECK (2026-09-23) judges a real landmark against its attached photo, never the written description, from the same `LANDMARK_PHOTO_AUTHORITY` sentence the author's fidelity block carries. Since 2026-09-25 BODY's Wrong text check fails a `{PLATE_MARKS}` item and a Frame edge check fails a `{PLATE_SURROUNDS}` item (shotVocabulary.js, the lists the generator's `PLATE_EDGE_RULE` names). Since 2026-09-26 the Figures check fails any `{PLATE_PEOPLE}` item, even one the scene names (the list `PLATE_NO_PEOPLE_RULE` forbids) |
| image-visual-inventory.txt | 5,377 | 138 | regeneration.js (admin route: shown as `prompt` only, the call it makes sends image-inventory-unified.txt); testlab.js `split_p1` arm | **Not the production inventory** (corrected 2026-09-23): `runVisualInventory` (evalPipeline.js:98) loads image-inventory-unified.txt |
| image-inspection.txt | 2,457 | 52 | images.js `inspectImageForErrors` | Image error inspection |
| generated-image-analysis.txt | 1,106 | 39 | sceneValidator.js `analyzeGeneratedImage` | Generated-image analysis |
| feedback-consolidator.txt | 26,306 | 194 | feedbackConsolidator.js `consolidateFeedback` | Merges all four evaluators into `deduped_issues[]` — **this list is what scoring charges**; no model-computed score since 2026-09-23 (the audit-only `final_score` section was deleted). Since 2026-09-24 a per-character fix whose types are all crop artefacts (`cutout_artifact`, or a `figure_completeness` only the entity check reported) is moved to `dropped_issues` (`dropCropArtifactFixes`); the finding stays in `deduped_issues` at 0 points. Since 2026-09-24 each transcribed vote in `severities` is clamped to the highest severity that source showed on the page (`sourceSeverityCeilings` / `resolveEntrySeverity`) before the median, and reader lines render their closed-list type `(type)` like every evaluator line; the closed type list is pinned to `evalBuckets.CONSOLIDATED_TYPES` by test |
| repair-verification.txt | 1,799 | 48 | repairVerification.js `verifyRepairWithGemini` | Verifies a repair changed the target region |
| story-text-quality-judge.txt | 2,880 | 48 | textQualityJudge.js `judgeStoryText` | Test Lab text-only harness: scores story TEXT on 5 criteria (cross-model judge) |
| story-scorecard-judge.txt | 2,398 | 50 | testlab.js `runStoryScorecardStage` / storyScorecard.js | Test Lab `story_scorecard` stage: LLM judge rates 4 final artifacts (beats/scene/text/VB) on a 4×5 rubric for model comparison |
| story-retell-judge.txt | 1,263 | 20 | storyScorecard.js evaluator `4.4` (`storyRetellJudge`) / testlab.js `scoreArtifactsWithJudge` | Evaluator 4.4 "child listener": role-plays the target-age child, retells the finished text, then scores ONE artifact (storyText) on comprehension/stake/resolution/engagement. Own rubric (RETELL_RUBRIC) — never comparable with 4.1-4.3 |
| story-scorecard-judge-v1_1.txt | 2,927 | 41 | storyScorecard.js `EVALUATORS['1.1']` (`storyScorecardJudgeV1_1`) | Scorecard evaluator generation 1.1, "legacy harsh" — RUBRIC_V1, judge model chosen per run |
| story-scorecard-judge-v1_2.txt | 3,622 | 46 | storyScorecard.js `EVALUATORS['1.2']` (`storyScorecardJudgeV1_2`) | Scorecard evaluator generation 1.2, "legacy 10-dim beats" — RUBRIC_V3 |
| story-scorecard-judge-v2.txt | 3,622 | 46 | storyScorecard.js `EVALUATORS['2.1'/'2.2'/'2.3']` (`storyScorecardJudgeV2`) | Scorecard generation 2 — one template, three judge models (sonnet / grok / gemini). Brief-BLIND, which is why 3.x exists |
| story-scorecard-judge-v3.txt | 3,932 | 48 | storyScorecard.js `EVALUATORS['3.1'/'3.2'/'3.3']` (`storyScorecardJudgeV3`) | Scorecard generation 3 = premise-aware. A new generation rather than an edit to 2.x: changing what a judge sees changes every score, so 2.x rows stay comparable among themselves |
| story-scorecard-judge-v4.txt | 5,541 | 56 | storyScorecard.js `EVALUATORS['4.x']` (`storyScorecardJudgeV4`) | Scorecard generation 4 = shape-aware. Generation 4 also carries the separate "child listener" evaluator 4.4 (story-retell-judge.txt above) |
| story-arc-judge.txt | 5,155 | 46 | testlab.js `runArcRoundsStage`; rubric `ARC_RUBRIC` in storyScorecard.js | Arc-only judge for the Lab's arc-rounds stage: scores ONE arc across 14 dimensions (shape, attempts, lost, agency, ensemble, change, blockers, grounding, fit, focus, entrances, difficulty, sense, engaging). The rubric lives in storyScorecard.js, not in the template |

## Bounding boxes

| Template | Consumer | Stage |
|---|---|---|
| bounding-box-detection.txt | images.js `detectAllBoundingBoxes` | Pass-1 bbox detection (Gemini path) |
| bbox-refine.txt | regeneration.js refresh-bbox handler | Bbox refinement (2-pass) |
| bbox-refine-overlay.txt | images.js `detectAllBoundingBoxes` (LOCAL_PROMPTS) | Bbox refine with overlay |
| sub-region-detection.txt | images.js `detectSubRegion` | Sub-region detection |

## Entity consistency & visual bible

| Template | Consumer | Stage |
|---|---|---|
| entity-consistency-check.txt | entityConsistency.js `evaluateEntityConsistency` | Cross-page entity consistency eval |
| entity-single-page-repair.txt | entityConsistency.js `repairSinglePage` | Single-page entity repair. Its physical-traits face comes from `buildFaceDescription`, the page/cover face builder (2026-09-24) |
| incremental-consistency-check.txt | images.js `evaluateIncrementalConsistency` | Incremental consistency |
| ~~final-consistency-check.txt~~ | — | **DELETED 2026-07-26** — `runFinalConsistencyChecks`/`evaluateSingleBatch` chain was dead (imported, never called); removed in the Pt 10 cleanup (decisions.md) |
| visual-bible-analysis.txt | visualBible.js `analyzeVisualBibleElements` | VB element analysis |
| reference-sheet.txt | referenceSheets.js `buildReferenceSheetPrompt` | Element reference sheet generation. Cell lines open with a kind sentence (`elementKindSentence`, name + type as prose); `{TEXT_RULE}` is the blanket no-lettering line, swapped for a quoted-words-only line on a solo cell whose entry carries `text` (rendered on `MODEL_DEFAULTS.vbTextCellModel`) |
| sheet-cell-identification.txt | referenceSheets.js `identifySheetCells` | Maps labelled sheet cells to requested elements when the model's drawn grid does not match the requested cell count |

## Repair (image)

| Template | Consumer | Stage |
|---|---|---|
| character-repair-cutout.txt | images.js `repairCharacterMismatchWithGrok` | Grok cutout repair |
| character-repair-blended.txt | images.js `repairCharacterMismatchWithGrok` | Grok blended repair |
| `repair-naturalness.txt` | faceRepair.js checkRepairNaturalness | post-repair figure-integrity check: MATCH/EDGES observations -> enum; clearly off rejects the repaint |
| `repair-face-check.txt` | repairPipeline.js char-fix gate | post-repair comparative face check: original + repaired in, {intact,confidence,reason} out; not intact refuses the repair |
| character-repair-body-blended.txt | images.js `repairCharacterMismatchWithGrok` | Grok body-blended repair |
| character-repair-inpaint.txt | faceRepair.js `buildPrompt` (crosshatch + box) | Full-scene (box-mode) char repair |
| inpainting.txt | images.js `inpaintWithMask` | Mask inpainting (Runware) — **DEAD CHAIN**, no live caller |
| inpaint-grok-regions.txt | images.js `inpaintWithGrokBackend` (LOCAL_PROMPTS) | Grok region inpainting — **DEAD CHAIN**. The LIVE `inpaint` repair method is `inpaintPage` → `editImageWithPrompt` (whole-image edit, no mask) |
| grid-repair.txt | repairGrid.js | Grid-based repair (legacy) |
| text-space-repair.txt | textSpaceRepair.js `ensureCalmZone` | Text calm-zone repair |
| style-transfer.txt | images.js `applyStyleTransfer` (LOCAL_PROMPTS) | Style transfer repair |

## Covers

| Template | Consumer | Stage |
|---|---|---|
| cover-composition.txt | promptBuilders.js `buildCoverPrompt` (`coverComposition`) | TRIAL cover only since 2026-09-24 (a full-story cover is a page; its layout lives in its beat): composition bullets (front / back — the initialPage section was removed, the trial renders no opening page), injected into image-generation.txt. RETIRED 2026-08-26: front-cover.txt, back-cover.txt, initial-page-with-dedication.txt, initial-page-no-dedication.txt and the derived textless variants — a cover is now built from the SAME image-generation template as a page, plus the typography pass. 2026-09-24: the per-child face in CHARACTERS IN THIS IMAGE is the shared shorter `buildFaceDescription` text (−46 chars per child on the stored four-child covers). |

Note: `PROMPT_TEMPLATES.coverImageEvaluation` is referenced in regeneration.js but the file
was deliberately deleted — callers guard and fall back to `imageEvaluation`. By design.

## Avatars

| Template | Consumer | Stage |
|---|---|---|
| avatar-main-prompt.txt | avatars.js | Gemini avatar generation |
| avatar-system-instruction.txt | avatars.js | Avatar system instruction |
| avatar-retry-prompt.txt | avatars.js `POST /generate-clothing-avatars` | Avatar retry |
| avatar-ace-prompt.txt | avatars.js | Runware ACE++ avatars (route exists; ACE++ has no prod pipeline caller) |
| avatar-evaluation.txt | avatars.js `evaluateAvatarFaceMatch` | Avatar face-match eval |
| character-analysis.txt | avatars.js `extractTraitsWithGemini` | Photo trait extraction |
| sheet-2x4-style-eval.txt | character2x4Sheet.js `evaluateStyledSheetWithGemini` | 2×4 styled sheet eval (pass 2). Fills `{REAR_TURN}` and `{SHEET_GROUND}` (the same `SHEET_GROUND_RULE` the restyler gets, TASK 8 backgroundScore). Hair colour and skin are judged against the pass-1 sheet, not the photo (2026-09-23). Final computed in code (`scoreStyleReport`). What the verdict decides (2026-09-24): an attempt with identity or solo < 6 never ships; if both attempts fail on other axes only, the higher-scoring one ships with a warning (`styleJudgeRejected`); if none is shippable the sheet generation throws — the realistic pass-1 sheet is never a substitute |
| sheet-row-heads-eval.txt | character2x4Sheet.js `evaluateSheetRow(row, 'heads')` (`sheetRowHeadsEval`) | SPLIT sheet eval, call 1 of 3 — STRUCTURE of the heads row (angles / clean / coverage+garment / solo / crop), judged on the crop ALONE. No reference images by design: with the photo in context the judge answered "is the head visible" from the reference and scored a headless crop 8. TASK 5 CROP (2026-09-23) scores the head-and-shoulders crop the generator asks for; final computed in code (`scoreHeadsReport`) |
| sheet-row-bodies-eval.txt | character2x4Sheet.js `evaluateSheetRow(row, 'bodies')` (`sheetRowBodiesEval`) | SPLIT sheet eval, call 2 of 3 — STRUCTURE of the bodies row (head-to-toe / angles / outfit / proportions), crop alone. Fills `{REQUESTED_OUTFIT}` + `{REQUESTED_COSTUME}` + `{CHARACTER_AGE}` (the declared age TASK 4 scores proportions against) |
| sheet-row-identity-eval.txt | character2x4Sheet.js `evaluateIdentity` (`sheetRowIdentityEval`) | SPLIT sheet eval, call 3 of 3 — IDENTITY, the only one that sees the reference faces (source photo + avatar faces) and only against the HEADS crop. `CHARACTER_AGE` is substituted in code, not via fillTemplate. Shared by production `generateCharacter2x4Sheet` and the Lab through `evaluateSheetSplit` |
| styled-costumed-avatar.txt | **DEAD in prod** — loaded but only tests/manual + scripts use it; superseded by the JS-built 2×4 sheet prompts. No longer the Lab `avatar_style` prefill (2026-09-23): that prefill is now `buildStyleTransferPrompt` for `params.artStyle` | — |
| styled-costumed-avatar-2x4.txt | **Never loaded** — scripts/test-costumed-2x4.js only | — |

## Hardcoded prompts (NOT in prompts/ — live in JS)

These act like templates but can only be edited in code:

| Location | What it builds |
|---|---|
| `character2x4Sheet.js` `buildBodyRowPrompt()` / `buildHeadRowPrompt()` | **Pass-1 realistic sheet, the live generator**: two Grok edit calls, body row then head row, via `generateComposited2x4`. The single-call `buildPrompt()`, `evaluateSheetWithGemini` and `sheet-2x4-evaluation.txt` were deleted 2026-09-23 |
| `character2x4Sheet.js` `buildStyleTransferPrompt(artStyle)` | **Pass-2 style-transfer prompt**. Carries `SHEET_GROUND_RULE`, keeps hair/skin as Image 1, and (with an anchor) "the style text wins" over the swatch |
| `avatars.js` `getClothingStylePrompt(category, isFemale)` | Clothing-style fragments |
| `grok.js` (~313) | Magenta-padding outpaint instruction prefix |
| `coverIterate.js` (`feedbackParts`, ~331) | Cover feedback block + visual-bible prose assembly |
| `sceneComposite.js` (~893), `character2x4Sheet.js` (~395) | Duplicated art-style descriptor tables — comments require alignment with `prompts/art-styles.txt` |
| `sceneComposite.js` `buildPopulatedPlatePrompt` | Coloured-silhouette plate for the page composite (hardcoded, not a template) |
| `sceneComposite.js` `buildDepopulatePrompt` | Removes the silhouettes to leave a clean background plate (hardcoded) |
| `sceneComposite.js` `buildBlendEditPrompt` | The composite BLEND prompt: goal + scene overview + cast (clothing/action) + interactions + emotions from metadata. **Never the page prompt** — that relocates characters (decisions.md 2026-08-15). Built from `buildBlendMetadata` (hardcoded) |
| `figureDetection.js` `_somIdentifyFigures()` | Set-of-Mark who-is-who: badged page + sanitised identity lines. Asked twice — the primary detection pass and, from a later tier, the second witness (`secondOpinionIdentity`) |
| `figureDetection.js` `arbitrateIdentity()` | **Identity ARBITER** — the only call allowed to overrule the detector's figure names. Page + a tight crop per contested figure + the candidate names' clothing contract and reference faces; deliberately NOT the SoM question the other two voters already answered, and it is never told who claimed what. Model: `MODEL_DEFAULTS.identityArbiter` (decisions.md 2026-09-21, detector-is-master) |
| `referenceSheets.js` `elementCellGatePrompt()` / `stateCellsGatePrompt()` / `cellGatePrompt()` | The VB reference-cell render gates (gemini-2.5-flash-lite, `askCellGate`). Since 2026-09-24 the element gate judges the NAMED object — `elementKindPhrase` ("the brass nutcracker, a hand tool"), the same phrase the cell was rendered from — never the `type` family alone, and asks the judge to name what it sees (`seen`, stored on `cellGates`) before it is told the name |
| `premiseWorld.js` `detectPremiseNamedWorld()` | YES/NO utility-model classification: does the premise name its own world/location (other than the reader's home town)? Fallback rung only — structured wizard signals (`ideaWorld`, `selectedIndex`) are consulted first (decisions.md 2026-08-31, named location binding) |


**Deleted 2026-09-15** (unreachable — beats replaced the unified writer for every full story, and the trial writes its own single call): `story-unified.txt`, `story-unified-imagefirst.txt`, `outline-analysis-textfirst.txt`, and the `buildUnifiedStoryPrompt` builder. See `docs/decisions.md`.


## What each call sees / is blind to (beats pipeline, 2026-09-23)

> Built from the 2026-09-23 prompt audit of staging `job_1790100385959_1nitlympp`
> (findings: `docs/audits/prompt-audit-2026-09-23/`) and checked against the code at `e3923bf03`.
> Rows changed by the same day's fixes (e47a513f0, 86d6ff4a7, ee8890278, 6a2b0e121, c9484709c,
> 2e36a6636) were re-checked against `31fd7db0c`; their new inputs cite the fix commit and the
> `docs/decisions.md` entry by title (dated 2026-09-23).
> One table per stage. **Given** lists only placeholders the template actually declares:
> `fillTemplate` silently drops a key a template does not declare (decisions.md:43393), so a value a
> builder spreads in is NOT an input unless the template names it. **Not given** lists inputs a sibling
> call in the same stage gets and this one does not. **Reason** quotes a recorded reason with its
> `decisions.md` line (line numbers as of this commit) or a code comment. Where no reason is recorded
> anywhere, the cell says **reason not recorded — owner to confirm**. No reason has been inferred.
> Models are the HEAD defaults (`server/config/models.js` unless noted); a run can override some of them.
> Update this section when a call gains or loses an input.

### Arc

| Call | Model | Given | Deliberately NOT given | Reason recorded | Stored? |
|---|---|---|---|---|---|
| `arc_create` (arc-create.txt, `buildArcCreatePrompt`) | `arcCreateModel` claude-opus-5-5, effort xhigh (models.js; re-tell: `arcRetellModel` claude-opus, medium) | page count, language, reading level, names, full commission, `CHARACTER_SOURCE_RULE` (master premise), character details, topic guide, landmarks, story shape, challenge draw, age mode, rules of the logic, the STORY LOGIC spec (invented allowance, chain length from `arcChainRange`; budgets section gone 2026-09-24), age mode in the premise view, arc length, `arcCritiqueSpec()` | a previous book's challenges (excluded from the draw, never written in) | "Nothing about a previous story is ever written into a prompt" (promptBuilders.js ~6697-6704) | prompt `arcReviewReport.createPrompt`, raw reply `.create`, discarded arc `.discarded` (beatsPipeline.js:1181-1185) |
| `arc_panel` ×3 (arc-panel.txt, `buildArcPanelPrompt`) | `arcPanelModels` grok-4.6, deepseek-v4-pro, gpt-5.6-luna-pro (models.js:394), temp 0.8 | commission, character details, landmarks (names + photos, no DESCRIPTION, since 2026-09-23), committed block (story logic + arc + creator's critique since 2026-09-24; the two-arc "Stronger" block before), working-language line, the LOGIC lens (`ARC_LOGIC_CHECK`, the creator critique's own check; CAST and ACTION lenses and the invented allowance removed 2026-09-24), the creator's ENTRANCE / GIVEN / SENSE rule strings, `EVERY_CHILD_ACTS_RULE` (2026-09-23), `ARC_PLACE_RULE` (2026-09-24) | reading level, names, `CHARACTER_SOURCE_RULE`, topic guide, story shape, age mode, budgets, telling rules, challenge draw, critique spec, page count, the discarded arc, the other panelists | Cross-vendor: "so the creator's blind spots are not shared" (decisions.md:29814). Source rule: "make no source claim, so they cannot contradict" (decisions.md:49588). Allowance given: decisions.md:38543. Landmarks given: decisions.md:52854. **Age mode, budgets, telling rules, story shape, topic guide, challenge draw, language line, discarded arc: reason not recorded — owner to confirm** | prompt `rounds[].panelPrompt`, raw replies `rounds[].panel[].text` (beatsPipeline.js:1016, 1105, 1112) |
| `arc_retell` (arc-retell.txt, `buildArcRetellPrompt`) | claude-opus (models.js:393), effort medium (:441) | everything create gets (no names line) + committed arc + all panel solutions as `PANELIST A/B/C` + retell critique spec | the discarded arc, panelist model names | Anonymity: decisions.md:30130 ("Panelists are anonymous in prompts"). Challenge draw kept on purpose: promptBuilders.js:8616-8618. **Discarded arc: reason not recorded — owner to confirm** | prompt `rounds[].retellPrompt`, raw reply `rounds[].retellRaw` (since 2026-09-23) |
| `arc_hints` (arc-hints.txt, `buildArcHintsPrompt`) | `arcHintsModel` grok-4.6 (models.js:418), temp 0 | full commission, final arc and its STORY LOGIC (`{STORY_LOGIC}`, since 2026-09-24), `CHARACTER_SOURCE_RULE` (master arc), character details, working-language line and sentence-anchor output format (2026-09-23) | the critique and Fixing/Keeping lines, panel output, invented allowance, landmarks, age mode, budgets, telling rules, story shape, topic guide, challenge draw, reading level, the planner's picture rules | Allowance not given: "a hint nothing can act on is noise" (decisions.md:38547). Source rule given: decisions.md:51328. Picture rules unseen: decisions.md:49785. **All other omissions: reason not recorded — owner to confirm** | prompt `arcReviewReport.hintsPrompt`, raw reply `.hintsRaw`, model `.hintsModel` (since 2026-09-23, kept even when the pass fails to parse); parsed `arcHints` |

### Page plan

| Call | Model | Given | Deliberately NOT given | Reason recorded | Stored? |
|---|---|---|---|---|---|
| `beats_plan` (story-beats.txt, `buildBeatsPrompt`) | planner = `outline` claude-sonnet (models.js:316) | final arc, `ARC_HINTS` + `HINT_VS_ARC_RULE` + `HINT_ANCHOR_RULE`, world-only premise (`worldOnly: true`), `CHARACTER_SOURCE_RULE` (master arc), character details, the landmarks the arc names with their PHOTOS and the vantage rule (`buildDividerLandmarksSection`, 2026-09-23 — the full author block only when there is no arc), age mode, shot floors, act spans, `{CAST_COVERAGE}` (castCoverage: own-action focal page + appearance floor), `{CAST_TABLE}` / `{CAST_FORMAT}` (the CAST block written before the plan lines, 2026-09-25: deed page + floor-1 more pages per character, the last page's cast; a re-plan is shown it and writes none) and shared definitions | the idea prose, arc critique / weak points, challenge draw, topic guide, budgets, telling rules, reading level, landmark descriptions and landmarks the arc did not choose | World-only: "Re-showing a settled stage the raw idea re-opens those rulings" (decisions.md:50084). Critique and draw: beatsPipeline.js. Landmarks: a divider needs the chosen landmarks' photos, not an author brief (decisions.md 2026-09-23 "Page plan after prompt audit 02"). **Topic guide, budgets, telling rules: reason not recorded — owner to confirm** | prompt `beatsReviewReport.plannerPrompt`, raw reply `plannerReply` (2026-09-23) |
| `plan_check` (plan-check.txt, `buildPlanCheckPrompt`) | `planCheckModel` gpt-5.6-luna-pro (models.js:429) | page count, names (`inputData.characters` only), final arc, arc hints (critic heading), page plan, shared definitions, act spans, the own-action rule; 13 questions + ROSTER / OBSTACLES / PEOPLELESS / WANTED / ACTION | landmarks and photos, character details (ages, pets; spread in but no placeholder), commission, premise figures, age mode, shot floors, appearance counts, counter findings | Counter findings: decisions.md:50805 ("The plan checker is not shown the counter findings"). Appearance counts: a stated range was read as a cap (decisions.md 2026-09-23 castCoverage entry). **Character details, landmarks, commission, premise figures: reason not recorded — owner to confirm** | prompt `beatsReviewReport.prompt`, reply `checkReply`, `wanted`, `actions` |
| counters (`planCounters.js`, code) | none | pages, commissioned names (cast + the arc's (commissioned) figures), place names, cast cap, the arc's invented list (its (new) tags) + allowance, the arc's central figure (`CENTRAL_FIGURE_ABSENT_THIRD`, 2026-09-24), the check's roster / PEOPLELESS | arc text. A figure named only in a character's saved details is commissioned once the arc tags it (commissioned) in its STORY LOGIC (`COMMISSIONED_CAST_DEF`; the "Premise figures:" list until 2026-09-24; audit 02 W7) | Order: beatsPipeline.js:1303-1307. Saved-detail figures: decisions.md "Arc prompts after the prompt audit" (2026-09-23) | `counterFindings`, `counterStats` (beatsPipeline.js:1812-1814) |
| `beats_replan` (same builder + `buildReplanSection`) | claude-sonnet (models.js:316) | everything the planner gets + its own plan + `MUST FIX` / `ALSO NOTED` finding lines + the pages it must keep (`replanKeepPages`: last page, WANTED, ACTION, only focal pages) + `UNDONE LAST ROUND` (the review's refusals of the previous round) | the roster, the checker's prose reasoning | Keep list and refusals: decisions.md 2026-09-23 "Page plan after prompt audit 02". **Roster: reason not recorded — owner to confirm** | prompts `replanPrompts` / `discardedRounds[].replanPrompt`; raw replies `replanReplies` / `discardedRounds[].replanReply` (2026-09-23) |
| `plan_recheck` (same as plan_check) | gpt-5.6-luna-pro | as plan_check, on the merged plan | as plan_check, plus the earlier findings and the replan's declared changes | **reason not recorded — owner to confirm** | prompt `recheck.prompt` (2026-09-23), reply `recheck.reply`, `recheck.wanted` / `recheck.actions` |

### Story bible (wardrobe) and clothing review

| Call | Model | Given | Deliberately NOT given | Reason recorded | Stored? |
|---|---|---|---|---|---|
| `beats_story_bible` (story-bible-from-beats.txt, `buildStoryBibleFromBeatsPrompt`) | planner model claude-sonnet (beatsPipeline.js → models.js:316) | page count, `ART_STYLE`, names, mains, the wardrobe brief (`wardrobeStoryBrief`: world, season, setting, idea — no plot-binding paragraph), wardrobe cast details (age, gender, special details), CHARACTER APPEARANCE (looks, build, hair, eyes, facial hair, glasses, marks), the ARC (`approvedArc`), plan lines, era rule, style wardrobe | the character's saved clothing (owner 2026-09-23: the wardrobe is per story); strengths / flaws / challenges; face geometry and age cues; reading level and story language | decisions.md 2026-09-23 "The wardrobe calls: no saved clothing, the arc for the writer…"; art style: decisions.md:18704 | `storyBibleReport` (prompt, raw reply, model, duration) since 2026-09-23 |
| `beats_clothing_review` (clothing-review.txt, `buildClothingReviewPrompt`) | `clothingReviewModel` deepseek-v4-pro (models.js:452, env `CLOTHING_REVIEW_MODEL`), **reasoning OFF** (`clothingReviewReasoning` `{enabled:false}`, same constant in the Lab replay; owner 2026-09-24, Lab 1425/1427) | names, the wardrobe brief, wardrobe cast details, the bible's current outfits, plan lines, style wardrobe | the arc (slow call in front of the avatars; check 12 reads the plan line's garment); CHARACTER APPEARANCE (hair, glasses, build), `ART_STYLE`, saved clothing; strengths / flaws | Arc: decisions.md 2026-09-23 "The wardrobe calls…" item 2. Style: decisions.md:15734. **Appearance and art style: reason not recorded — owner to confirm** (audit 03 J1) | prompt `clothingReviewReport.prompt`, raw reply `clothingReviewReport.rawResponse` (since 2026-09-23); `outfitsIn` holds the post-review wardrobe |
| wardrobe ↔ Visual Bible check (code, `applyWardrobeBibleCorrections`, clothingCheck.js) | none | contract + the Art Director's VB (headwear, footwear, outer layer). A linked entry naming the SAME garment `adopt`s the contract's clause (the entry is rewritten, the contract never); a DIFFERENT garment in a filled slot is a `conflict` and becomes an OUTFIT VERSION on the entry (`outfitVersion` {character, category, slot, replaces, garment, outfit}; an unlinked entry is linked `wornAs`) — the contract is never rewritten | tops and bottoms; no judge | decisions.md 2026-09-23 "The wardrobe contract owns garment wording"; `conflict` → version: decisions.md 2026-09-24 "A garment the Art Director adds in a filled slot is a new outfit version" (owner) | `wardrobeBibleReport` (kind, character, slot, element, clause, corrected, `versions[]`). No avatar is re-rendered |

### Art Director and scene review

| Call | Model | Given | Deliberately NOT given | Reason recorded | Stored? |
|---|---|---|---|---|---|
| `beats_scene_expansion` (scene-expansion-all.txt, `buildSceneExpansionAllPrompt`) | `sceneDescription` gemini-3.1-pro (models.js:518) | final arc, all plan lines, cast block (physical + contract outfit), height order, available clothing (each used version with its outfit since 2026-09-23; the AD selects, never rewords), landmarks with photos, child age band, creature tone, season, VB budget 4, shared rule constants | art style; page text; avatar images (text-only call) | Model: decisions.md:29006. **Art style (owner, 2026-09-23): blind by design** — the style is added in the image prompt next, and describing for it here buys nothing; the dead "render in the named illustration style" line is gone (4994fe530). **Page text (owner, 2026-09-23): blind by design** — it is written after the briefs; rules 5f / 12j, which referred to it, are removed from this template (4994fe530). decisions.md "The all-pages Art Director and the scene review see no page text and no art style…" | prompt `sceneExpansionReport.prompts[]`; raw reply per attempt `sceneExpansionReport.replies[]` since 4994fe530 (VB and briefs also stored parsed; the cover briefs are pages −1/−2/−3 since 2026-09-24) |
| code checks → review (`clothingCheck`, `sceneBriefCheck`, `vbElementBudget`) | none | the briefs + VB + contract; `wornItems` rows as page presence. The element budget no longer counts a garment the reference picker never packs (`wornItems.carriedByReference`, one predicate for both, ee8890278) | — | "the review is the ONE place they get fixed" (promptBuilders.js ~9584-9593). Budget / worn rows: decisions.md "A worn garment is not a budget element…" (2026-09-23) | `sceneReviewReport.clothingFindings` / `briefFindings` / `…Unfixed` |
| post-review clothing re-check (code, beatsPipeline.js) | none | the reviewed briefs; declared `wornItems` rows carried forward per id at both review adopt points (`carryForwardWornItemsInBrief`) | — | **Runs on every reviewed run** since ee8890278 — it was gated on pre-review findings, so a fault the review INTRODUCED (a deleted `off` row) was never seen | log warning + findings (`clothing check after review`) |
| `beats_scene_review` (scene-review.txt, `buildSceneReviewPrompt`) | `sceneReviewModel` deepseek-v4-pro (models.js:449) | page count, names, cast cap, commission, cast block, plan lines, all briefs, a partial VB (entries with states, declared text — one line when nothing declares any — vantage plates with their shot), `CLOTHING_FINDINGS`, `BRIEF_FINDINGS` (incl. `shot_off_plate` since 4994fe530; `landmark_view_missing` was sent from 4994fe530 and deleted 2026-09-24, Lab 1433); 36 tagged checks, plus check 0 only when a clothing section is sent | the arc (although `[visual_arc]` asks whether framing builds with the plot), page text, art style, full VB entries, cover hints, landmark photos, height order, age band, the Art Director's rulebook (only shared constants — eyes-open and creature-face are two of them since 4994fe530) | Partial VB: "a whole bible would swamp a prompt that already carries every brief" (promptBuilders.js ~9434). Model: decisions.md:39394. **Page text and art style (owner, 2026-09-23): blind by design** — its job is drawability, and the page text is written after it; the reviewer's copy of the text-not-a-checklist rule is removed (4994fe530). **Arc, cover hints, landmarks, height order, age band: reason not recorded — owner to confirm** | prompt `sceneReviewReport.prompt`, `briefsIn`, `analysis`; **raw reply NOT stored** |

### Story text

| Call | Model | Given | Deliberately NOT given | Reason recorded | Stored? |
|---|---|---|---|---|---|
| writer (story-text-from-beats.txt, `buildStoryTextFromBeatsPrompt`) | `storyText` claude-sonnet (models.js:511) | final arc, `ARC_HINTS`, reading level, page count, names, character details, language block, per page plan line + compact picture spec (where, what happens, outfit short form, in view; 21.7k → 18.6k on run 6), title rule, age, DO-NOT-WRITE | the commission | "by this stage the arc IS the story" (promptBuilders.js ~9758) | prompt, raw reply, analysis in `storyTextPrompts[]` |
| arc-informed audit (story-text-audit.txt, `buildTextAuditPrompt`) | `textAuditModel` gemini-3.1-pro (models.js:360) | final arc, plan lines, PULL question by age band, per page text + the writer's compact picture spec (`buildTextStagePictureSpecs`), the HINT question when there are hints, `{ARC_HINTS}` = the arc hints under one heading with `HINT_VS_ARC_RULE` (`buildCriticArcHintsSection`, since e47a513f0) | back cover, commission, cast / character details, reading level, language | Commission: promptBuilders.js ~9231-9236. Hints given: "a critic shown only the unamended arc faults the text for following them" (promptBuilders.js ~7456-7465; decisions.md "Text chain after dragon run 6…", 2026-09-23; audit 05 #2). **Back cover, cast, reading level: reason not recorded — owner to confirm** | prompt `audits[].prompt` (since 2026-09-23), raw reply `audits[].raw` |
| blind audit (story-text-audit-blind.txt) | `textAuditBlindModel` grok-4.6 (models.js:370) | page text only | everything else, including the arc hints | "That is the whole point of having two" (promptBuilders.js ~9182-9187; decisions.md:32447). Hints withheld: "it is denied the arc by design, and a hint is an arc amendment" (promptBuilders.js ~7463-7464). Picture exemption narrowed to the instant's staging and look (owner order 2026-09-23) | prompt and raw reply in `audits[]` |
| word counter (textRefine.js, code) | none | writer text + reading-level budget, tolerance +50% / −20%; since 2026-09-23 also sentences (same +50% over the level's ceiling; 1st-grade band 4-9, so past 13) and paragraphs (more than `PAGE_PARAGRAPHS.maxPerPage`, 5 since 2026-09-24, exact — no tolerance), one LENGTH line per page; since e47a513f0 also the SHIPPED text after the diff and the lector (`wordBudget.shipped`, WARN only) | an under-count on sentences | "Meaning outranks word count" (textRefine.js ~308); sentences/paragraphs: decisions.md 2026-09-23 "Text stage after prompt audit 05" | `textRefineReport.wordBudget` (incl. `.shipped`) |
| `text_refine` (text-refine.txt, `buildTextRefinePrompt`) | `textRefineModel` claude-opus (models.js:473) | language, reading level, page count, names, psychological profiles, final arc, `{ARC_HINTS}` (same critic section as the audit, since e47a513f0), plan lines, the writer's compact picture spec (19.4k → 14.6k SCENE OUTLINES on run 6), current text, `PAGE_MEASURES` (every page's counts), merged audit findings, DO-NOT-WRITE (checker form, `doNotWriteListBody({ forChecker: true })`: without the writer-only "does NOT need to re-check them" note); one scope rule (outline contradiction is the fifth case) | commission, visual profile, back cover | Commission: promptBuilders.js ~5533. Visual profile: "Feeding cheekbones to a prose reviewer invites it to 'fix' appearance" (~5508). Hints and scope rule: decisions.md "Text chain after dragon run 6…" (2026-09-23) | prompt `roundTrace[].prompt`, raw reply `roundTrace[].rawResponse` (since 2026-09-23, owner order), `analysis`, `pages[].after`, `findingLedger` |
| `text_diff` (story-text-diff.txt, `buildTextDiffPrompt`) | `textDiffModel` gpt-5.6-luna-pro (models.js:504) | GRAMMAR CHECK: rewritten pages only, BEFORE / AFTER as numbered sentences, edits FIX/RESTORE only + `FINDINGS THE REWRITE ANSWERED` for the page and its neighbours, language | picture / brief, arc, plan, reading level, cast | Findings given since 5a4672c7a (decisions.md "The diff pass sees the findings the repair answered…", 2026-09-23). **Picture, arc, reading level: reason not recorded — owner to confirm** | prompt and raw reply stored (`roundTrace`, `diffReview`, `diffApplied`). Since e47a513f0 each applied correction records the writer sentences it restored (`restoredSentences`) and `settleLedgerAfterDiff` re-marks those pages' findings `rewrite-restored`: the ledger is settled after the diff, not before |
| lector (story-text-proofread.txt, `buildTextProofreadPrompt`) | `textProofreadModel` gemini-3.1-pro (models.js:494) | language name, reading level (short form, since 2026-09-23), pages | arc, plan, picture, cast, findings | Scope only: "objective language faults" (promptBuilders.js ~9107-9108) | prompt and raw reply stored (`proofread`, `lectorApplied`) |

### Avatars (styled 2×4 sheet)

| Call | Model | Given | Deliberately NOT given | Reason recorded | Stored? |
|---|---|---|---|---|---|
| pass-1 body row (`buildBodyRowPrompt`) | grok-imagine-image (`GROK_MODELS.STANDARD`, grok.js:51) | body phantom row, standard avatar, face photo; costume text + name, season, hair block, declared age, garment rules, no-invented-trim rule (`buildUnnamedTrimRule`, 1927c5660); cell 4 = `REAR_TURN_POSE` (one constant shared with the head row and all four judges, since 86d6ff4a7) | art style (realistic pass), story context; "apparent age in Image 3" when a declared age exists | Age: character2x4Sheet.js ~309-312 | prompt in `styledAvatarGeneration` / `passes.pass1.prompt` |
| pass-1 head row (`buildHeadRowPrompt`) | same | head phantom, face photo, accepted body row; costume text, garment rule, neckline-from-Image-3 + no-invented-trim rule, passport-photo crop ending at the upper chest (1927c5660), hair, declared age; cell 8 = `REAR_TURN_POSE` | standard avatar, costume name, season block | Refs: "Grok edit takes only 3 refs" (character2x4Sheet.js ~212-213). **Costume name, season: reason not recorded — owner to confirm** | as body row |
| heads row eval (sheet-row-heads-eval.txt) | `sheetEvalModel` gemini-2.5-flash (models.js:941) | head-row crop + requested outfit text, `{REAR_TURN}`; `<placeholder>` examples and per-cell reasons; reply through the echo guard (`askSheetJudge` → `isEchoedJudgeVerdict`: re-ask once, then throw — since 86d6ff4a7) | photo, avatar faces, hair text, age | "with them in-context the judge answers 'is the head visible' from the reference" (character2x4Sheet.js ~1211-1217). Echo guard / placeholders / rear turn: decisions.md "Every avatar-sheet judge rejects a copied verdict…" (2026-09-23). **Age: reason not recorded — owner to confirm** | prompt stored as `passes.pass1.judgePrompts.heads` (1927c5660); raw reply NOT stored (parsed verdict only). TASK 5 CROP scores the crop; final computed in code (`scoreHeadsReport`) |
| bodies row eval (sheet-row-bodies-eval.txt) | gemini-2.5-flash (models.js:941); head presence by YOLO pose, not the model | body-row crop + outfit, costume name, age, `{REAR_TURN}`; placeholder examples, per-cell reasons, echo guard (as heads) | photo, avatar, hair text | as heads; head by pose: models.js:942-949 | prompt stored as `passes.pass1.judgePrompts.bodies` (1927c5660); raw reply NOT stored |
| identity eval (sheet-row-identity-eval.txt) | gemini-2.5-flash (models.js:941) | face photo, standard-avatar head row, heads crop, age, `{REAR_TURN}`; placeholder examples, echo guard (as heads) | hair text, body row, costume | Body row: "Identity is a face question … (user direction)" (character2x4Sheet.js ~1249-1251). **Hair text: reason not recorded — owner to confirm** | prompt stored as `passes.pass1.judgePrompts.identity` (1927c5660); raw reply NOT stored |
| pass-2 style transfer (`buildStyleTransferPrompt`) | `avatarStyleTransferBackend` grok (models.js:931) | pass-1 sheet, style anchor (attempt 1 only), style line, `SHEET_GROUND_RULE`, hair/skin-as-Image-1 line, "the style text wins" over the swatch (1927c5660) | age number, costume, face photo, name | Costume: "the restyler is never told which garments to produce" (character2x4Sheet.js ~1066-1067). The anchor keeps its figures (owner, 2026-09-23): a figure-free anchor was tested and does not work — the 2026-08-12 decision stands (decisions.md "The avatar style anchor keeps its figures", 2026-09-23). **Age: reason not recorded — owner to confirm** | winning prompt `passes.pass2.prompt`, `attempts[]` |
| pass-2 style eval (sheet-2x4-style-eval.txt) | gemini-2.5-flash, hardcoded (character2x4Sheet.js ~1069; does not read `sheetEvalModel`) | photo, pass-1 sheet, styled sheet, style, age, `{REAR_TURN}` (was "cell 8 needs no face" until 86d6ff4a7); placeholder examples; the echo guard now also rejects a reason made only of the prompt's own sentences; `{SHEET_GROUND}` = `SHEET_GROUND_RULE` (TASK 8, 1927c5660); hair/skin judged against the pass-1 sheet; identity + solo are the no-ship axes, the rest ship-with-warning on the final strike (2026-09-24) | costume, per-cell head check | Costume: character2x4Sheet.js ~1066-1068. Heads: ~1345-1347. Background / dividers: added 1927c5660 | prompt stored as `passes.pass2.judgePrompt` (1927c5660); parsed verdict only |
| outfit-version sheet (`redressSheetVariant`, via `deriveWardrobeVariantRequirements` → `prepareWardrobeVariantAvatars`) | same chain as the off-state redress | the approved default sheet of the version's plain category + a wardrobe instruction QUOTING the version's two recorded texts (`versionRedressNote`: the contract clause it replaces, the VB garment) + the sheet-mechanics scaffold; one sheet per version, only when a page (or cover) declares or cites the garment | photos (never a fresh build); the default sheet is never re-rendered; a costumed-category version or a version combined with an off-state gets NO sheet (loud refusal) | decisions.md 2026-09-24 "A garment the Art Director adds in a filled slot is a new outfit version". Replaces the 2026-09-15 re-render-from-photos hook (deleted) | stored as `styled-<cat>--off:<ARTid>` in `characterAvatars` like an off-state variant |

### Page images and final checks

| Call | Model | Given | Deliberately NOT given | Reason recorded | Stored? |
|---|---|---|---|---|---|
| page image (image-generation.txt, `buildImagePrompt`) | `pageRenderImage` grok-imagine-2 (models.js:607 → runtime.js:95) | art style, scene prose, reference list, required objects, VB, text area, era guard, scene intent, season, rule constants, required text, the page's shot; avatar / plate / VB-grid / landmark refs | the page's story text; the split-state markings rule unless a listed element has bible states (since 2026-09-23); over the cap, the shrink cuts in the order of "Image prompt length: the Grok cap and the cut order" above (images.js `PROMPT_CUT_ORDER`), never the blocks on its never-cut list | Rank: images.js `PROMPT_CUT_ORDER` and decisions.md 2026-09-23 "The shrink ranks its cuts"; page facts last: decisions.md 2026-09-21 | sent prompt + `compressedScene`; **pre-shrink prompt NOT stored** (only the shrink event in the generation log) |
| vantage plate (empty-scene.txt) | `emptyScenePlateModel` grok-imagine (models.js:616) | full style, SHOT + location + vantage prose + AD framing, era, landmark fidelity, VB, geometry; landmark photo or VB grid | figures; other pages' prose | "HYBRID PLATE (owner-approved 2026-08-29)" (storyJobPipeline.js ~4642) | `sceneBackgrounds[pn].prompt` |
| plate QC (empty-scene-qc.txt, `buildEmptySceneQcPrompt`) | gemini-2.5-flash, hardcoded (evalPipeline.js ~610) | plate image, first 300 chars of the scene (starts with SHOT), era, placements, 800 chars of prose, geometry checks (only dimensions a fact names a direction for), ART STYLE and the plate's camera (since 2026-09-23: a vantage base plate at its class, eye level; a derived plate at its own shot, without the page's geometry or placements); the landmark reference photo the plate was painted from, as the second image (since 2026-09-23) | vision runs only when the pixel checks pass | — | prompt + raw reply in `eval_calls` (kind `plate_qc`, label = vantage / derived / page context) since 2026-09-23 |
| plate QC retry (base, per-page and derived plates) | gemini-2.5-flash, same options as the first judgement (since 2026-09-23; was pixel only) | same as plate QC | same as plate QC | a pixel-only retry "passed" plates that fixed nothing the vision check had failed | retry prompt via `plateQcRecord` / the derived plate's `qcRecord` |
| plate derive (`buildPlateDeriveInstruction`, shotVocabulary.js ~594) | grok-imagine (models.js:616) | base plate + camera-move instruction | ART STYLE, location / landmark / VB grid, "no figures"; **the derived plate is never QC'd** | Derive: decisions.md:482. **No QC, no style: reason not recorded — owner to confirm** | derive prompt, `plateDerivedFor` |
| blind visual inventory (image-inventory-unified.txt, `runVisualInventory`) | `inventoryModel` qwen3-vl (models.js:638 → runtime.js:144) | image only | prompt, refs, names | "P1 is blind and names nobody" (decisions.md:23812) | prompt + raw reply in `eval_calls` (kind `inventory`) since 2026-09-23 |
| quality eval (image-evaluation.txt, `buildEvaluationPrompt`) | `qualityEval` gemini-2.5-flash (models.js:635) | image + refs, original prompt (the sent or compressed scene), interactions, scene intent, ART_STYLE, clothing contract, expected cast, required objects, landmark, TEXT_RULES; every missing claim carries an `absent` field and the region looked at, D-03 cites two non-overlapping matched figures, N-07b "an item missing from your own figures entry is not evidence" (since 2e36a6636) | page text; rule blocks the shrink dropped | Page text: "this judge is handed no page text on any path" (services/prompts.js ~903-905). Post-shrink: sceneMetadata.js ~2287-2288 | prompt + raw reply in `eval_calls` (kind `quality` / `quality_cover`) since 2026-09-23; raw reply also `qualityRawOutput` |
| semantic eval (image-semantic.txt, sceneValidator.js) | gemini-2.5-flash (models.js:635) | image, story text, scene hint, image prompt, interactions, elements, art style, clothing, cast, landmark, TEXT_RULES, text-not-a-checklist; `absent` + region on every missing claim (since 2e36a6636) | reference images | **reason not recorded — owner to confirm** | prompt + raw reply in `eval_calls` (kind `semantic`) since 2026-09-23; parsed `semanticResult` |
| compliance judge (image-prompt-compliance.txt) | qwen3-max (models.js:552) — **OFF** (`promptComplianceJudge`, models.js:587) | (when on) inventory, not the image | the image | docs/SETTLED.md:34 ("THE REASON IS PRECISION, NOT COST") | `notEvaluated` while off |
| duplicate cap (code, absenceCheck.js `capDuplicatesByDetector`) | none | the judges' `duplicate_character` findings + the stored detection's people count + the roster | — | A duplicate needs the detector's room for it: MINOR when detector people ≤ roster people (2e36a6636; decisions.md "Absence findings…", 2026-09-23) | capped severity on the finding (`severityBeforeCap`, `severityCappedBy: detector_count:*`) |
| absence second look (absence-second-look.txt, absenceCheck.js `secondLookAbsenceClaims`) | `absenceSecondLookModel` gemini-2.5-flash (models.js), one call per page, only when a CRITICAL/MAJOR absence claim exists; runs inside `evaluateImageQuality` before scoring and consolidation | the image + the numbered claim lines only | the brief, refs, the judges' reasoning | An independent look, because the sighted judge answered with the brief in view; a claim not confirmed (`yes` / `unclear`) drops to MINOR (decisions.md "Absence findings…", 2026-09-23). A failed call keeps the claims' severity and logs an error | **prompt NOT stored**; the answer is stored per finding (`secondLook: {visible, where}`, `severityCappedBy: second_look:*`) |
| feedback consolidator (feedback-consolidator.txt) | `evalModel` qwen-plus (models.js:543) | text only: scene description, all judges' issues (after the severity guards below), reader findings, figures, clothing, landmark, VB, required lettering (`requiredTexts` from the eval, since c9484709c); rule 2a `finding_contradicts_brief`; a rule-2/2a drop carries type + character and never enters `deduped_issues` (`enforceNotADefectDrops`, since 2e36a6636) | the image | "Text-only — no image passed" (feedbackConsolidator.js ~436-437) | full I/O in `consolidator_calls` |
| inpaint (`inpaintPage`) | grok-imagine-2 (models.js:607) | image + consolidated plan + issues; up to 2 VB / missing-item refs; quiet zone (pages), preserve and required-text clauses | art style (except reframes) | "the source image already carries the style" (images.js ~3493) | version `prompt` = `promptSent` |
| char-fix (faceRepair.js) | `charRepairModel` grok-imagine-image (models.js:1241) | crop + avatar, name, appearance, clothing, action context, defect-type phrase, style | the judge's sentence, the full page, other characters | "NO JUDGE TEXT (owner, 2026-09-23)" (faceRepair.js ~689) | `promptSent` |
| entity consistency (entity-consistency-check.txt) | gemini-2.5-flash, hardcoded (entityConsistency.js ~432) | body and/or head grids with reference cell, entity info, clothing context, garment enum | page text, brief, art style | **reason not recorded — owner to confirm** | **prompt NOT stored**; raw reply returned, persistence not verified |
| iterate re-brief (scene-iteration*.txt) | `sceneIteration` qwen-plus (models.js:525) | text only: page text, locked cast, previous brief, VB, clothing, plan line, image analysis + fix issues, worn state | the image (a text analysis of it instead) | Plan line: iterateBeat.js header | prompt + raw reply in `eval_calls` (kind `iterate_rebrief`) since 2026-09-23 |
| book audit (book-audit.txt) | `utility` gemini-2.5-flash (models.js:671) | per page text + shipped image (covers labelled) + since 2026-09-24 the brief's `sceneIntent` and cast roster | the full brief prose, VB, refs, art style, the cover text contract | Covers labelled: bookAudit.js ~270. **Others: reason not recorded — owner to confirm** | faults; raw reply for the final round only; **prompt NOT stored** |

### Covers

| Call | Model | Given | Deliberately NOT given | Reason recorded | Stored? |
|---|---|---|---|---|---|
| cover image (`buildCoverPrompt` → image-generation.txt + cover-composition.txt) | `coverImage` grok-imagine-2 (models.js:608); baked title on `coverTitleBakedModel` (runtime.js:83) | as a page + COMPOSITION GUIDELINES; front cover only: `**REQUIRED TEXT:**` title block at the protected tail — the one lettering exception the shared preamble names (headed `**TITLE:**` until 2026-09-23) | dedication / branding (composited later on initial page and back cover); **no SHOT block** — a cover declares no shot, and its "portrait" prose was read as close-up (since 2026-09-23) | docs/SETTLED.md:60 (front title baked); no SHOT: decisions.md 2026-09-23 "The shrink ranks its cuts" | sent prompt, `compressedScene` |
| cover quality eval (evalPipeline.js cover branch) | gemini-2.5-flash (models.js:635) | as a page + COVER_NOTE and COVER_TEXT (or the app-overlay note) prepended **into USER_PROMPT**; the painted title in item-11 `{TEXT_RULES}` (since 2026-09-23); ART STYLE block stripped from USER_PROMPT | since 2026-09-23 the app-overlay note excuses only the app's own three strings (title, dedication, brand line); other lettering painted into the textless art is judged by the page text rules | Style strip: "noise for evaluator" (evalPipeline.js ~2338). Title moved into item 11 on 2026-09-23 | as a page |
| cover semantic eval | gemini-2.5-flash (models.js:635) | scrubbed cover brief + an inline `coverEvalNote` + COVER_TEXT, cover cast branch, TEXT_RULES = VB lettering + the painted title (since 2026-09-23) | the COVER_NOTE / app-overlay blocks | decisions.md:9437 (covers get semantic eval). Title withheld until 2026-09-23 (run 6 filed the painted title as CRITICAL); now given, see decisions.md 2026-09-23 | as a page |
| cover consolidator | qwen-plus (models.js:543) | as a page, with the cover description, plus a `Required lettering — PRESENT BY DESIGN (never remove)` block from the eval's `requiredTexts` (the painted title, since c9484709c) | — | Title given: decisions.md "A painted cover title is REQUIRED TEXT on every side…" (2026-09-23; run 6's title-blind consolidator wrote "Remove the text") | `consolidator_calls` |
| cover inpaint (`inpaintPage` via repairPipeline.js ~1228-1320) | grok-imagine-2 (models.js:607) | the textless `${key}Art` layer when one exists (initial page, back cover), then `restampCover`; refs as a page | quiet zone | A baked front cover (no Art layer) now gets the required-text repair clause for its title: repairPipeline passes `expectedText` / `textMode` when it will not restamp, and `inpaintPage` builds the clause with `requiredText.buildRequiredTextRepairClause` (c9484709c; decisions.md "A painted cover title is REQUIRED TEXT on every side…", 2026-09-23). A restamped cover gets none — its lettering is composited after the edit | version `prompt` |

### Prompts and replies that are NOT stored in the DB

Rebuilding any of these after a run means re-running the builder over stored inputs:

- `beats_scene_review`, `beats_scene_expansion` raw replies (prompts are stored)
- all avatar sheet-eval prompts (heads, bodies, identity, style) and their raw replies
- the absence second-look prompt (its answer is stored per finding). Quality, semantic and inventory prompts and replies are in `eval_calls` since 2026-09-23 (pipeline runs only: rows are keyed by the job scope, `trial-<userId>` on trials; repairs started from the regeneration routes are not recorded)
- the pre-shrink image prompt (only the sent one and the shrink event)
- entity-check prompt, book-audit prompt (the plate QC and iterate re-brief prompts are in `eval_calls` since 2026-09-23)
