# Image Generation Routing Hub

**The single decision map: for a given image task, which method/model to use, what the verdict was, and which Test Lab stage re-tests it — so we stop re-deriving and flipping back and forth.**

- **Read this before any image-gen routing decision** (which model, direct vs composite, which pass).
- **Update this after every experiment**: add the verdict + link the Test Lab stage that reproduces it.
- Deep verdict detail lives in memory `project_image_model_tests.md`; entry-function inventory in `docs/image-generation-methods.html`; Grok-vs-Gemini prose in `docs/codebase-guide.md`. This file is the **router** that points at them.

Test Lab: `/admin/test-lab`. Stages run via `server/lib/testlab.js` `STAGE_RUNNERS` / `AVATAR_STAGES` / `STORY_STAGES`. Recipe to add a stage: `reference_testlab_howto` memory.

---

## Decision matrix

| Task | Method / Model | When | Verdict | Test Lab stage | Ref |
|---|---|---|---|---|---|
| **Page (scene) image** | **Grok `grok-imagine-2` ($0.04) in EVERY environment since 2026-09-06** (`runtime.js:95` `pageRenderModel` / `coverRenderModel` — no longer `perEnvironment`; the earlier prod-`grok-imagine`/staging-2.0 split is gone). Declared as `pageRenderModel` in `server/config/runtime.js`; read as `MODEL_DEFAULTS.pageRenderImage` / `simplePageImage` / `complexPageImage`. **Page redos and repair regenerations read the SAME key**, so a page cannot change tier mid-repair. | Always default | ✅ `grok-imagine` (Standard, $0.02) was the production tier until 2026-09-06 — half of Gemini, "good enough" storybook — and remains the tier for **plates and char repair** (see those rows). 🟡 `grok-imagine-2` promoted to production 2026-09-06 on the owner's judgement of rendered pages, NOT a scored A/B (decisions.md 2026-09-06); when it was staging-only (2026-08-30) — **NO scored evidence yet**: Lab experiments 959/963/965 all ran `autoEval:false`, so their score arrays are empty and the basis is an eyeballed sample. It was staging-only for exactly that reason; the promotion happened without one. A scored A/B is still owed. | `image` (model override) | image_model_tests, decisions.md 2026-08-30 |
| Page — strict cross-page style | Gemini `2.5-flash-image` backend override | Story needs uniform style across 10-15 pages | ✅ better consistency, 2× cost | `image` (model override) | image_model_tests |
| **Large-element reference (`double` / `house` / `landmark` scale)** | Attach to the **PLATE** only — `getEmptySceneElementReferences` → `buildEmptySceneVbGrid` → `buildEmptyScenePrompt` `**STRUCTURES:**`. **Never a page reference cell beside the cast** when a plate is sent: both filter sites (`buildPageCompositeRefs`, Phase 5a-pre-grid) drop `scaleClass ∈ {double, house, landmark}` through the shared `isPlateBorneElement` (the retired 2026-09-15 morning bands `vehicle`/`building`/`landscape` resolve onto exactly those three, so stored bibles route unchanged). Selection is by the authored `scaleClass` enum, not by VB collection — a building-scale ARTIFACT routes to the plate too. `scaleClass === null` (every stored bible) falls back to the pre-2026-09-15 type rule (`vehicle`; `location` was dropped from it 2026-09-24 when locations lost their cells). | Every page that is sent a plate | ✅ owner, 2026-09-15. Evidence: `job_1789420511893_zly5rcdej` p12 cited VEH001 as a page cell and a three-master rendered as an open rowboat although the plate prompt named the ship — **the cell won over the plate.** A reference cell has exactly one size, its own, and nothing in the cell says whether it depicts a thumb or a three-master; the plate sizes a structure against bollards, cobbles and quay height. A scale referent INSIDE the cell is REJECTED and is not the alternative (`feedback_no_scale_referent_in_vb_cell`). **The no-plate fallback is mandatory**: with no plate sent the large element KEEPS its cell rather than travelling on nothing (precedent: p1 of `job_1788295892348_l028ggiq7a`, a cast-0 ship exterior that attached zero references while a finished plate existed). | `empty_scene` (the plate now carries the structure reference) + `image` (the page's cell set changed) | plan `tasks/vb-scale-class-plan-2026-09-15.md`, decisions.md 2026-09-15 |
| **Empty-scene plate + scene art-style** | Generate DIRECT in-style from TEXT (`empty-scene.txt` + FULL `resolveArtStyle`, NOT the sheet/empty-scene-stripped variants on the main path), Grok `grok-imagine` edit with a landmark GEOMETRY ref only — NO style reference image. **The plate tier is SPLIT from the page tier (2026-08-30): `MODEL_DEFAULTS.emptyScenePlateModel = 'grok-imagine'` in EVERY environment, staging included.** Plate call sites used to pass the page's already-resolved model verbatim — there was no plate key — so a plate silently inherited any page-tier change. Spread `emptyScenePlateRouting()` at any new plate call site; never pass `pageImageModel`. A plate is a people-free style anchor, so neither reason to pay for the 2.0 tier (typography, figure fidelity) applies to it. | Every page's background plate (Phase 5a-pre). **Note 2026-09-23:** this row predates DERIVED plates (decisions.md 2026-09-21 "An angled page takes a plate DERIVED from the vantage's"): an angled page's plate is a Grok EDIT of the vantage plate (`buildPlateDeriveInstruction`, shotVocabulary.js), i.e. the edit-on-a-base path this row calls realism-prone. On run 6 the p12 aerial derive read as a photograph (docs/audits/prompt-audit-2026-09-23/08-page-images.md S9). **Since 2026-09-23** the derive edit carries the book's art style (`editImageWithPrompt` artStyle, was null) and is judged by the plate QC at its own camera with medium and figure checks, with one re-derive on the feedback (decisions.md 2026-09-23 "The plate is judged on what its author was told"). Replay of the new QC on the stored run-6 derive: FAIL on medium and figures, the base plate PASS. No render verdict on the style-carrying derive yet | ✅ 2026-08-09: this is already the shipped path and it is correct. A/B (3 Swiss landmarks × watercolor·comic·cyber·steampunk·pixar) confirms **prompt-only wins; a style REFERENCE image is REJECTED**: a char-sheet anchor under-stylises, a *scene* anchor LEAKS its own buildings into the target (Kapellbrücke morphed into the anchor's chalet village), a content-free *texture swatch* is a no-op that biases wet media toward photographic. From-scratch text-gen is LOOSER / more painterly than edit-on-a-photoreal-base (which drags toward realism). Plates themselves are excellent — the "some pages look off" impression is the FACES on the composited page, not the background. Nothing to port. | `empty_scene` | this experiment 2026-08-09 |
| **Invented (non-landmark) location on a plate, cover, iterate or page** | **TEXT only.** The location lives in the Visual Bible as prose; the plate is built from that prose (the vantage LOCATION/VANTAGE lines + the Art Director's FRAMING). **No reference cell is ever rendered for a location** (`getElementsNeedingReferenceImages` does not walk `visualBible.locations`), and no stored location cell is ever attached anywhere: the plate grid (`getEmptySceneElementReferences` → `buildEmptySceneVbGrid`), the page and cover grids (`getElementReferenceImagesForPage`, `getElementReferenceImagesByIds`) all skip locations, so an OLD story that carries a location cell re-renders text-only on every plate / page / cover / iterate / repair / Lab call. The plate's REFERENCE line names a landmark photo as "the place" and an element grid as "the vessel or structure" — never a location. Real landmarks keep their photo; vehicles and building-scale artifacts keep their cells. | Always | ✅ owner 2026-09-24. **Correction:** until 2026-09-24 this file implied invented locations were already text-only; the code rendered a cell for every non-landmark location on ≥2 pages with the isolated-object template and attached it as the plate's only image. Survey: 6-11 of 65 recent location cells were dioramas or isolated objects, and the plate copied the cell almost pixel for pixel including camera, plinth and pillar bars (5/6 bad cells gave bad plates); prod `job_1790107559778_fcmlfa8kn` LOC001 kitchen = miniature on a plinth (p1/p3), LOC002 = a wall-less door (p2, p4-6). | `empty_scene` | decisions.md 2026-09-24 (locations are text only) |
| **A page's time of day / weather on a shared vantage plate** | The page declares `timeOfDay` / `weather` (brief metadata, `server/lib/sceneLight.js`). The vantage base plate is painted in the light most of its pages declare; a page in another light gets a plate **RE-LIT from the base** (`editImageWithPrompt` on the plate model with `buildPlateRelightInstruction`, or ONE angle-and-relight edit via `buildPlateDeriveInstruction(…, { relight })`), plate-QC'd with `LIGHT_CHECK` and one fed-back re-derive. The page prompt carries the fixed `**LIGHT:**` line (protected tail) and copies only the plate's setting and geography | Every vantage group whose pages declare more than one light | ✅ owner 2026-09-24. Lab exps 1442-1450: 4 staging pages (3kx p13 dusk, riq p17 night, z2c p10 evening, riq p12 dusk) — each relit plate kept its place, each page rendered its declared light; before, 3kx p13 was full daylight on a daylight plate. ❌ A fresh plate per light: breaks same-place continuity between adjacent pages (2026-09-21 derive rationale). ❌ Splitting the vantage in the Art Director's prose (2026-09-23 rule, removed): prose-only, never reached a plate already shared. Not yet measured: the combined angle + relight edit. **Since 2026-09-25** every derive (angle, relight, both) is sent through its own template, plate-derive.txt (`editImageWithPrompt` `{ plateDerive: true }`), not illustration-edit.txt, and each angle is worded as a camera move with no frame fraction (decisions.md 2026-09-25). Lab 1479 (matted base, ultra-wide): no inner frame, real pull-back; 1480 (clean base): real pull-back with a deckle edge; 1481 (aerial): top-down but radially symmetric, place not kept. Re-test with `edit_image` `params.source: 'derive_base'` (the base a stored derived plate was edited from) | `edit_image` (`params.source: 'empty_scene'`, the relight instruction) then `image` (`backgroundRef` → the relit plate) | decisions.md 2026-09-24 |
| **Any style that renders photographic when it shouldn't** (concept, oil — the painterly styles closest to photography) | Fix the DESCRIPTOR: name the CAMERA's fingerprints, not "photorealistic" | A commissioned painted style ships as photographs | ✅ 2026-08-16/17: shared `NOT_A_PHOTOGRAPH` constant in `ART_STYLES` — "painted by hand, never captured by a camera: no bokeh, no lens flare, no photo grain, no skin pores, no camera-real fabric; depth from atmospheric haze not optical blur; visible brushwork on every surface, faces included". Denying "photorealistic" alone does NOT work, and a negation needs a positive target to paint instead. Lead with the MEDIUM: "realistic proportions" early in a descriptor reads as "make it photographic" (that one phrase shipped a whole concept-art book as photographs). Measured on the worst pages: oil 2/2 painted, concept 3/4 then 2/2. Residual drift is per-roll variance on real-world subject matter, and now shows up as painted ENVIRONMENTS with photographic FACES (open). All 14 descriptors compressed to 386-755 chars, same shape (medium → faces → shared AGE_LINE / NOT_A_PHOTOGRAPH). | `image` | decisions.md 2026-08-16, 2026-08-17 ×2 |
| **Cyber / any "setting"-named art style** | Fix the DESCRIPTOR, not the model | Style renders photoreal despite the style prompt | ✅ 2026-08-06: cyber rewritten to name a figure MEDIUM ("cyberpunk anime, cel-shaded, never photographic") + carry cyber on neon SIGNS mounted on the scene's own fixtures, keep the story's time-of-day (no night/rain). Model swap ❌ (Gemini returned `IMAGE_OTHER`, its safety refusal, on that cover — a content-combination block, not an age rule). Steampunk already complies; audit other setting-named styles the same way. | `image` / `cover` (model override) | decisions.md 2026-08-06 |
| **Avatar Round 1** (realistic anchor) | **Grok** STANDARD | Always | ✅ preserves hair length + outfit; Gemini drifts identity (bob, invented skirt) | `avatar_realistic` | image_model_tests 2026-07-19· ⚠ 2026-07-30 competitive analysis: Gemini 3.x image ("Nano Banana 2") now leads identity-preservation in public head-to-heads — A/B QUEUED (testing-backlog #7.1) before any switch; Grok verdict stands until then |
| **Avatar Round 2** (art/style transfer) | **Grok** (default `avatarStyleTransferBackend='grok'`; Gemini optional alt via `AVATAR_STYLE_BACKEND=gemini`, model `AVATAR_STYLE_MODEL`) | Always | ✅ **Grok is the default (reverted 2026-08-09).** Gemini returns `IMAGE_OTHER` — a SAFETY refusal yielding no image — on a share of realistic-face inputs, confirmed even reframed as a fictional character; the trigger is a content COMBINATION, NOT the subject's age, and it is unpredictable input to input AND the 2026-08-09 A/B shows it is NOT a stronger watercolour styliser. Grok has a fixed moderate stylisation ceiling that prompt wording (preserve-vs-repaint-vs-content-only) and extra passes do NOT move — do not re-try prompt tweaks for "stronger watercolour". **Failure chain (2026-07-31):** per-attempt throws are consumed retries → ONE alternate-backend retry → final `ensureStyledAvatarCoverage` backstop. **Since 2026-09-24 the realistic Round-1 sheet is never shipped in a styled story:** both attempts style-rejected → the higher-scoring styled attempt ships with a warning; no image, or every attempt failed identity/solo → the sheet generation throws (decisions.md "No realistic fallback for a style-rejected avatar sheet"). A character can no longer end a story with zero avatars (decisions.md "Styled-avatar MUST guarantee"). The 2026-07-19 "Gemini stylises better" verdict is REVERSED. | `avatar_style` | image_model_tests 2026-08-09 |
| Avatar sheet eval | Gemini `2.5-flash` (`sheet-2x4-style-eval.txt`) | After each Round-2 attempt | ✅ 6 checks incl. `bodyFaceScore` (body-cell faces) → redo on fail | `avatar_eval` | this repo |
| **Cover — direct** | shared `generateImageOnly` (Grok/Gemini per `coverImage`), `composite:false`; eval/detection in the pipeline (Step 1 / Phase 5b-pre) or explicitly in `iterateCover` for scored callers | **≤5 figures** (see rule below) | ✅ current default (`compositeCovers` gate → `composite` option) | `cover` (⚠️ hardcodes `compositeCovers:false`) | image_routing |
| **Scene anchor: a raw landmark photo never** (what fills slot 0) | One check, `server/lib/landmarkScene.js` `assertLandmarkScene`, in `generateImageOnly` and the shared dispatch: a render with landmark photos carries a people-free plate or declares a role — `'plate'` (the plate render; `emptyScenePlateRouting()` marks it), `'castless'` (THE CAST-0 EXEMPTION, `landmarkPhotoIsPageScene`: a page with no named cast renders on its photo, 2026-09-02 ruling), `'composite'` (iterateCover's composite route, unchanged). Otherwise `PlateRequiredError`. `packReferences` no longer promotes the photo on its own. **Covers:** `buildCoverReferences` `use` — `render` always plated or throws, `edit` no plate and no photo, `composite` unchanged; the trial cover reuses its pages' plate. **Pages:** landmark pages are plated in every mode (`singlePassScene` included); the Phase 5a render throws without a plate and the one retry renders it (`renderPagePlate`) or the page fails; trial pages the same; iterate (`resolveIteratePlate`), page regenerate / test-models / style-lab (`ensureStoryPagePlate`) and the calm-zone / regenerate repairs (`resolveRepairScene`) reuse the stored plate, render one when none is stored, else fail. Gemini attaches the photo only under a role. | Always | ✅ owner 2026-09-24: plate or fail everywhere; cast-0 pages and composite covers are the named exemptions. ❌ Raw photo as the scene: the render becomes an edit of the photograph — strangers from it painted beside the child (prod trial `job_1790169018278_n57xpnufo`). | `image` / `cover` | decisions.md 2026-09-24 (plate or fail) |
| **How the landmark photo reaches a PLATE** (RC5: photo strangers/signs copied into plates) | Production: slot 0 = the photo at native aspect, magenta-padded, prefix "the non-magenta center region must remain pixel-faithful" (`buildMagentaExtensionPrefix` 'default'). Lab arms (staging, `empty_scene` params): **A** `plateExtensionPrefix: 'structure_only'` ("Repaint the whole frame in the ART STYLE; the photo gives structure only."); **B** `plateRefFit: 'crop'` (photo centre-cropped to the page aspect, no pad, no prefix). | Landmark plates | 🟡 2026-09-25, n=4 per arm, staging job_1790277448294_5herh01j7 p1 (Lindenhof, chess-player photo) + p11 (Rathausbrücke, pedestrians + "30" sign), post-9cf38a556 watercolor. Photo strangers/signs copied: baseline 2/4 (Lab 1485), **A 3/4 and worse** (1486: one full photo copy with all 6 chess players and the declared stairs gone; one white padding bar top+bottom), **B 0/4** (1487). Landmark recognisable: p11 base 2/2, A 2/2, B 2/2; p1 the photo is ignored in base 1/2, A 1/2, B 2/2 (its photo does not show the declared stairs). B loses the photo's edges (p11 right-hand building, road signs); no bars in B. ❌ A. B is the candidate; not adopted — too few pages, and it reverses decisions.md "Scene-plate slot 0 is magenta-extended" (2026-06-16) for landmark plates, so it needs a larger sample and the SETTLED protocol. | `empty_scene` (params above) | Lab 1485/1486/1487 |
| **Cover — composite** | `iterateCover` calls the `_maybeGenerateComposite` route helper directly (`composite:true` + `compositeInputs`) → `generateCoverViaComposite` (cutouts + plate + 2-pass edit) | **>5 figures** + landmark present (rule below) | 🟡 PROVISIONAL — recently fixed eval/title drift; not yet re-tested with new Pixar avatars | `cover` with `params.composite=true` | scene_composite_killed, cover_vbid_leak |
| **Composite = a shared-path OPTION** | `_maybeGenerateComposite` is a standalone route helper (default **off**). `iterateCover` computes `compositeOn` (fig-count gate + explicit override) and calls the helper; it routes to `generateCoverViaComposite` when on AND a landmark buf is present, else returns null → direct `generateImageOnly`. Pages never call it. | covers on, pages off | ✅ owner directive: "covers = images, composite is just an option; different defaults, same code" | `cover` (`params.composite`) | decisions.md |
| Cover typography (title/Widmung/brand) | `coverTypography.composeCover` baked into served version | `appSideCoverType=true` (default). **Corrected 2026-09-23:** since `coverTitleMode = 'baked'` everywhere (docs/SETTLED.md, 2026-09-06; `runtime.js:79`) this stamps the initial page and back cover only; the front cover is skipped (`bakeCoverTypographyPostPersist` `skipKeys`) because the model painted the title into it | ✅ baked into every version (2026-07-19) | via `cover` | image_routing |
| **Painted cover title** (lettering in the artwork's medium, not a flat layer) | **PLATE pass** (`server/lib/coverTitlePaint.js`): composeCover renders real-font glyphs → the title alone on a plain WHITE canvas padded to the **widest landscape preset that fits the strip (up to 20:9)** → **Grok** `grok-imagine` via `editWithGrok` DIRECTLY, with the artwork as a 2nd *style-reference* input → key on inkiness → edge-feather → composite onto the untouched textless plate | **Stale — corrected 2026-09-23:** runs automatically only in `coverTitleMode = 'composited'`, which no environment uses since 2026-09-06 (docs/SETTLED.md: model-baked title everywhere). A baked front cover has no textless `frontCoverArt` layer, so neither the automatic pass (`coverTypography.js` `paintServedCoverTitle`, skipped with `frontCover`) nor `POST /stories/:id/repaint-title` ("no textless art layer") can run on it. The "never let a model SPELL the title" verdict below was reversed by that SETTLED line | ✅ 5/5 incl. the twice-failed repro story (exps #447/#448, 2026-08-09). Plate = TITLE STRIP only, preset-padded — a cover-shaped plate page-fills (#439-#443): the model fills empty canvas and no prompt wording stops it. **The model NEVER sees the cover it edits** — the artwork is never re-rendered, so faces/background cannot degrade. **❌ never let a model SPELL the title** (2026 SOTA ~90-95% on short copy, worst on ä/ö/ü/é). **Three traps, all burned already:** (1) do NOT call `editImageWithPrompt` — it wraps the text in the `illustrationEdit` template, which is empty unless `loadPromptTemplates()` ran → Grok 400 "Prompt cannot be empty" → silent **Gemini** fallback; (2) the plate must be LANDSCAPE and TIGHT around the lettering — a wide title in a square canvas gets **re-flowed** (2 lines → 5), and any white slack invites the model to recompose the lockup into it; (3) a 3-channel mask read as 1 channel gives a squeezed, row-wrapped alpha (comb striping). **2026-08-17 (three fixes, all measured on the same cover):** cut the strip from the typography **spec rect** (~20% of page), never the pixel-diff bbox — compression noise inflated it to the 45% cap, the plate went half-empty and the lockup was recentred (coverage 0.00); pad to **20:9 / 2:1** — Grok's documented `aspect_ratio` enum includes them, the "16:9 is the widest" assumption was wrong, and at 16:9 the same tight strip still left enough slack for a 2→3-line reflow (coverage 0.33) while 20:9 passed unchanged (0.52-0.87); and call the plate "a plain flat white background", never "a strip of white paper" — the model painted a physical paper sheet with edges and a drop shadow, which composited as a hairline across the cover (spill 0.29 → 0.15). A 3-row alpha feather at the strip edge removes the residual seam. | `cover_title_paintin` (`mode:'plate'`, calls the production module) | `docs/cover-text-rendering-research.md`, decisions.md 2026-08-07 |
| **Face repair** | **Production: Grok.** `executeCharFixAction` hardcodes `imageBackend: 'grok'` (`repairPipeline.js:1457`), `resolveRepairAxes` defaults `model: 'grok'` (`faceRepair.js:1339`) and `decideRepairMethod` never passes a model (`repairLogic.js:714`, `:781`); `repairCharacterMismatch` has only a grok and a gemini branch (`images.js:4537`). **Qwen `qwen-image-edit@2511` (Runware) + SAM head mask is LAB-ONLY** — `model:'qwen'` is reachable in `repairCharacterFace`/`callModel` (`faceRepair.js:546`) but its only caller is the Test Lab `qwen_insert` stage (`testlab.js:2722`, `backend || 'qwen'`). | Face mismatch | ✅ **as a Lab measurement**: Qwen $0.008, union-hard-pad6 blend (fresh detection → SAM head mask → 3 refs → SAM round 2 → hard union). ⚠️ **NEVER WIRED TO PRODUCTION** — the port is recorded as approved-but-not-done in `image_model_tests` ("REMAINING PORT (user-approved, staging only): qwen face-repair backend + hard-union blend into repairCharacterMismatch"). Production ships the Grok path; this row previously implied Qwen was the production route. Wiring it is a new decision, not a doc fix. | `char_repair` / `qwen_insert` / `repair_verify` | image_model_tests, decisions.md 2026-09-14 |
| **Full-character repair** | **Grok** (crosshatch/blended) | Whole-figure mismatch | ✅ "grok makes better images" for full figures | `char_repair` | image_model_tests SETTLED |
<!-- ASSERT models.figureDetectionBackend === 'grounding-dino' -->
| Figure detection / masks | **GroundingDINO → MobileSAM (local) — DEFAULT EVERYWHERE incl. PRODUCTION since 2026-08-17** (set in `server/config/runtime.js` — behaviour lives in code, no env override; the warmup is told what to preload by the caller) | Detection for eval/repair/composite | ✅ box-prompted MobileSAM winner; full-identity text 5/5. ⚠️ box-prompt mask **blows out** on touching figures (see below) — guarded 2026-07-20 | `bbox` | image_model_tests, local_grounded_sam_detection |
| Quality eval (bbox fix_targets) | Gemini `2.5-flash` | Page quality | ✅ required for spatial fix_targets | `quality_eval` | CLAUDE.md |
| **Blind visual inventory** (the describe-only pass whose fields feed the blind compliance judge) | **Qwen `qwen3-vl` (OpenRouter, ~$0.0008/call) in EVERY environment since 2026-09-19** — declared as `inventoryModel` in `server/config/runtime.js` (a plain value, no longer `perEnvironment`), read via `MODEL_DEFAULTS.inventoryModel` in `runVisualInventory`; a Lab/dev quality-model override still wins. **Gemini `2.5-flash` remains the FALLBACK only** (`evalPipeline.js:112-133`: OpenRouter throw, 120s timeout or non-2xx), measured at 2 of 814 calls = 0.25%. From 2026-09-07 to 2026-09-19 this key was `perEnvironment({ default: 'gemini-2.5-flash', staging: 'qwen3-vl', local: 'qwen3-vl' })` — a staged rollout, now promoted. | Every page, before compliance scoring | ✅ **2026-09-19, promoted to all environments** on the headless-animal sets (exps 1038-1054: 2.5-flash/2.5-pro/3.1-pro/grok-4.6/minimax-m3/kimi-k2.6 called a headless animal whole, qwen3-vl and gemini-3.7-flash saw it), the 20 crowded pages (1053 vs 1054: 2 ghost figures Gemini missed, verified on pixels) and the 34-page head-to-head (1060/1061 vs 1062/1063: *"nothing Gemini caught was missed by Qwen"*). Known costs carried into production: one occlusion false positive, hair-length/age drift, single upstream vendor. 4.3x cheaper per call. — Earlier Gemini-variant grading, still valid as history: 🟡 **2026-09-13, graded against the pixels for the first time (exps #1237 vs #1238, 10 benchmark pages, `unified` arm):** 2.5-flash makes **1.6 consequential errors/page**, 3.7-flash **0.4** — 2 vs 7 clean pages, and the two disagree on a graded field on 8/10 pages. 2.5-flash's errors are hallucinated or inverted detail (a phantom quiver of arrows on a crossbow-wearing figure; a rider facing the camera called "away from viewer"; `clipped_by: none` on four frame-cut figures and `clipped_by: frame` on a fully-visible one; a missed second guard, missed spectacles, missed belts). **But the direction inverts on `items_held`, the field that triggered this:** 3.7-flash is the one that invents held objects (ground-resting sandals, and a grounded plank "held in both hands"), while 2.5-flash's only `items_held` error is an omission — and a false held object is what commissions a repair. Neither model raises false `rendering` alarms (0 in 20 descriptions); both missed the same smeared face and impossible crossbow geometry. Cost: **$0.068 vs $0.184 per 15-call story (+$0.12)**; from 2027 $0.368 (+$0.30). ⚠️ the "reasoning OFF" arm (#1241) is **VOID** — it silently fell back to 2.5-flash (`evalPipeline.js:103-114`) and is byte-identical to #1237 on 7/10 pages. | `inventory_ab` | decisions.md 2026-09-19 (promotion), 2026-09-07 (staged rollout), 2026-09-13 (Gemini-variant grading) |
| **Garment hue normalize** (fix cloth colour drift) | `normalizeGarmentHue` / `normalizeGarmentHueBatch` (garmentHueNormalize.js) — **no model**, masked LAB hue-rotation toward the character's AVATAR garment colour | Runs before EVERY eval in the chain: initial pre-eval (Phase 5b-hue) AND each repair round (before `evaluateImageBatch`), via the shared `normalizeGarmentHueBatch` driver. Corrects only outliers; per-round pass reuses the round's own detection (iterate) and skips redraws without one | ✅ lighting-preserving (rotate a*/b* about origin → L*+chroma exact); illumination-discounted so day/night survives; defers garment-TYPE errors to redraw. Flag `garmentHueNormalize`. **2026-08-06:** the AVATAR side takes NO cast discount (a reference sheet is neutral-lit; discounting it cancelled the signal — hit-rate 3/52 → 10/52), page cast comes from the background, sampling mask is SAM-else-torso-band, and the page hue matches the NEAREST of the avatar's top-K hue clusters (two-garment characters) | `garment_hue` | decisions.md 2026-07-30 + 2026-08-06 |
| Prompt-compliance eval | qwen `qwen3-max` | Stage-2 compliance | ✅ presence-is-input, never-CRITICAL gate | `quality_eval` | project_unified_call |
| **Scene composite** (non-cover, PAGES) | `generateSceneComposite`: colour-silhouette plate (Grok generate) → depopulate (Grok edit) → **GroundingDINO** person+face boxes, palette hue names them → paste avatar cut-outs at measured heights → ONE blend (Grok edit) with a metadata-built prompt, NEVER the page prompt | Pages where `needsScaleRepair()` fires (≥1 declared foreground AND ≥1 background, outdoor, no shared vessel) — it REPLACED scale repair there 2026-08-15 | 🟡 2026-08-25: blend v2 (`8c1baa515`, one positive description) held framing/cast/depth on its first run (exp 851) after exp 848 duplicated a figure and re-framed at 5451 chars. Earlier: correct on a page with real depth (Lab 726 — occluded man behind a parapet, standing figures whole). **Aborts and keeps the original render** when a detected box cannot be a person, or when tallest÷shortest figure < 2.0x (no depth to correct). Needs DINO available or it aborts every time — as of 2026-08-17 DINO is the default in prod too, so this no longer aborts there by construction. Blend prompt = goal + scene overview + cast (wearing/doing) + interactions + emotions from metadata; sending the page's own prompt RELOCATES characters (3/3). **`figureMethod: 'inPlace'`** — PRODUCTION default since 2026-09-11 (Lab exp 1136–1148, 7 runs; clean on 1146 and 1148 after five fixes: sheet-is-reference wording, held-object cut, closing, silhouette-bottom clip, clipped-IoU match + one fed-back re-render; paste+phantom lost the pose 2/2 on the same page): one Grok edit per figure on the ORIGINAL plate ("replace the red silhouette with A"), DINO person box with best IoU to the silhouette → SAM cut-out → pasted on the depopulated plate, no blend; falls back to the dilated silhouette region when no figure is found. **2026-09-11, Lab set 54 (five pages production lost, exps 1150-1156):** `refMode: cell` (ONE sheet cell as Image 2, owner's design) won p10 cleanly (exp 1150) and lost p17/p6 on scale, pose and no-op renders - not promoted; the reference is not what fails. Two gates shipped on the in-place path: an unpainted placeholder (>= 50% of the silhouette unchanged or >= 60% flat saturated) is re-rendered once then unplaced, and leftover placeholder colour is cut away. A secondary with NO bible reference (the dragon) can only enter via the plate creature block (`unreferencedSecondaries: plate`, Lab-only). | `scene_composite` (params `figureDetect`, `figureMethod` = `paste` / `charRepair` / `inPlace`, `refMode` = `sheet` / `cell`, `unreferencedSecondaries` = `omit` / `plate`, `blend`, `replayPasteOf`, `blendMode`) | decisions.md 2026-08-15 ×8, scene_composite_killed (superseded for pages) |
| **Style-repair** (repaint a style-outlier page/cover toward the dominant style) | `repairPageStyle` (styleRepair.js): **prompt-only** (no style-ref image), **Grok** `grok-imagine` since 2026-08-24 (`STYLE_REPAIR_MODEL=gemini` to go back). Raw validated prompt (NOT the illustration-edit template), temp 0.7, retry ≤3 on safety no-image. **Neither model is reliable:** Gemini may return `IMAGE_OTHER` — a SAFETY refusal yielding NO image, so the page keeps what it had — triggered by a content COMBINATION (e.g. a young girl in a bikini as a mermaid), NOT by adult or photoreal faces, and unpredictable page to page; Grok no-ops on other pages. A fallback across both is the real fix and is not built | Step-5 audit outliers; **OFF by default since 2026-09-19** (`styleRepairProduction` = false; `STYLE_REPAIR_PRODUCTION=true` re-arms). DETECTION still runs — the verdict and outliers still reach `finalChecksReport.styleConsistency`; only the paid repaint is gone | 🗄 **TURNED OFF IN PRODUCTION 2026-09-19** — owner call on cost and simplicity (drift not considered a problem on `grok-imagine-image-2.0`), taken against the measurement: run 4 `job_1789759147125_p08djwhbl` repainted 3 covers for $0.06, its gate went 3-for-3, and all three covers were STILL moderate style outliers. Do not re-enable on that evidence — it is a SETTLED.md reversal. The recipe below stays correct if it is ever re-armed, and the Lab stage still runs it. ✅ **RE-ENABLED 2026-08-09** (supersedes same-day disable). The disable rested on two prompt bugs, not a real limit: a 9.5k page-gen prompt + a "match the reference image" instruction with NO ref attached → Gemini text-replied ("no image"). Clean prompt → Gemini restyles fine. Recipe = **character-focused + feature-preserving**: "background already correct; CHARACTERS too realistic → repaint people into {artStyle}; change only style; keep every feature exactly, eyewear if present / none if absent; add nothing, remove nothing." Validated p3/p10/initial page incl. both-ways glasses. **Grok can't restyle** (no-op ~10/255); a content-rich ref leaks the ref's people; "minimal detail/suggest features" DROPS small features (a child's eyes) — all avoided. Refused page keeps its original. | `style_repair` (Lab stage NOT gated by the production flag) | decisions.md 2026-09-19 (OFF, owner cost call); 2026-08-09 (later) for the recipe |
| **Make an object render at its real-world SIZE** (egg, creature, chestnut, structure) | ~~Enlarge the object's drawn cell in the Visual Bible reference grid~~ | — | ❌ **KILLED 2026-09-19. Cell AREA is not the size lever — do not re-propose "just make the reference cell bigger".** Measured on `job_1789759147125_p08djwhbl` p7, 8 renders, $0.36: byte-identical stored production prompt (`diff` empty every run), md5-identical plate + 2-character slots, same character-card size, `grok-imagine-image-2.0`, ONE variable = the egg cell's width. Run 1 at 2.7× area, runs 2-4 at the packer's MAXIMUM spread (133 px → 665 px = **25× area**). Egg width ÷ nearest standing child height — small 0.116 / 0.154 / 0.174 / 0.185, big 0.124 / 0.182 / 0.176 / 0.213: the ranges **overlap**, pair 3 is flat, and 25× area bought **0-18%** when the target (a football vs a ~95 cm 4-year-old ≈ 0.23) needs **+90%**. No headroom is left — 665 px was the maximum. Note `scaleClass` (2026-09-15) is correct at the TEXT layer and also does not move the pixels, so scale is unexplained from both directions. | `vb_reference_sheet` / `image` — cell-area knobs committed `7ec03fb1c` on staging (NOT pushed; the run was local through the production path, no Lab experiment ID) | decisions.md 2026-09-19 |
| **A large CREATURE drawn far below its size** (per page) | **Text layer, image-side only — no new lever.** (1) The Visual Bible entry of a creature at `adult-height` or above leads with its MATURITY, "a fully grown adult <species> with adult body proportions" (`buildAnimalDescription`) — identity the cell can draw, never a size. (2) The page prompt's REQUIRED OBJECTS line measures the creature against the people IN FRAME, by name: "twice the height of a standing adult — about three times the height of Levin and about four times the height of Max" (`creaturePageScaleNote`; only `adult-height` / `twice-adult-height` state an exact multiple, every other band keeps its phrase). (3) The quality judge gets the same sentence as CREATURE SIZES and files D-34 `creature_scale` (MAJOR cap). (4) Repair: `creature_scale` routes to **iterate** (gate 2d in `decideRepairMethod`; a CRITICAL inpaintable finding goes first), never char-fix (no reference) and never inpaint; every repaint instruction (pipeline inpaint + manual `/repair/image`) carries "Sizes that hold after the edit" for each cited creature, unnamed. | Every page citing a VB creature | 🟡 **UNVALIDATED against pixels (2026-09-26).** Built from staging `job_1790373080139_vnx5l8iy7` (grown dragon `twice-adult-height`: large p5, cat-sized p10, child-plus p16/p17, boy-sized p18; its cell looked like the hatchling). Free replay only: rebuilt p10/p16/p18 prompts and the VB entry from stored inputs. Note the 2026-09-15 verdict that the band phrase alone "does not move the pixels" — this adds a yardstick that is IN the frame, which that test never had; it still needs a render to prove it. | `image` (page prompt), `quality_eval` (D-34), `iterate`, `inpaint` | decisions.md 2026-09-26 (creature size) |
| **DETECT an object drawn at the wrong size across pages** (cross-page scale drift) | ONE question per QUALIFYING PROP, each in its OWN call, over only the pages that prop appears on — `askObjectScale` (bookAudit.js) loops the candidates, question built by `buildScaleQuestion` (objectScaleAudit.js), answer read by `collectObjectScale`. It is NOT in the reading call and `prompts/book-audit.txt` carries no scale placeholder (folding it in was measured 2026-09-19 and reverted: 5 and 6 reader's-eye faults with it, against 16 without, same story same day). Outliers only ("significantly LARGER / SMALLER than on the rest"), per-page numeric estimates explicitly banned, "SCALE: none" permitted; a reply with no answer → `notEvaluated` (`object_scale`/`no_scale_answer`) | Every finished book, on VB `artifacts`/`vehicles` entries (**animals excluded by construction**) that carry a DECLARED size (`scaleClass` band or stored pre-enum `size`, read through `elementScaleNote`) and that a brief CITES in `objects[]` on ≥4 audited pages | 🟡 **SHIPS, FLAGS A HUMAN, REPAIRS NOTHING — read before trusting a finding.** Validated on `job_1789759147125_p08djwhbl` in the shipped shape (truth: egg larger p3 1.69 / p5 1.83, smaller p9 0.65, borderline p8 0.88, rest 0.94-1.32). **The two gates reduce 3 candidates to ONE call** (egg; the dog and raven are animals) and it returned, byte-identically over two runs, `SCALE[LARGER]: dragon egg — p4, p5` + `SCALE[SMALLER]: dragon egg — p8, p16`: **p5 caught (the boulder, the book's most visible defect), p8 caught, p3 and p9 missed, p4 (1.22) and p16 (0.94) named in error.** The previous ALL-PROPS-in-one-call shape named the egg SMALLER on p8, p9, p14, p16 — the **wrong direction** — plus a raven, so one-prop-per-call is strictly better. Animals are excluded because a bird's apparent size moves with its wing spread: that finding is meaningless by nature, not merely noisy. **COST, SAID PLAINLY: one vision call PER QUALIFYING PROP** (~2.6k in / 28 out / 2.3k thinking, ≈CHF 0.005 each) — one on this story, three on a story with three declared-size props. Not free, not one-per-story. The free arithmetic route was tested FIRST and has no input: `bboxDetection.objects` is empty on every page because object grounding is gated off (`GDINO_GROUND_OBJECTS`, owner 2026-08-10). Per-page ratios are separately dead (ceiling ~4/6 on direction, order-dependent). Repeats buy nothing at temperature 0 — the earlier {p3,p5} win was three SHUFFLED orders intersected, which is three calls per prop and ruled too expensive. No repair is wired and none should be: shrinking stranded the hands off the object and both levers are dead (cell area; `scaleClass`). | `book_audit` | decisions.md 2026-09-19 (both entries) |
| **Keep an object's IDENTITY (colour, markings) stable across pages** | Give its VB cell enough drawn area to be READ | Whenever an element shares a crowded reference sheet | ✅ **CONFIRMED 2026-09-19, 3/3 vs 3/3** — same experiment, the other axis. Every **small**-cell arm rendered a generic plain grey-white egg; every **big**-cell arm rendered the reference's cream **speckled** egg. Framing/zoom varied WITHIN both arms, so they are not arm-linked and were ruled out as the confound. **A cramped cell costs the object's identity even though it does not change its size** — almost certainly why that story's egg flip-flopped cream (p2/p4) → grey (p3/p5/p8) → white (p9/p16) as sheet crowding varied. Acting on this is open on the backlog. | `vb_reference_sheet` (same knobs, `7ec03fb1c`) | decisions.md 2026-09-19 |
| **Paint a string the story needs READABLE in the picture** (a sign, a plaque, letter tiles) | `server/lib/requiredText.js` — **no model, no extra call**. The VB element's declared `text` is quoted into a `**REQUIRED TEXT:**` block in the page prompt (tail, never cut), into `inpaintPage`'s repair instruction, and into a `{TEXT_RULES}` allow-list built ONCE and handed to all three judges. New scored type `required_text` (D-33) for a required string absent, misspelled or out of order. | Any page whose Visual Bible element declares `text` | 🟡 **SHIPPED 2026-09-21, not yet measured on a render.** Before it, `text` reached the model only as PIXELS in the VB reference cell: the prompt banned all lettering, the repair guard banned it again, and no judge checked a required string — so `job_1789945743706_8ayo2w19e` rendered a scrambled letter run (p14), the wrong pair at 100/100 (p15) and scattered letters on a signpost (p16). Validated by REPLAYING the real builders over that story's stored inputs: p16 now quotes its string in the page prompt and in all three judge prompts; p14/p15 emit nothing because their own elements declare no `text` — **the authoring side (scene-expansion / VB) declaring `text` when a page's plot turns on lettering is the open half.** | `image` (the page prompt changed) + `quality_eval` (the judges gained D-33 / `required_text`) | decisions.md 2026-09-21 |



---

## Routing rules

### Gemini vs Grok — by edit MAGNITUDE (user, 2026-07-19)
- **Big transform** (style transfer, full re-render, "make this Pixar") → **Gemini**. It renders real style; Grok under-stylises.
- **Small/precise tweak** (nudge, minor colour, targeted repair) → **Grok** (or Qwen for masked). Gemini is **lazy** on tiny edits — returns the input essentially unchanged.
- This is what the old "Gemini returns source unchanged" note actually meant: magnitude-dependent, not universal.

### Direct vs Composite cover — by FIGURE COUNT (✅ SETTLED, user decision 2026-07-19)
- **≤ 5 figures → DIRECT.** Direct renders up to 5 people fine (Pixar @5 verified end-to-end). Composite adds no value at these counts — it's slower, needs the analyzer, and looks more "assembled".
- **> 5 figures → composite.** Only crowded covers justify the cutout+plate path.
- **Wired** in `coverIterate.js`: the default `compositeCovers` flag gates `compositeOn` on `coverFigureCount > 5`; an explicit `options.compositeCovers === true` (Test Lab) still forces composite. `iterateCover` no longer forks — it calls the `_maybeGenerateComposite` route helper directly, falling back to the shared `generateImageOnly` direct render when the helper returns null. (`generateImageWithQualityRetry` deleted 2026-08-10 — see decisions.md.)
- (Older style-nuanced draft — comic@4, realistic@3 — is SUPERSEDED by the flat >5 rule per the user.)
  - **Realistic / painterly styles** → even **3** is challenging for direct; composite often needed.
- **⚠️ GATING FACT (2026-07-19): the composite cover path requires a LANDMARK photo.** `generateCoverViaComposite` builds its background plate from a landmark; with no landmark, the shared path (`images.js _maybeGenerateComposite`) logs "composite requested but no landmark buffer — using normal generation" and **silently falls back to DIRECT** (the fallback moved from coverIterate into the shared function when composite became an option). So for landmark-less stories (e.g. first-kindergarten), `compositeCovers:true` never engages — the rule is moot, it's always direct. Composite also needs the Python analyzer (port 5000) up for figure detection/cutouts (cascade-detect); it was down in local runs (`fetch failed`).
- **CORRECTION:** earlier "Pixar direct ≥ composite @5" and this oil run's "composite" were BOTH the landmark-less fallback — i.e. two DIRECT generations, not a real composite comparison. **Composite vs direct is still UNTESTED.** To test it properly: use a story WITH a landmark (e.g. Wilhelm Tell oil, rotation entry 4) AND the Python analyzer running.
- **What IS confirmed (2026-07-19):** with the new Grok-R1→Gemini-R2 avatars, **direct covers with 5 figures are excellent in both Pixar and oil** (all 5 recognizable, style-faithful). So for landmark-less multi-figure covers, direct is the working path today. Harness: `scripts/analysis/cover-composite-vs-direct.js`, `scripts/analysis/oil-experiment.js`.
- **✅ REAL oil composite achieved (2026-07-19)** by calling `generateCoverViaComposite` directly with a BORROWED background plate (an `empty_scene` from any story) as `landmarkBuf` — bypasses the landmark requirement. All 5 oil figures came out identity-LOCKED to their avatars (Emma's butterfly shirt, Noah's green hoodie, Hans present), genuine oil brushwork, on the borrowed background. Harness: `scripts/analysis/oil-composite-real.js`. **Two prerequisites, both mandatory:**
  1. **Python analyzer (port 5000) MUST be up** — `rembg` (`/remove-bg`) cuts each figure out. Down → chroma-key fallback → garbage cutouts on painterly sheets → Grok's repose pass REGENERATES generic figures (loses avatar identity/clothing, dropped a character). This is the #1 composite prerequisite.
  2. **Pass `styleHint` for the target style** — `generateCoverViaComposite`'s `styleHint` DEFAULTS to watercolor (coverComposite.js:513); passing `artStyle:'oil'` alone renders watercolor. Always pass a matching `styleHint`.
- **Composite vs direct (oil, both correct):** composite locks each figure to its exact avatar (tightest identity); direct is a more cohesive single render but identity comes from refs (can drift). ⇒ composite's real edge = strict per-figure identity, at the cost of the analyzer dependency + a more "assembled" look.
- **New avatar creation across styles — ✅ verified:** oil Round-2 (Gemini) scored 5/5 valid at final=9 first attempt (style/identity/bodyFace/clean all 9); painterly brushwork, faces present in body cells. Same pipeline, `artStyle='oil'`.

### Composite cover — figure ORIENTATION (✅ SETTLED, 2026-07-19)
The composite's all-frontal lineup reads flatter than a direct render because the cutout is always the body-front cell (Panel 5) **and** the repose prompt squares the centre to camera + forces every gaze at the viewer. Two levers tested (harness `scripts/analysis/cover-orient-ab.js`, `generateCoverViaComposite({orient})`, 5 oil figures, grid `scratchpad/cover-orient-ab.html`):
- **`turned-prompt` ✅ WINNER (recommended default).** Keeps the faithful front cutout; the repose prompt turns flanking shoulders ~30° toward the group and relaxes the forced eyes-at-viewer into a natural mix (outer figures glance inward). Candid family-photo feel, real depth, **no identity drift**. Grok reposes body rotation reliably when instructed (`feedback_grok_inpaint_capability`).
- **`turned-source` ❌ REJECTED.** Swapping outer cutouts to the 45° Panel-6 cell caused **clothing identity drift** (Noah's plain olive hoodie → camouflage) — the turned cell is a lower-fidelity clothing render and Grok amplifies it. Small orientation gain, not worth the identity risk.
- **`both` ❌** inherits the turned-source drift.
- **`frontal`** = old prod behaviour (flat lineup), kept as the control / fallback.
- Lever lives in `coverComposite.js generateCoverViaComposite({orient})`, threaded through `coverIterate.js` (`options.orient`) and the Test Lab `cover` stage (`params.orient`). Reverses the earlier "every cover figure faces the reader" decision (coverComposite.js ~720) **for outer figures only** — the centre still favours the viewer.
- **Feet-crop regression (fixed 2026-07-19):** first turn-prompt pass cropped the CENTRAL cluster at the shins (turn + "pulling close" made Grok redraw them larger/lower, feet off the bottom edge; once pass-1 crops, the cutout can't recover shoes). Fixed with a per-pose `feetClause` ("keep the whole figure head-to-feet at the same scale, both feet + footwear visible, never crop at ankles/shins/knees, do not enlarge past the bottom edge") appended to every POSE line — universal (all modes). Re-run: all five shod and full-length in both turn variants.

### Figure-detection blown mask — REJECT rule (✅ 2026-07-20, revised)
The point-prompt-from-head approach was replaced (user preference) with a simpler validation at
Stage 2 (`server/lib/images.js` `_cleanMaskAndCheck`): the DINO box is trusted; a box-prompted SAM
mask is ACCEPTED (its bounds become `bodyBox`) ONLY when it is ONE connected figure ≤10% larger than
the DINO box. Specks are always trimmed (largest connected component kept). If the mask has a real
disconnected region (>3% of the main figure) OR exceeds the DINO box by >10%, it is REJECTED and the
tight DINO box is used as `bodyBox`. Output field `maskVerdict` = mask-ok | rejected-disconnected |
rejected-over-10pct | no-mask. grounding-dino path only; Gemini bbox untouched. (Historical detail below.)

### Figure-detection blown mask — box-prompt union (root cause, 2026-07-20)
Local grounding-dino path: MobileSAM is BOX-prompted per DINO person box and `/figure-mask`
UNIONS every mask it returns. On flat painterly art (watercolour/oil) with touching figures the
silhouette grabs neighbours + background, so `bodyBox = mask bounds` EXPLODES — measured 2.1×–4.2×
the DINO box (a whole-frame box for one figure), which breaks text placement AND character repair
(repair targets the figure box → a blown box has no figure to fix). **The DINO box itself is tight
and correct** — figures where SAM failed (`samApplied=false`) fell back to the DINO box and were all
accurate. FaceBox/head point accurate throughout. **Fix** (`server/lib/images.js`, after Stage-3 face
pairing): any `samApplied` det with `bodyBox ≥ 1.6× gdinoBox` → re-derive the silhouette by
POINT-prompting SAM from the head point + a torso point one head-height below (`_mobilesamMaskPoints`);
if that still blows out / fails, drop the mask and keep the tight `gdinoBox`. Verified: −57%…−81%
area, correct single figure. grounding-dino path only; Gemini bbox path never reaches it. Re-test via
the `bbox` Test Lab stage on a multi-figure painterly page.

### Avatar 2×4 LAYOUT validation — split-figure defect (2026-07-19)
Grok Round-1 sometimes renders ONE figure split across the mid-row divider (top = head+torso, bottom = lower body) instead of 4 head-cells + 4 body-cells. If it ships, the composite extracts cell 5 as a **headless lower body** → broken cover.
- **Detection:** `quickLayoutCheck` (deterministic gutter-uniformity, `character2x4Sheet.js`) catches it — Emma's split anchor scored 41.5%/57.8% gutter uniformity (< 60% ⇒ fail). Production's `generateCharacter2x4Sheet` runs it + the Gemini Pass-1 eval + **up to 3 retries**; verified live — Emma retried (attempt 1 split → attempt 3 clean) then oil-stylised valid.
- **⚠️ `quickLayoutCheck` is WHITE-BACKGROUND / REALISTIC-sheet only.** It measures gutter *whiteness*, so painterly/textured styles (oil, watercolor) FALSE-POSITIVE even when the layout is correct (verified: Sarah 25.3% + Noah 57.4% oil sheets were structurally fine but flagged). Documented as "over-eager" in `character2x4Sheet.js` (comment ~672, why the Pass1→Pass2 gate was removed 2026-05-17). **Do NOT wire it as a hard gate on Pass-2 / styled sheets.**
- **Pass-2 has no deterministic layout GATE** — only the Gemini styled-sheet eval, which validates layout *preservation* (styled vs anchor), not *correctness*; a bad anchor passes as "faithfully preserved." So the guarantee lives at Round 1 (validate the anchor), not Round 2. Since 2026-09-07 `quickLayoutCheck` DOES run on every Pass-2 output, but **advisory only**: it `log.error`s and stores `layoutValid` on the attempt record, and decides nothing — the bullet above still holds. It exists so a merged sheet is never silent again (job_1788763045123_z8so79ngb shipped one in total silence).
- **The Pass-2 STYLE ANCHOR is not attached when nothing can judge the sheet (2026-09-07).** Every `style-anchor-*.jpg` is a finished illustration of three people, and Grok sometimes paints ITS cast over the character instead of taking only its brushwork (staging job_1787252581387_6sn8z0nh2, 2026-08-20; job_1788763045123_z8so79ngb, 2026-09-07). The two defences built for it — reject-and-fall-back-to-Pass-1, and an anchor-dropping retry — both key off the Gemini verdict, so a run with `skipQualityEval` (every trial) rolled the dice once with the contaminant attached and shipped whatever came back. `character2x4Sheet.js` now attaches the anchor only when `GEMINI_API_KEY` is set AND the eval is on (Test Lab `promptOverride` runs keep it, so an A/B still measures its exact reference set).
- **RULE for experiment harnesses:** never reuse a raw one-shot `editWithGrok` anchor — run Round 1 through `generateCharacter2x4Sheet` (validated + retry) or gate the anchor on `quickLayoutCheck`, or you test on broken sheets (this is why the Pixar/oil A/B harness produced a split Emma).

---

### Page composite — what the blend may and may not be told (✅ measured 2026-08-15)

Nine blend variants on ONE fixed pasted canvas (Lab 695–705, replayed with
`params.replayPasteOf` so only the words changed — a fresh composite re-rolls
the plate and makes two prompts incomparable):

| variant | result |
|---|---|
| the page's own generation prompt | character relocated off the bridge, 3/3 |
| poses reworded as descriptions of Image 1 | relocated again |
| page prompt minus Composition / EXACT POSES / EXPRESSIONS | stayed, but sat ON the parapet |
| a generic line explaining what a clipped figure means | relocated |
| naming the occluder explicitly | worst — giant figure on a plank that does not exist |
| retouch-only, no page prompt | DELETED the half-hidden man as an artefact |
| retouch + a cast census | correct, but blank faces |
| census + expressions + attention | expressions landed; frame crept in, feet cut |
| the above at 5451 chars, full DO-NOT block (exp 848) | DUPLICATED the occluded man at foreground size AND re-framed — both explicitly forbidden in that prompt |
| one positive description, no DO-NOTs, 1635 chars (exp 851, `8c1baa515`) | held framing, cast, and depth; occluded man engaged with the railing at background size; gaze/expression only partially landed |

Rules that follow, and the reason each exists:
- **Never send the page prompt.** Its `Composition`, `EXACT POSES` and
  `EXPRESSIONS` sections are orders to arrange a scene; the scene is already
  arranged, so the model carries them out.
- **Always send a cast census.** Without it a partly-hidden figure is painted out.
- **Rewording does not defuse a staging instruction** — deletion does.
- **Do not add another prohibition.** Four have been overridden; prompt length
  itself pulls the camera in (1810 chars held the framing, 3063 cut feet;
  5451 duplicated a figure and re-framed THROUGH the prohibitions, exp 848).
- **State the picture, not the repairs.** One positive description at 1635
  chars (each fact once, no scene overview, no DO-NOT list, occluder unnamed)
  held framing/cast/depth on the page that failed at 5451 — exp 851,
  `8c1baa515`. One clean run, not yet a corpus.
- **The gate, not the blend, is now what stops pages.** Proof suite 853/854/855
  (2026-08-25): flat-ground plates paint real but moderate depth — refused at
  1.48×/1.68×/1.96× against `MIN_DEPTH_SPREAD = 2.0`; architectural-depth pages
  roll 3.8–4×. Cast fixes (back views, compound actions, creatures, no
  fabricated facing) all verified in those plates. Threshold SETTLED by owner
  2026-08-25: keep 2.0× — near-misses are noise around the line, refusing is
  safe, the composite corrects dramatic depth only. decisions.md same date.
- **Eval-then-repair is not an escape hatch.** Feeding the top-3 evaluator
  findings back as the repair instruction reproduced the same relocation, because
  the evaluator scores the paste against the same brief and its prescribed fix
  literally says "Regenerate".

### Page composite — detection (✅ measured 2026-08-15)

DINO for boxes, palette for identity, DINO face box for the head. Colour-only
detection on the plate was measured and INVERTS the depth verdict on all three
test pages (bridge timbers read as the red character; lawn as the yellow one),
so the depopulated background cannot be skipped for the diff detector — and the
DINO detector does not need it at all. SOM naming is useless on ghosts (UNKNOWN
for 4 of 5, Lab 723): a flat cartoon figure gives a VLM nothing to recognise.

---

## Test Lab stage map (route BACK here to re-test, don't re-script)

`image`, `empty_scene`, `quality_eval`, `semantic_eval`, `bbox`, `char_repair`, `entity`, `text_zone`, `consolidate`, `inpaint`, `iterate`, `repair_round`, `edit_image`, `artifact_repair`, `scale_repair`, `style_transfer`, `pick_best`, `scene_expansion`, `scene_expansion_ab`, `scene_variant`, `scene_description`, `rewrite_blocked`, `repair_verify`, `qwen_insert`, `avatar_realistic`, `avatar_style`, `avatar_eval`, `cover`, `style_check`, `style_repair`.

`edit_image` with `params.source: 'empty_scene'` edits the page's stored background plate the way the
production plate-derive step does (plate model and the book's art style, since 2026-09-23) — the stage for testing a derive
instruction (ultra-wide / high-angle / low-angle plates). Target the page that holds the vantage's BASE plate.

`style_repair` (story-level, target `{storyId}`): the missing repair half of the detection-only
`style_check` — finds style-outlier pages via `checkStoryStyleConsistency`, repaints each toward the
dominant cluster with BOTH Gemini and Grok (`repairPageStyle`, model-parameterized), and surfaces the
two candidates side-by-side with before/after `checkStyleMatch` scores so a human picks the winning model.
`params.characterRefs: true` adds a second arm per model — the same repaint with the page cast's styled
avatars attached as style reference sheets (`gemini` vs `gemini+refs`, `grok` vs `grok+refs`) — which is
the A/B holding `styleRepairCharacterRefs` flag-off. Doubles the images per page; pair it with
`params.maxTargets: 1` and pin the page via `params.pages` so both runs hit the same target.
Test-Lab-first; production wiring deferred (decisions.md Pt 10).

Targets: page stages `{storyId, pageNumber}`; avatar stages `{storyId, character}`; story-level `{storyId}` (+ `coverType` for `cover`).

---

## Open experiments / gaps (to close, not re-derive)

- **Scale drift is now DETECTED even though it cannot be fixed** (2026-09-19): the book audit asks one extra question PER QUALIFYING PROP, each in its own call, and names the outlier pages (row above) — one read per prop, so one or two innocent pages a run, accepted because it flags for a human and repairs nothing. Animals are never asked about (a bird's size moves with its wing spread) and a prop with no declared size is never asked about either, which is what keeps the call count down: exactly one call on the validation story. Making it free by folding it into the whole-book reading call was measured and reverted: it cost the reading most of its findings. The repair side of this gap is still open, and the PLATE remains the only untested lever.
- **Scale: the PLATE is the only untested layer — do NOT re-test words or cell geometry.** Both of the layers we control are now measured inert: the `scaleClass` canonical phrase reaches the prompt correctly and does not move the pixels (2026-09-15), and the reference cell's drawn AREA does not either (25× bought 0-18%, decisions.md 2026-09-19). The remaining candidate is the **plate** — the only reference that carries real-world scale cues, which is why this file already routes structures there: paint the object's FOOTPRINT into the empty scene rather than describing its size or sizing its cell. Untested; it is NOT implied by the 2026-09-19 result. Note the owner separately rejected putting a scale referent INSIDE a cell (silhouette/coin/hand) because cell contents leak onto the page — memory `feedback_no_scale_referent_in_vb_cell`, 2026-09-14.
- **Composite cover with new Pixar avatars — UNTESTED.** The `cover` stage now accepts `params.composite=true` (added 2026-07-19). Remaining enabler: the story's characters must be re-avatared through the new Grok-R1→Gemini-R2 pipeline (showcase avatars are old all-Grok) so the composite pulls the good Pixar cutouts. Then run 5-figure composite vs direct to validate the figure×style rule. UI toggle in the Test Lab client (`params.composite`) still to be exposed.
- **Direct-vs-composite thresholds are intuition only** — pin per style with lab runs (comic@4, realistic@3).
- **Faces-in-style ceiling (the real scene weak point, 2026-08-09).** Backgrounds stylise fine; on final composited pages the CHARACTERS' faces render semi-photographic under painterly styles (watercolor/oil), which reads as "wrong style" even though the plate is correct. This is a character-rendering problem (avatar Round-2 style ceiling + composite blend), NOT a scene-style problem — scene style is settled (row above). Experiment TBD: pull staging pages where faces went photographic, test a stronger face-in-style clause / per-character style pass.
- **✅ ANSWERED 2026-08-24 — per-page art-style drift on adult-cast books was cause (d), not (a)/(b)/(c):** the repaint produced **no image at all**. Gemini returned `IMAGE_OTHER` — its safety refusal, no image at all — on both photographic pages of staging `job_1787638394061_hs70901tfsn` and in Lab #837, and Gemini was the default. The refusal is triggered by content combinations, not by adult or photoreal faces, and Gemini restyles comparable pages fine on other runs — so that 16-page book shipped 4 pages the audit itself flagged photographic, after 18 attempts kept 3. Grok repaints the same page correctly in ~18s/$0.02. Default flipped to Grok; gate rejections and errors are now recorded per attempt in `finalChecksReport.styleConsistency.repairs`, so a future run is diagnosable without re-deriving. decisions.md 2026-08-24.
- **Per-page art-style drift (original 2026-08-09 note, kept for the other candidate causes)** despite `style_repair` wired to prod. Some pages in a finished story read as a visibly different art style. Root cause unconfirmed — three candidates to check on a real multi-page story before any fix: (a) `checkStoryStyleConsistency` under-detects (threshold too loose), (b) the repaint fires but fails the `checkStyleMatch` gate and is discarded, or (c) the "drift" is actually the faces-in-style ceiling above, not a whole-page flip. Do NOT re-derive — pull a recent story, score every page's style, and see which pages drift + whether `style_repair` fired.

## Framing a gap action (one figure sending something toward another)

**Task:** page where A throws / aims / kicks / calls something toward B across a distance.

**Verdict (owner, 2026-09-19):** two framings, and the page takes one.
- `over-the-shoulder` — **default**. Camera behind the actor, target small and deep in the opposite corner. The camera axis IS the line of the action, so the DIRECTION holds whether or not the model understood the verb. Declared as `framingPattern: "over-the-shoulder"`, which also switches on the background-reference drop in `storyJobPipeline.js` (attaching a background character's portrait forces the renderer to upsize them past "tiny in the distance").
- `ultra-wide` — the alternative, when the page needs both faces readable. Makes the DISTANCE readable; the gap must be written and measured against something in the frame, because a flat framing draws two figures almost touching however many paces the sentence claims (owner, observed repeatedly; ultra-wide "worked sometimes" on its own).
- Never the flat middle: both figures the same size at opposite edges of an ordinary shot states neither.

**Untested here:** no Lab stage reproduces this yet. The previous mechanism (`framingPattern` set by a scene hint) was never wired — 0 non-null across 139 stored staging stories — so the ref-drop it gates has never executed. First real exercise of it should be watched. Rule lives in `GAP_ACTION_FRAMING_RULE` (`server/lib/promptBuilders.js`), filled into all four brief-authoring templates.
