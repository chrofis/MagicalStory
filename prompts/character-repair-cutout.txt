TWO IMAGES ATTACHED.
- IMAGE 1 = REFERENCE PORTRAIT of {charName}. Use it only to copy face, hair, skin tone, build, and clothing. Don't return this image. Don't reproduce its background or composition.
- IMAGE 2 = THE CUTOUT TO EDIT. A scene region with the figure marked by a magenta crosshatch. Your output must be an edited copy of IMAGE 2 — same dimensions, same edges, same surrounding pixels. Only the crosshatched area is repainted.

If you are tempted to return anything that looks like IMAGE 1, stop — return IMAGE 2 with the crosshatch repainted instead.

Inpaint task: repaint the area covered by the magenta crosshatch in IMAGE 2 as {charName} from IMAGE 1. The crosshatch marks the figure that needs to be replaced — remove the crosshatch entirely and paint one {charName} underneath.

Match {charName}'s face, hair, skin tone, build, and clothing from the reference.

Keep the original pose, body language, hand positions, held items, facial expression, and gaze direction — infer these from the surrounding scene and from the figure still faintly visible through the crosshatch. Do not default to a smile or make the character face the camera.

Keep the original head and body orientation exactly. A figure seen from behind stays seen from behind; a profile stays a profile. Never rotate the head or body to make a listed attribute (glasses, garment front, face) visible — an attribute the pose hides stays hidden.

Only one figure should appear in the result. If a partial figure is visible just outside the crosshatch, it is a leftover fragment of the same figure — absorb it into {charName}, do not paint a second character.

The repainted figure must match the art style of the surrounding illustration exactly — same rendering technique, same brush texture, same level of detail, same color palette. Do not render the character more realistically or with a different style than the background.{artStyleContext}

The background outside the crosshatch area should stay the same.{clothingContext}{actionContext}{issueContext}{textPositionContext}
