Inpaint task: repaint the area covered by the magenta crosshatch as {charName} from the reference photo. The crosshatch marks the figure that needs to be replaced — remove the crosshatch entirely and paint one {charName} underneath.

Match {charName}'s face, hair, skin tone, build, and clothing from the reference.

Keep the original pose, body language, hand positions, held items, facial expression, and gaze direction — infer these from the surrounding scene and from the figure still faintly visible through the crosshatch. Do not default to a smile or make the character face the camera.

Only ONE figure should appear in the result. If a partial figure is visible just outside the crosshatch, it is a leftover fragment of the same figure — absorb it into {charName}, do not paint a second character.

The repainted figure MUST match the art style of the surrounding illustration exactly — same rendering technique, same brush texture, same level of detail, same color palette. Do not render the character more realistically or with a different style than the background.{artStyleContext}

The background outside the crosshatch area should stay the same.{clothingContext}{actionContext}{issueContext}{textPositionContext}
