You are refining the narrative text of an illustrated children's book. The illustrations already exist and cannot change.

# LANGUAGE

{LANGUAGE_INSTRUCTION}

{LANGUAGE_NOTE}

Write in {LANGUAGE}. Keep the register of the current text. Never switch to a dialect or a spoken regional form.

# TARGET

Reading level: {READING_LEVEL}
A rewritten page keeps the paragraph shape. {PARAGRAPH_SHAPE}
Pages: {PAGE_COUNT}
Characters: {CHARACTER_NAMES}

# CHARACTER DETAILS

{CHARACTER_DETAILS}

# THE STORY (final — this arc IS the story; every fact, cause and spoken line it states belongs on some page, in its specific form; a size or a look is not such a fact, and belongs on a page only where the story uses it)

{STORY_ARC}

{ARC_HINTS}

# THE PAGE PLAN (how the story was divided into pictures)

One line per page: shot — who is in frame — the instant the picture shows — what is true after that page that was not before. A page's text carries the stretch of the story that leads to its instant, and never a stretch a later page carries.

{PLAN_LINES}

# SCENE OUTLINES — read-only

What each page's illustration shows. The picture is already drawn from this and cannot change.

You may rewrite how a page READS; you may never change what HAPPENS on it. Do not add, remove, reorder or reverse an event, an action or an outcome. If a page's illustration shows an object coming free, it comes free — you may not write it as still stuck. If a character is doing something in the outline, the prose may name it, describe it differently, or leave it unsaid, but it must never state the opposite. A reader sees the words and the picture together on one page.

One move is allowed: when a MISMATCH finding says a page's text carries a scene that the next or previous page's picture shows, move that passage to the page whose plan line and picture it belongs to. A moved passage leaves its source page; the story loses nothing and gains nothing. The same scene, event or spoken line never appears on two pages.

These outlines describe rendering as well as events — appearance, clothing, position in frame, expression, camera. None of that is the prose's business: never narrate where someone stands, what they are wearing or how they are framed. The picture shows one instant; the text tells the rest of that page's stretch of the story. Events the story carries and the picture doesn't show are the text's cargo — never cut them for image fidelity. The text's one duty to the picture is non-contradiction: rewrite a sentence only where it states the opposite of what the outline stages.

That duty covers appearance, not only events. Where the story needs to name a thing the outline describes — a garment, a costume, a creature, a prop — the prose may name it plainly but never gives it a colour, material or kind the outline does not. A page that colours such a thing differently from the outline is flagged, and the rewrite uses the outline's colour or drops the colour word.
The outline is the story in summary and the text is the telling of it, so the text carries what a summary leaves out: feelings, dialogue, and the ordinary business between its turns (going somewhere and arriving, waiting, approaching people, asking, a greeting, names given and heard). That is filler, not event: never cut it as an addition, and write it in where a page opens after a jump. What is fixed is what the story is made of: no character, creature, object, place or turn of the plot the story does not carry, and nothing that changes what happens, to whom, or what it costs.
Nothing in the story or its pictures is dangerous enough that it could lead to death — for anyone. A fault is never fixed by putting a person in danger.

{SCENE_OUTLINES}

# CURRENT TEXT

{CURRENT_TEXT}

# PAGE MEASURES (counted in code, against the reading level above)

{PAGE_MEASURES}

# AUDIT FINDINGS (from two independent readers of the finished pages)

{AUDIT_FINDINGS}

# YOUR TASK

A page is rewritable when AUDIT FINDINGS names it, and otherwise not. Every other page ships exactly as it stands, however you would have phrased it. Five cases add a page to that set and no others: a MISMATCH move needs its destination page; no early page states plainly what must happen and why, which is fixed on the earliest fitting page; a page's text states the opposite of what its scene outline stages; a page carries an abbreviation or acronym, which is named the way a child says it; a page carries a banned dash. Closing a finding on a page the finding does not name is allowed only under these five, and the ledger says which one.

The checks below are answered for the rewritable pages only, with their neighbours read as context; every other page is scanned for the five cases above and not analysed. A clean analysis section is a real answer.

Return only the pages you rewrote. Omit every page you left alone. If no page needs rewriting, return the marker followed by NONE and nothing else.

A rewritable page is rewritten for a real problem, never for stylistic preference. On every page you rewrite, also fix what follows.

Never use an em-dash (—) or an en-dash (–), anywhere, for any purpose. Replace every one you find with a comma, a full stop, a colon, or parentheses, and recast the sentence if none of those fit.

Every page you rewrite keeps the style rules below:

{STYLE_RULEBOOK}

A fault that a character acts on a reason the page has not given is closed this way: {MOTIVE_AT_THE_ACT}.

A sentence longer than the reading level allows, a subordinate clause inside another subordinate clause, or a word a child at that level does not already use: recast it for that level.

Judge the text as a reader who has only the pages in front of them. They never see the brief, the character details or the scene outlines — those are given to you so you know what the book is FOR, never as evidence that something works. A motivation, trait or fear that exists only in the character details and never reaches the page is a failure to fix, not a justification. Never resolve a weakness by citing the brief.

Work in two steps.

Step 1 — write the analysis. Answer every check below for the rewritable pages, naming pages and quoting lines. This is the work: a page is weak because a check found something specific wrong with it, not because it could be phrased differently.

Step 2 — rewrite the rewritable pages your analysis found problems on. Every other page stays exactly as it is. End the analysis with a line per named problem — the problem, then the page(s) the rewrite fixes it on, or the reason it stands. A named problem with neither is a failed review. Every AUDIT FINDING above enters that ledger the same way, and it has three ways to close: fixed on a page, the detail removed, or a stated reason it stands. A fault whose reason exists in the scene outlines is fixed by saying that reason on a page, never by citing the outline. Close a fault about a missing path, arrival or move by showing it happening on one of the pages, or by leaving it to the page turn; never by opening a page with the move already completed. A fault about something the book never established is closed by establishing it on an earlier page or by cutting the claim, never by adding a sentence that asserts both the claim and its opposite. A fault naming an ability or event the pages need but never establish is closed by establishing it earlier, never by describing the moment more vividly.

Take the second way whenever the detail that caused the fault carries no plot: drop the detail instead of inventing an event to justify it. Never invent an event that makes an established obstacle easier to pass, and never state a clock time or a named part of the day to close a fault — light, shadow and what the characters do carry time instead.

A sentence naming what a character feels, notices, or almost does is never the sentence you cut to make room. Where a fix needs room, shorten a sentence that names only objects and positions. End the ledger with one line per rewritten page listing any sentence you removed from it. A MISMATCH closed by a move is recorded as "moved from p<a> to p<b>", and the source page's line lists the sentences that left it.

A rewritten page keeps everything the fix does not touch: no fact of the page is dropped, changed or contradicted, and every pronoun and name still lands on the person, thing and moment it did before.

# THE CHECKS

Read the pages in # CURRENT TEXT against # THE STORY, # THE PAGE PLAN and # SCENE OUTLINES. Every answer names a specific page and quotes the line it is about. An answer with no page and no quote is not an answer.

The pictures are locked. Where a page's text names or implies something its scene outline does not show — a figure, an object, an action, a detail of the place — the TEXT is what moves. There is no way to change a picture from here.

Priority: sections A and B first and at length, C after them, D and E last.

---

**A. NARRATIVE & PLOT**

1. **Plot holes** — Name the weakest causal link. What happens because the plot needs it to rather than because the earlier pages forced it?
2. **Cause and effect** — Does each page's event follow from the page before? Name the transition that reads most arbitrary.
3. **Pacing** — Name the slowest page and the most rushed one, with word counts or beat density. Say what would be cut from one and expanded in the other.
4. **Stakes** — Name the beat where the stakes read weakest. If they are stated and not felt, that is a show-vs-tell failure and belongs in section B.
5. **Resolution** — Is the ending earned by the pages before it? Name one element that could be stronger.
6. **The forcing figure on the page** — Wherever a page plays out a coerced action — a punishment, a forced choice, a rule imposed, something taken — that page's TEXT names the character forcing it or carries their words. What the picture shows does not count. Name any such page where the forcing figure is absent from the words.

---

**B. CHARACTER & DIALOGUE (per-character, by name)**

For EACH of {CHARACTER_NAMES} who appears on a rewritable page write one line — none of them may be skipped, including a character the commission did not name who the pages carry anyway. If all five axes hold, write `none found`; never invent a weakness to fill the line.

Format A (all five axes hold):
- **[Name]**: consistency ✓, voice ✓, show-not-tell ✓, motivation ✓, growth ✓ — none found.

Format B (at least one fails):
- **[Name]**: consistency [✓/✗], voice [✓/✗], show-not-tell [✓/✗], motivation [✓/✗], growth [✓/✗ or N/A]. Weakest moment: Page X, "[quoted line]" — [one sentence why]. Fix: [the concrete change].

Mark an axis ✗ only where you can name the page and the line that fails it.

7. **Consistency** — Any moment out of character, any trait established early and contradicted later, any ability or fear the pages break?
8. **Voice** — Does this character sound different from the others in dialogue? Two characters whose lines could swap unnoticed is a voice failure: name both and the page.
9. **Show vs tell** — Find one emotion written as a label ("he was proud") for this character and say what action would show it instead.
10. **Motivation** — Is it clear from the pages alone WHY this character acts as they do on their busiest page? Motivation that exists only in the character details and never reaches a page fails.
11. **Growth** (main characters only) — Do they end in a different emotional or knowledge state, and on which page does the shift become visible? A shift told rather than shown fails.

---

**C. PROSE QUALITY**

12. **Reading level** — Name one word or sentence off-register for the target level and what replaces it.
13. **Repetition** — List any word (names, articles and pronouns excluded) used four or more times across the pages, and any sentence shape repeated on three consecutive pages. Name the replacements.
14. **Flow** — Name the most awkward sentence and rewrite it.
15. **Passive voice** — List passive constructions in narration and their active forms. Dialogue is exempt.

---

**D. TEXT VS THE LOCKED PICTURE**

16. **The moment** — Does each page's text narrate the moment its scene outline stages? Name every page where it narrates a different one.
17. **Figures acting on the page** — For each page, find the verbs aimed AT a main character: chasing, catching, holding back, freeing, guarding, surrounding, threatening, mocking, accusing, blocking, rescuing. Where such a verb has a named or implied doer that the page's scene outline does not stage, the picture stays as it is and the text moves: the doer's action goes off the page (already done, heard, remembered) or the moment is retold as what the picture shows. A figure mentioned in passing needs nothing.

---

**E. DO-NOT-WRITE LIST**

18. **Verification** — Confirm the pages honour the DO-NOT-WRITE LIST below: banned gestures, banned phrases, banned trait labels, banned buzzwords, the gesture re-use cap, banned plot formulas, banned page-1 openings. Name the page and the line for anything that slipped in. If nothing did, write `clean`.

{DO_NOT_WRITE_SECTION}

# OUTPUT FORMAT

Both blocks, in this order, nothing before or after.

---ANALYSIS---
<your answers to A, B, C, D and E, in that order, in the formats those sections specify, then the ledger>

---STORY TEXT---
## Page <number>
<rewritten text for that page>

## Page <number>
<rewritten text for that page>

Include a page under STORY TEXT only if you rewrote it. Use its real page number. When the analysis found nothing to rewrite, still output the analysis, then:

---STORY TEXT---
NONE
