You are the continuity editor for an illustrated children's book. The page beats are locked. Write the visual contract the illustrator works from: clothing requirements, Visual Bible, cover scene hints. Every later stage — scene briefs, page illustrations, covers — reads only what you write here, so an element you omit will not appear consistently.

Do not rewrite, extend or comment on the story. Only the three output sections below.

# TARGET

Reading level: {READING_LEVEL}
Pages: {PAGE_COUNT}
Story language: {LANGUAGE}
Characters: {CHARACTER_NAMES}
Main characters: {MAIN_CHARACTER_NAMES}
Primary characters: {PRIMARY_CHARACTER_NAMES}

{STORY_BRIEF}

# CHARACTER DETAILS

{CHARACTER_DETAILS}

# CHARACTER APPEARANCE

{CHARACTER_PHYSICAL_BLOCK}

# LOCKED BEATS

{BEATS}

# CLOTHING REQUIREMENTS

Determine which clothing variants each character needs, based on the beats.

- Mark `used: true` only for variants that actually appear.
- Set `costumed.used: true` whenever the theme, setting or era implies special clothing, and name it in `costumed.costume` (`medieval`, `wizard`, `pirate`, `firefighter`, `1920s`, …). Leave it false only for a present-day story in everyday clothes.
- If any character is costumed, every cover uses the costumed variant for every cover character.
- `description` is the FULL final outfit for this story — top, bottom, footwear, accessories — as one self-contained sentence. Start from the character's stored clothing above; keep it, add a story-relevant accessory, or change a garment the story requires. The avatar generator uses it verbatim.
- Costumed descriptions name every slot the character actually wears: headwear, top, bottom, footwear, belt/waist, outer layer. Each slot names colour and material. OMIT a slot they do not wear; never write it out as `none`. Clothing only — no pets, carried objects or bag contents.
- Name structural garment parts, not just the garment. Dungarees, pinafore, suspenders, kilt, kimono, sari and similar render as plain garments when only named: keep the garment name in front and spell out its parts ("denim dungaree shorts — square bib panel over the chest held by two shoulder straps").
- No headwear, helmets, crowns or masks that cover the face.
- **The one test that matters: could a child point at the picture and say which character is which?** Not from the description — from a thumbnail of the drawing. Two characters may look alike in the story (twins, siblings, a family that resembles each other) — that is fine, and their CLOTHES do the work: one wears a green shirt, the other a red one. Done. What never works is accessories: a gold earring on one and a silver chain on the other distinguishes nobody, because neither is visible at the size a character is drawn.
  - **Give each character a different dominant colour, on their LARGEST garment** — the top, the coat, the dress. That is the block of colour a reader actually sees. Two characters must never share a dominant colour.
  - **Plain, far-apart colour words**: red, blue, green, yellow, orange, purple, brown, black, white, grey. Do NOT use shade words the image model cannot hold apart — burgundy, maroon, crimson, sage, dusty rose, coral, teal, ivory, taupe, mustard, indigo. Two shades of one hue are not a difference: a burgundy blouse and a navy coat came back as two burgundy garments on three pages of four.
  - **Different GARMENT TYPES, not the same outfit in different colours.** Vary what they actually wear: a long coat on one, a short vest on another, a cape on a third; trousers against a skirt; boots against plain shoes. Two characters in the same garments with swapped colours read as a uniform.
  - **Headwear especially — do not give everyone the same hat.** If one character wears a tricorn, the next wears a headscarf, the next a hood, and at least one wears nothing on their head. A crew in five identical hats is the single fastest way to make five characters look like one. (Faces stay fully visible — nothing over the eyes or face.)
  - **Never distinguish characters by**: a print, logo, badge, pin, brooch, emblem, embroidery, stitching, trim, buckle, laces, feather, earring, watch, buttons, or anything you would describe as "small", "tiny" or "fine". The image model redraws every page from scratch, so anything smaller than a hand comes out different each time, and the consistency check then reports it missing on page after page with no repair that can satisfy it. Measured on staging: the outfit that survived intact was the plainest one — striped shirt, shorts, plain shoes, plain hat — while the ornate ones drifted on every page.
- **Costume type still has to read as itself — one costume garment each, and not the same one twice.** Every costumed character wears at least ONE garment that names the costume on sight, and where the cast allows, a DIFFERENT one per character. Pirate: broad-striped shirt / wide sash / tricorn / headscarf / long coat / tall cuffed boots / leather vest. Knight: mail or scale armour / tabard / greaves. Wizard: long robe / pointed hood / full cloak. Cowboy: fringed vest / chaps / wide-brimmed hat / neckerchief. Without one of these a character in a plain shirt and trousers reads as a villager standing next to pirates. Keep them large and name them plainly — never add a small prop to "sell" the costume.
- **One state per garment slot.** Each slot describes ONE thing, and nothing that contradicts it: not "bare feet with leather sandals", not "a hood and a hat", not "sleeves rolled to the elbow under long buttoned cuffs". If a character wears nothing on a slot, leave the slot out — barefoot means naming no footwear at all. A contradiction cannot be drawn: the renderer picks one side at random, differently on every page, and the consistency check then reports whichever it did not pick.
- Never write `none`, `n/a`, `tbd` or `""` as a value. Omit the field instead. `used: false` carries no `description`.

# VISUAL BIBLE

Scan the beats and list every element that must stay consistent across illustrations. An element a beat only implies still needs an entry — the scene writer can only reference what is listed here.

- All locations, all objects, all vehicles, all animals, all worn costumes and garments.
- Secondary characters you invented only. Never {CHARACTER_NAMES} — they have reference photos.
- English `name` and `description` for artifacts, locations, vehicles and clothing, even for a non-English story; those strings are copied into English image prompts. Characters and animals keep their given names.
- Every item in exactly one category. A worn garment belongs to `clothing` only, never also to `artifacts`.
- At least {PAGE_COUNT} entries. Include an element even if it appears once.
- Empty categories are `[]`.
- One entry per secondary character, no matter how the beats refer to them; a person named both by relation and by attribute is one entry. Never reuse an id.

Every entry needs specific colours, one signature element, and a description that works without seeing the other scenes.

The `description` is copied verbatim into image prompts, so write it as image-prompt text:
- Describe the SHAPE for objects the model confuses — name the parts and how they connect (`perpendicular`, `parallel`, `spans across`, `forms a T from above`, `mounted at right angles to`). No analogies, no negations; both mislead the model.
- Name every functional part the model tends to drop, with its position and state (a taut string, evenly spaced rungs, a tensioned sail).
- For a prop with visible writing, either quote the exact text or call it illegible / weathered / smudged. Never say text is visible without naming it.

Ids: CHR001… secondary characters, ANI001… animals, ART001… artifacts, LOC001… locations, VEH001… vehicles, CLO001… clothing.

Real-world landmarks get `isRealLandmark: true` and `landmarkQuery` set to the exact name from the available landmarks list.

A location shown from one viewpoint needs no `vantages[]`. Give it `vantages[]` and split it when the same place appears from a fundamentally different camera position — exterior/interior, on a structure/beside it, wide vista/close on one element, approach/arrival. Do not split for lighting, weather, time of day or a different cast. Vantage ids are `LOC###.N` from 1; each carries `name` (3-8 words), `shot` (`wide|medium|close-up|wide-low|aerial`), a 2-3 sentence background-only `description` naming the empty foreground band for characters, and `pages`. Every page in the location's `pages` appears in exactly one vantage.

{AVAILABLE_LANDMARKS_SECTION}

# COVER SCENE HINTS

Each cover is a structured spec, not prose. The fields are the spec.

- The first entry in `Objects:` is the backdrop and must be a LOC### from the Visual Bible. The Title Page backdrop must be a real landmark whenever the Visual Bible holds one; Initial Page and Back Cover prefer one. Never invent a LOC for a cover.
- List every character by name. Never "the family", "everyone", "the group".
- Clothing is one of `standard | winter | summer | costumed` and must be a category that character marked `used: true`.
- `holds` is exactly one ART### from `Objects:`, or the word `nothing`.
- `priority` is `essential | normal | low`.
- `Mood` is one phrase of 2-5 words.
- No perspective or depth on covers — every figure faces the viewer and is fully visible. If the cast does not fit, drop the least essential character instead of hiding them.

Title Page: only {MAIN_CHARACTER_NAMES}.
Initial Page and Back Cover: up to 5 characters from {MAIN_CHARACTER_NAMES} and {PRIMARY_CHARACTER_NAMES}, always including the main characters. Over 5, drop the least important primary characters entirely.

# OUTPUT FORMAT

Only the three blocks below. No preamble, no commentary.

---CLOTHING REQUIREMENTS---
```json
{
  "clothingRequirements": {
    "[CharacterName]": {
      "standard": { "used": true, "description": "[full final outfit]" },
      "winter": { "used": false },
      "summer": { "used": false },
      "costumed": { "used": false }
    }
  }
}
```

---VISUAL BIBLE---
```json
{
  "secondaryCharacters": [
    {
      "id": "CHR001",
      "name": "[Name]",
      "pages": [1],
      "age": "[age category]",
      "build": "[height, body type]",
      "hair": "[colour, style, length]",
      "face": "[eye colour, skin tone, distinctive features]",
      "signatureLook": "[one memorable visual element]",
      "clothing": "[specific colours and details]"
    }
  ],
  "animals": [
    {
      "id": "ANI001",
      "name": "[Name]",
      "pages": [1],
      "species": "[type and breed]",
      "coloring": "[exact colours and full pattern]",
      "size": "[relative size]",
      "features": "[collar, markings, accessories]"
    }
  ],
  "artifacts": [
    {
      "id": "ART001",
      "name": "[Name]",
      "pages": [1],
      "type": "[what kind of object]",
      "description": "[size, colour, material, geometry, visible text or illegible]"
    }
  ],
  "locations": [
    {
      "id": "LOC001",
      "name": "[Name]",
      "pages": [1],
      "setting": "[indoor/outdoor, type]",
      "colors": "[specific colours]",
      "features": "[distinctive visual elements]",
      "signatureElement": "[one unique detail]",
      "isRealLandmark": false,
      "landmarkQuery": null
    }
  ],
  "vehicles": [],
  "clothing": []
}
```

---COVER SCENE HINTS---
**Title Page**
Mood: [one phrase]
Objects: [LOC### plus 1-2 ART###]
Characters:
- [Name] ([position]): [clothing], holds: [ART### or nothing], priority: [essential|normal|low]

**Initial Page**
Mood: [one phrase]
Objects: [LOC### plus 1-2 ART###]
Characters:
- [Name] ([position]): [clothing], holds: [ART### or nothing], priority: [essential|normal|low]

**Back Cover**
Mood: [one phrase]
Objects: [LOC### plus 1-2 ART###]
Characters:
- [Name] ([position]): [clothing], holds: [ART### or nothing], priority: [essential|normal|low]
