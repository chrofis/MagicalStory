Describe exactly what you see in this illustration. No interpretation, no guessing what was intended — only what is visible.

For EACH human figure:
- Zone: one of `left-foreground`, `center-foreground`, `right-foreground`, `left-midground`, `center-midground`, `right-midground`, `left-background`, `center-background`, `right-background`
- Facing direction (toward viewer, away from viewer, left, right, toward another figure)
- Hair color, length, style
- Clothing: describe every visible garment with colors and type
- What they are doing (action, pose). For a mid-motion figure (jumping, falling, reaching), describe only the pose and which surface or object each foot and hand touches (or that it is in the air). Never state where the figure is coming from or going to — a still frame cannot show direction of travel, so phrases like "jumping from X toward Y" are guesses, not observations.
- What they hold in each hand (or nothing)
- Expression (if face visible)
- Approximate age group (child/teenager/adult/elderly)
- Standing surface: name the surface the feet / lower body rest on or in (cobblestones, floor, grass, sand, water, snow, a boat deck, inside a fountain/pond/basin, a rooftop, etc.), or "mid-air" if not touching a surface

For EACH notable object, animal, or vehicle:
- What it is
- Position in frame
- Is it grounded properly or floating?
- Relative size

Setting:
- Indoor/outdoor
- Key visual elements (architecture, landscape, weather)
- Lighting direction and quality
- Camera angle (low/eye-level/high, close/medium/wide)

Physics check:
- Does any figure have more than 2 arms or 2 legs?
- Are all figures and objects grounded on surfaces (not floating)?
- Any body parts merged, detached, or phasing through objects?
- Is any figure standing somewhere a person would not normally stand — in water, inside a fountain/pond/basin, inside a solid object, on top of furniture or a fragile surface, or floating? Name the figure and where.

Output as structured free text, not JSON. Be precise and factual.
