Replace the figure under the magenta crosshatch with {charName} from the reference photo. The crosshatch traces the exact silhouette — match its shape, size, and pose. Do not paint outside the crosshatch. Do not paint a smaller figure inside it.{actionContext}

Face: match the reference photo (face shape, eyes, hair, skin tone).

Every pixel outside the crosshatch is final — copy the input image verbatim. Do not re-render other characters, the background, lighting, or colours.{artStyleContext}{clothingContext}{issueContext}{textPositionContext}
