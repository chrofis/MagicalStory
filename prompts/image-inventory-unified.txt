Describe exactly what you see in this illustration. Only what is visible — no interpretation, no guessing what was intended. Do not name or identify anyone.

For EACH human figure:
- `label`: a short handle built from what is visible — age group plus the main garment's colour and type (`the young boy in green`, `the woman in the red coat`). Use it for that figure everywhere below.
- `zone`: one of `left-foreground`, `center-foreground`, `right-foreground`, `left-midground`, `center-midground`, `right-midground`, `left-background`, `center-background`, `right-background`
- `facing`: exactly one of `toward viewer`, `away from viewer`, `left`, `right`, or `toward ` followed by the other figure's own label copied in full (`toward the woman in the red coat`). No sixth form, no qualifier.
- `gaze`: where the EYES point. This is not where the body points — a figure can stand square to the camera and look down. Exactly one of `at the viewer`, `not visible`, or `at ` followed by what the eyes rest on: another figure's own label copied in full, or the object they are on. `not visible` when the eyes are turned from the camera, closed, occluded, or too small to read. Read the pupils and the eyelids, never the head or the shoulders.
- `hair`, `clothing`: colour, length and style; every visible garment with colour and type. A lower-body garment seen from behind or at a steep angle hides the split between the legs, and an over-garment seen from behind hides its front fastening: give the colour plus `type not visible from this angle` rather than choosing between trousers and a skirt, or between a blouse, a coat and a cloak.
- `eyewear`, `headwear`, `facial_hair`, `worn_carried`: each mandatory — answer `none visible` or `cannot tell at this size` when that is the answer
- `action`: pose only. Name which surface or object each foot and hand touches, or that it is in the air. Never state where the figure is coming from or going to.
- `items_held`: `{"left": ..., "right": ...}`. Each value is the name of the object that hand's fingers wrap around, or `nothing`. Two values, nothing else — no verbs, no phrases about touching, resting against, leaning on, hanging from or reaching for. A hand that an object merely overlaps or rests against wraps nothing.
- `expression`: if the face is visible, else `cannot tell at this size`
- `emotion`: exactly one of {EMOTION_ENUM}, or `unreadable`. No other value, no qualifier, no combination. Read the mouth corners, the brows and the eye openings, and pick the one the face most plainly shows. A face that is merely calm or attentive is `neutral`. `unreadable` when the face is turned away, occluded, or too small to read.
- `age_group`: child, teenager, adult, or elderly
- `apparent_age`: best estimate of the figure's age in years, from head size relative to the body, limb proportion and build — a young child's head is a much larger fraction of its height than an adult's. Give a single number, or a narrow range when unsure (`4-6`). `cannot tell` only when the figure is too small or too occluded to judge.
- `standing_surface`: what the feet or lower body rest on or in, or `mid-air`
- `body_bbox`: `[x1,y1,x2,y2]` normalised 0-1 around the whole figure — head to lowest visible point, including clothing and anything worn
- `face_bbox`: the same shape around the face, or `null` when no face is visible
- `same_ground_plane`: false when the feet rest at a different depth or surface from the other figures
- `clipped_by`: `none`, `frame`, or the element that cuts the figure off
- `height_rank`: shortest, middle, tallest, or equal to another label, among figures at the same distance from the camera. Judge it by head size relative to the body and by where the feet meet the ground, never by how much of the frame the figure fills. `cannot tell` when the figure is seated, kneeling, cropped, or clearly nearer or further than the others.

`interactions`: one entry per engaged pair, both referred to by label — who faces, looks at, reaches toward, holds or touches whom, and where the contact is. Facing, gaze, reach and contact only. Empty when no two figures are engaged.

`objects`: each notable object, animal or vehicle — what it is, dominant and secondary colour, what it looks made of, position in frame, whether it is grounded or floating, and its size relative to the figures. For an animal or creature add `complete`: true when it reads as one whole body; false when its head or body is missing where it should be visible. Frame crop or occlusion by scenery is not incomplete.

`setting`: indoor or outdoor, key visual elements, lighting direction and quality, camera angle.

`lettering`: every visible letter, number, word or symbol painted anywhere — signs, walls, paper, garments, banners, captions or watermarks. Quote each exactly, name the surface and position, and say whether it spells real words or unreadable scribble. Empty when there is none.

`rendering`: `extra_limbs` true only for a third arm, third hand or extra leg. `physics_ok` false for floating figures, impossible poses or detached limbs. `composite_seam` true when a straight or jagged boundary crosses the picture and the background, lighting or brushwork does not continue across it. `issues` lists any other visible anatomy or physics fault. Hidden or occluded parts are not faults; minor finger problems are not faults.

`scene_summary`: ONE sentence, 25-40 words, referring to figures by label. Observable verbs only — `aims`, `points at`, `extends arms toward`, `holds out with palms up`, `grips`, `kneels`, `raises`, `faces`, `looks at`, `stands over`. Never intent words: `offers`, `hands over`, `gives`, `presents`, `surrenders`, `threatens`, `attacks`, `greets`, `comforts`, `protects`.

`main_action`: the same action as a short observable phrase.

---

Return JSON only, no other text. Emit exactly these keys.

```json
{
  "figures": [
    {
      "id": 1,
      "label": "the young boy in green",
      "zone": "center-foreground",
      "facing": "right",
      "gaze": "at the small dog",
      "hair": "dark brown, short",
      "clothing": "green short-sleeved shirt, dark blue shorts, white trainers",
      "eyewear": "none visible",
      "headwear": "none visible",
      "facial_hair": "cannot tell at this size",
      "worn_carried": "brown satchel on a shoulder strap",
      "action": "standing, both feet on the cobblestones, right arm raised",
      "items_held": { "left": "nothing", "right": "a lantern" },
      "expression": "mouth open, eyebrows raised",
      "emotion": "surprised",
      "age_group": "child",
      "apparent_age": 6,
      "standing_surface": "cobblestones",
      "body_bbox": [0.28, 0.20, 0.49, 0.86],
      "face_bbox": [0.31, 0.22, 0.44, 0.38],
      "same_ground_plane": true,
      "clipped_by": "none",
      "height_rank": "middle"
    }
  ],
  "interactions": [
    { "from": "the young boy in green", "to": "the woman in the red coat", "observed": "faces her and extends the lantern toward her at chest height" }
  ],
  "objects": [
    { "what": "a large dog", "colour": "golden, darker ears", "material": "fur", "position": "right-foreground", "grounded": true, "relative_size": "reaches the boy's waist", "complete": true }
  ],
  "setting": { "indoor_outdoor": "outdoor", "elements": "narrow street, stone buildings, shuttered windows", "lighting": "low warm light from the left", "camera": "eye-level, medium" },
  "lettering": [
    { "text": "BAKERY", "surface": "a hanging wooden sign", "position": "left-midground", "readable": true }
  ],
  "rendering": { "extra_limbs": false, "physics_ok": true, "composite_seam": false, "issues": [] },
  "scene_summary": "The young boy in green extends a lantern with arm raised toward the woman in the red coat, who stands with hands cupped to receive it, on a cobblestone street at dusk.",
  "main_action": "extending a lantern toward another figure"
}
```
