You are reviewing every illustration brief for one children's book at once, before any image is generated. Judge them as a set: problems that matter most here are the ones only visible across pages.

# TARGET

Write in ENGLISH. These briefs are working notes for the illustrator, never shown to a reader.

Pages: {PAGE_COUNT}
Characters: {CHARACTER_NAMES}
Max named characters per scene: {MAX_CHARACTERS_PER_SCENE}

{STORY_BRIEF}

# CHARACTER DETAILS

{CHARACTER_DETAILS}

# PAGE PLAN

One line per page — the picture each brief must stage: shot, who is in frame, the instant, what is true after.

{PAGE_PLAN_LINES}

# ALL SCENE BRIEFS

{ALL_SCENES}

{VISUAL_BIBLE}

{CLOTHING_FINDINGS}

{BRIEF_FINDINGS}

# YOUR TASK

Answer each check, naming pages. Then rewrite only the pages a check faulted. The PAGE PLAN line is the authority for its page: a rewrite never moves an object the plan line places, never drops a character the plan line puts in frame, never keeps a character the plan line does not name, and never adds an action the plan line does not name. When a finding's fix would require any of those, simplify inside the plan line instead — a page faulted for two actions loses the action the plan line does not name, it never gains a third. A fault you name must change a page: end your analysis with a line per named fault — the fault, then the page(s) your rewrite fixes it on, or the reason it stands. A named fault with neither is a failed review.

Every check carries a tag in square brackets. Open each answer with that tag, then the pages: `[gaze_budget] pages 4, 9` or `[gaze_budget] none`. One line per tag, every tag answered.

<!-- CLOTHING_MECHANICAL_BEGIN -->
0. **[clothing_mechanical]** Mechanical clothing faults — not optional. If a MECHANICAL CLOTHING FAULTS section appears
above, every page it lists is faulted. These were found by exact comparison against the story's
clothing requirements, so they are facts, not opinions: do not re-judge them, do not argue that the
prose reads fine. Rewrite EVERY page listed there, and output each one under ---SCENES---. You may
not answer NONE while that section is non-empty. Fix only the named fault — add the `wornItems` row
the fault asks for to the page's JSON object exactly as the fault words it, `wearer` included, with
the prose agreeing — and change nothing else on the page.
<!-- CLOTHING_MECHANICAL_END -->

1. **[page_repetition]** Repetition — name any two pages whose images would look near-identical: same location, same grouping, same camera distance, same action. Say which one should change and how.
2. **[visual_arc]** Visual arc — read as a flipbook, does framing build with the plot? Name the page where intensity sags or spikes out of order.
3. **[continuity]** Continuity — does anything contradict an earlier page: a character's clothing, an object that was left behind, a time of day that moves backwards, a location that changes shape?
3a. **[light_fields]** Light — {SCENE_LIGHT_FIELD} Name any page whose `timeOfDay` or `weather` is missing, outside those values, disagrees with the light or sky its prose describes, or breaks the book's time: an hour that moves backwards, or a sky that changes between consecutive pages at the same place with no plan line moving it. Rewrite the page's fields and the prose's light together so they agree.
3b. **[clothing_incomplete]** Clothing completeness — every character in a brief must be described wearing a top, a bottom and footwear; a character whose outfit replaces the legs needs the tail or fin instead of bottom and footwear, and must have no feet or shoes. Name any page whose prose dresses a character in headwear alone, or in fewer than those three, or that names a clothing category key (`standard`, `costumed`) instead of the garments. A character whose body has no feet in the scene (a tail, fins) is exempt from footwear there.
3c. **[clothing_owner]** Clothing ownership — a garment belongs to the character the other briefs dress in it. Name any page whose prose puts that garment on a second character, or attaches one character's garments to another character's name. A figure the prose introduces by appearance rather than by name wears the outfit of the character its `characters[]` entry places there; an item this page's `wornItems` row hands to another wearer is that wearer's. Rewrite the clause into the wearer's own garments, never by leaving a character's clothing unstated.
4. **[interaction_multiple_actions]** Single moment — name any brief that describes more than one moment in time, a montage, or an action with no visible subject. Characters on one page share one focal point visible in the frame; a page where each figure performs its own separate activity is a fault even when the actions share a cause. Characters with the same goal share one activity, staged where the plan line puts them — together, or each at their own spot when the plan line scatters them. Rewrite around the single focal event by removing the action the plan line does not name — never by adding or substituting one, and never by regrouping figures the plan line places apart.
5. **[cast_over_cap]** Cast — name any page whose plan line (PAGE PLAN above) puts a character in frame that the brief leaves out. A brief with no named characters is correct when its plan line stages the subject alone; fault it only when the plan line names a character or the page carries nothing. If PAGE PLAN reads (no plan data), skip the comparison. Separately, a page carrying more than {MAX_CHARACTERS_PER_SCENE} named characters is worth a second look at its composition — name it and say what would read better. That half is a recommendation, not a fault, and never a reason to drop anyone.
5a. **[cast_not_in_plan]** Imported cast — name any page whose brief lists in `characters[]`, or names in its prose as present, a character the page's plan line (PAGE PLAN above) does not name. The plan line decides who is in the frame; a brief may not add anyone, however natural their presence would be — a crew to its vessel, a family to its house, the seekers to the thing found. {PLAN_LINE_CAST} A plan line that names nobody is a subject page: its `characters[]` is empty and its prose names no one. Rewrite by removing the imported characters — from `characters[]`, `objects[]` and the prose alike — and restating the scale the plan line gives the subject, never by keeping them small or far away. If PAGE PLAN reads (no plan data), skip the comparison.
5b. **[cast_crowded]** Figure count — name any page whose frame holds more than {MAX_CHARACTERS_PER_SCENE} figures, named or anonymous, at foreground or midground depth. The book's one whole-cast shared-action page, or a crowd the action needs, is exempt. A page whose `population` is `ambient` or `crowd` is counted on its named and staged figures only — the setting's own distant background people are not figures for this check and are never restaged or removed. This is a composition recommendation, not a fault: restage it only where the plan line supports the separation — a figure pushed to background depth, one standing behind another, one at distance, one cropped at the frame edge. Every name stays in `characters[]` and in the prose at its new place. Never drop a character, and never replace a name with a description of the figure — a commissioned character is drawn as themselves or not at all. Where the plan line supports no restaging, leave the page as it stands and say so.
5c. **[extras_undescribed]** Unnamed figures — name any page whose prose introduces unnamed figures (a crowd, a procession, villagers, soldiers, onlookers) without giving them an appearance of their own: varied hair colours and lengths, more than one garment shape, several garment colours, a mix of heights and ages. A bare `anonymous`, `generic` or `unnamed` group is a fault — with no look of its own it is drawn as copies of the nearest described character. A group in matching uniform, kit or livery is exempt from the garment variation and varies hair, height and age instead. Rewrite the group's description; never drop it. On an `ambient` page the background people stay distant and unnamed and need only a density and a distance, not an appearance each — never demand one, and never ask for them to be cut.
5d. **[population_contradicted]** Setting population — a brief's `population` field and its prose must describe the same place. Name any page whose `population` is `cast_only` while the prose puts unnamed figures in the frame: people crossing a square, riders passing, adults at the tables, onlookers, a group in the background. Every downstream reader takes that field as the roster of who may be in the picture, so figures the prose orders onto a `cast_only` page are drawn and then counted as surplus cast. Fix the page by setting `population` to `ambient` when the setting is a public place that simply has people in it, or to `crowd` when the page is written around them — and leave the prose as it stands. Only where the unnamed figures do not belong in the picture at all, cut them from the prose instead and keep `cast_only`. A BRIEF FAULTS line for this tag is a comparison already made against the page's own metadata: rewrite every page it names.
6. **[character_fields]** Character fields — every character needs depth and a drawable expression naming brows, eyes or mouth. Name any that is missing one, or that uses a bare mood word. {EYES_OPEN} {CREATURE_FACE} Name any figure or creature that breaks either.
6b. **[scene_intent]** Scene intent — every page needs a `sceneIntent`. Name any that is missing, that covers more than one moment, or that names a character absent from `characters[]`.
6c. **[depth_unearned]** Depth must be earned — every character in `characters[]` must be described in the prose, and one marked `background` must be placed there in words: further off, small in the distance, across the space, behind the others. Name any page listing a character the prose never describes, or marking a character `background` while the prose gathers the whole cast in one spot. A `background` figure also needs the plan line to require the separation (left behind, watching from afar, arriving in the distance) — otherwise bring them to the group's depth. Characters standing on one small shared surface — a boat, a raft, a cart, a wagon, a sled — are all at the same depth: name any page marking one of them `background` and change that depth to match the others. Rewrite the prose to place that character at its declared depth, or change the depth to match the prose.
7. **[drawability]** Drawability — name any instruction an illustrator could not execute: a character facing one way while looking at someone behind them, two body actions that cannot hold at the same instant — a body part held against a surface while that same part acts elsewhere — an impossible viewpoint, a described detail too small to read at page size, or a character declared `back view` whose action target the brief places closer to the camera than they are — moving toward a foreground character requires facing the camera. Rewrite an incompatible pair down to the action the page's declared interaction names. Also name any character interacting with an invented feature of a real landmark (a gap, opening, or ledge the landmark does not have) — rewrite to a feature the landmark actually has. Also name any page whose brief draws two named figures on different levels — one on a deck, floor, bank, wall or roof, the other on the water, ground or stair below — at equal size facing each other across the edge; the renderer puts them on one plane and raises the lower surface. Rewrite so the camera stands on one level: the other figure small and far below at `depth: background`, or high at the edge above. Both stay in `characters[]`.
7b. **[closeup_below_waist]** Close-up framing — a `close-up` frame ends at the waist. A sitting, kneeling or crouching POSE is not a fault: the legs are cropped, which is what a close-up is. Name only a page with `shot: close-up` whose prose or interactions make something below the frame line the subject — {CLOSEUP_BELOW_WAIST}. A garment list naming footwear does not count. {CLOSEUP_KEPT} So on a close-up the plan line asks for, rewrite the prose waist-up and keep `shot`; on one it does not ask for, rewrite `shot` to `medium`. Change nothing else.
7c. **[themed_setting_bare]** Themed-world setting — a page set in a fantastical world, or a real place the story's premise has transformed into one, dresses its environment with a lot of that world's characteristic elements. Name any such page whose setting is bare and rewrite it, adding elements to the environment and background only — never characters, never the action.
7d. **[footing]** Footing — name any standing character whose declared position lands where the scene offers nothing to stand on (open water, air, a drop) while their `position` field is a bare grid term. A figure on ground, in the water as a swimmer, or whose position already names its surface is fine. Partially immersed figures (wading, ankle-deep, knee-deep) are a fault — restage at the water's edge or as swimming. Rewrite the faulted position to name the standable surface (bank, path, deck, walkway).
8. **[gaze_budget]** Gaze — a page names at most one gaze target, and the composition makes that target the frame's dominant element: large, central, or nearest the camera. Name any page naming a second gaze target, aiming a gaze at something small or distant, or sending a gaze to the viewer. Rewrite so every figure looks at the one target or at the page's action, and enlarge or centre that target. {COVER_GAZE_EXCEPTION} Read each character's gaze from `looksAt`; two named characters whose `looksAt` is each other are ONE target, not two.
8b. **[contact_not_pose]** Contact — a demanded grip or touch must be written as a visible pose: what the fingers wrap, where the weight leans, what the body presses against. Name any page whose prose says only "hand on X", "grips X", "touches X". Name also any figure given two grips at once. {SHARED_GRIP} Rewrite the prose into the visible pose and leave one hand-duty per figure.
8bb. **[facing_not_per_character]** Facing per character — a character whose `characters[]` entry declares a `perspective` or facing has that facing stated inside their own prose clause ("…, seen from behind, walking away from the viewer"); a collective sentence ("all five are seen from behind", "the group walks away from the viewer") does not count. Name any page where a declared facing appears only collectively or not at all in that character's clause, and rewrite by adding it to each clause.
8bc. **[gaze_missing]** Eyes — name any foreground or midground character with no `looksAt`, or whose `looksAt` contradicts their own prose clause or the plan line: a plan line that stages two named characters facing each other gives each the other unless it names a different gaze for one of them; a character holding a thing looks at it only when the plan line says so. A secondary character staged only as a CHR id in `objects[]` needs a `watching` interaction saying what it looks at: name it when it has none. Rewrite the `looksAt` (or the `watching` row) and the clause together; hands stay in `interactions[]`.
8c. **[force_at_rest]** Force — `taut`, `strained`, `braced`, `straining` render at rest. Name any page relying on those words and rewrite them as the shape the force makes ("the rope is a straight line from the post to her hands", "the sail curves out toward the bow").
8d. **[elevation_unsupported]** Elevation — a figure off the ground needs its support written into the pose (which rung the feet are on, which edge the hands hold, what the heels press against). Name any elevated figure without one and rewrite the pose to name the support.
9. **[negation_named]** Negation — naming a thing that must be absent paints it. Name any page containing "no …", "without …", "bare …", "does not …", "empty of …" in its prose or any metadata field, and rewrite it to describe what does occupy that space.
9b. **[prop_unheld]** Props — a plot-critical object is in a named character's hands, or it is the page's subject filling the foreground. Name any page where the plan line's key object is neither, and any page with more than two named props standing unheld. Rewrite by putting the object into a character's hands or by making it the subject.
9c. **[scale_unanchored]** Scale — `small in the distance`, `far off`, `tiny` render at foreground size. Name any page using them without a comparison to something visible in the same frame, and rewrite with the comparison ("the far ship spans less than the main character's hand").
9d. **[element_uncited]** Element coverage — name any page whose plan line or prose stages a named element — a prop, a creature, a garment, a place — while `objects[]` omits the id other pages give that same element. Rewrite by adding that id to `objects[]` and leaving the prose as it is; never cite an id for something the page does not stage, and a page at its element cap withdraws its least important citation rather than going over.
9e. **[element_stranded]** Left elements — an element an earlier plan line set down at a location stays there for the next two or three pages set at that same location. Name any page inside that window, at that location, with no plan line moving the element, whose `objects[]` omits its id. Rewrite by adding the id and one prose clause placing it where it lies — the place, never a character the plan line does not put in this frame. A page that takes in the whole place from a distance, points its camera away from where the element lies, or has moved elsewhere is correct as it stands, and so is a page past the window; an element named only in a plan line's closing "what is true after" clause was never placed there.
9f. **[vb_state_range]** Object states — an object the story changes is ONE entry whose `states[]` are the looks it wears, in the order the story reaches them. The FIRST state is the unaltered look and carries every page before the first change; each later state's pages begin at the page whose plan line makes that change and run until the next one. Every page cites the dotted id of its state, the pages before the first change included. Name any entry in VISUAL BIBLE whose state pages disagree with the plan lines — a first state that is a change, a state claiming pages before the plan line makes it, a page belonging to no state — and output the corrected entry under ---VISUAL BIBLE---. {MULTI_PICTURE_PROP} Name also any state a page cites whose `delta` shows something other than what that page's plan line says the object shows, and correct that state's `delta` — or split the entry into one face state per picture — in the entry you output.

9g. **[required_text_undeclared]** Readable lettering — {REQUIRED_TEXT_AUTHORING} Name any page whose plan line or prose has someone read, spell out or point at specific characters while every element its `objects[]` cites shows `text: (none declared)` in VISUAL BIBLE — DECLARED TEXT. Output the corrected entry under ---VISUAL BIBLE---, that id with its `text` set to the exact characters the plan line requires, and add the id to that page's `objects[]` if it is missing. Name also any entry declaring `text` no plan line asks a reader to read, and output it with `text` emptied. When no listed element could carry the lettering, name the page and say so — the entry has to be written upstream and no rewrite here can supply it.

10. **[closeup_environment]** Close-up hygiene — a `close-up` brief describes only what that frame holds. Name any `close-up` page carrying a paragraph of surrounding environment and rewrite it, leaving the setting to the vantage plate. A close-up the plan line asks for keeps `shot` (7b); one it does not ask for may become `medium` instead.
10a. **[plate_contains_effect]** Plate without the action — a vantage's `emptyScenePrompt` in VANTAGE PLATES is the place its pages stand in, without what they do there. Name any vantage whose plate describes the result of an action on one of its pages: what that page's plan line instant or its "what is true after" segment produces (a jet of water, a splash, a break, smoke, dust, a fall, a thing moved from where the plan line puts it). Rewrite so the plate holds the static place with its props in place, and output the corrected entry under ---VISUAL BIBLE---; the effect appears only with the character, in that page's prose and `sceneIntent`.
10aa. **[result_not_at_contact]** Tip on the target, receiver clear (AD rule 7g) — when the page's action is a tool acting on one thing while a second thing receives the result, the prose must say where the tip touches the target, keep the receiver clear of the tool and never between actor and target, and place the result where the tool touches. Name any page whose prose leaves the tip unplaced, lets the receiver sit under or between, or has the result appearing from the tool or at the receiver instead of at the point of contact. Rewrite: place the tip on the target in words, move the receiver clear, put the result at the contact. Name also any `interactions[]` row whose `where` or `action` names a second object that receives the result ("into the trough", "filling the bucket") while the row has no `receiver` field; rewrite by setting `receiver` to that object's VB id or name.
10b. **[era_landmark_named]** Real place, other era — naming a real location pulls in how it looks today. Name any page whose `era` or genre differs from the place as it stands while the prose names the place, and rewrite to describe its shape, materials and surroundings instead; leave its LOC id in `objects[]`.
10c. **[critique_in_brief]** Critique leakage — a brief must describe characters only from CHARACTER DETAILS. Name any page whose prose or metadata carries review commentary, or gives a character an age, build or outfit that CHARACTER DETAILS does not, and rewrite it back to the character's real description.

# OUTPUT FORMAT

Both blocks, in this order, nothing before or after.

---ANALYSIS---
<one tagged line per check, in order, each naming its faulted pages or `none`>
FAULTED PAGES: <the page numbers a check faulted, comma-separated, or NONE>
REMOVED CAST: <one entry per page, `page = name, name: why the page no longer stages them`, semicolon-separated, or NONE>

The FAULTED PAGES line is the last line of the analysis, and every page on it
must appear below. Naming a page's defect and not rewriting it leaves the
defect in the book.

A rewrite that takes a character out of a page's `characters[]`, or stops naming
them in its prose, declares it on the REMOVED CAST line. Staging a character out
of frame, behind another figure, at distance or cropped at the edge keeps them in
`characters[]` and is not a removal. A name is never swapped for a description of
how the figure looks.

---SCENES---
## Page <number>
<the corrected brief for that page, complete: its prose, then a line reading ---METADATA---, then its JSON object unfenced — the shape it arrived in>

Include a page only if you rewrote it. Use its real page number. When nothing needs rewriting, still output the analysis, then:

---SCENES---
NONE

---VISUAL BIBLE---
```json
{"artifacts": [{"id": "<id>", "states": [{"id": "<id>.1", "name": "<look>", "delta": "<what differs from the entry's description>", "pages": [<pages>]}]}]}
```

Optional, and the last block. Carry ONLY the entries you corrected, in the collection they came from, in the shape above. Omit the whole block when you corrected none. Only `states[]` may change; no entry is added, removed or renamed, and no other field is touched. For an entry you do carry, `states[]` is its COMPLETE list of looks: repeat every state the entry already has, the ones you did not touch included. A state you leave out is deleted, and the pages that cited it lose the look they asked for.
