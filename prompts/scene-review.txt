You are reviewing every illustration brief for one children's book at once, before any image is generated. Judge them as a set: problems that matter most here are the ones only visible across pages.

# TARGET

Write in ENGLISH. These briefs are working notes for the illustrator, never shown to a reader.

Pages: {PAGE_COUNT}
Characters: {CHARACTER_NAMES}
Max named characters per scene: {MAX_CHARACTERS_PER_SCENE}

{STORY_BRIEF}

# CHARACTER DETAILS

{CHARACTER_DETAILS}

# PAGE BEATS

One line per page — the story beat each brief must depict.

{PAGE_BEATS}

# ALL SCENE BRIEFS

{ALL_SCENES}

{CLOTHING_FINDINGS}

{BRIEF_FINDINGS}

# YOUR TASK

Answer each check, naming pages. Then rewrite only the pages a check faulted.

0. **Mechanical clothing faults — not optional.** If a MECHANICAL CLOTHING FAULTS section appears
above, every page it lists is faulted. These were found by exact comparison against the story's
clothing requirements, so they are facts, not opinions: do not re-judge them, do not argue that the
prose reads fine. Rewrite EVERY page listed there, and output each one under ---SCENES---. You may
not answer NONE while that section is non-empty. Fix only the named fault — move the garment onto
its rightful owner, or state the outfit the fault says is missing — and change nothing else on the page.

1. Repetition — name any two pages whose images would look near-identical: same location, same grouping, same camera distance, same action. Say which one should change and how.
2. Visual arc — read as a flipbook, does framing build with the plot? Name the page where intensity sags or spikes out of order.
3. Continuity — does anything contradict an earlier page: a character's clothing, an object that was left behind, a time of day that moves backwards, a location that changes shape?
3b. Clothing completeness — every character in a brief must be described wearing a top, a bottom and footwear. Name any page whose prose dresses a character in headwear alone, or in fewer than those three, or that names a clothing category key (`standard`, `costumed`) instead of the garments.
4. Single moment — name any brief that describes more than one moment in time, a montage, or an action with no visible subject.
5. Cast — name any scene with more than {MAX_CHARACTERS_PER_SCENE} named characters, or a character present in the page's beat (PAGE BEATS above) but absent from the brief. If PAGE BEATS reads (no beat data), skip the beat comparison.
6. Character fields — every character needs depth and a drawable expression naming brows, eyes or mouth. Name any that is missing one, or that uses a bare mood word.
6b. Scene intent — every page needs a `sceneIntent`. Name any that is missing, that covers more than one moment, or that names a character absent from `characters[]`.
6c. Depth must be earned — every character in `characters[]` must be described in the prose, and one marked `background` must be placed there in words: further off, small in the distance, across the space, behind the others. Name any page listing a character the prose never describes, or marking a character `background` while the prose gathers the whole cast in one spot. Rewrite the prose to place that character at its declared depth, or change the depth to match the prose.
7. Drawability — name any instruction an illustrator could not execute: a character facing one way while looking at someone behind them, an impossible viewpoint, a described detail too small to read at page size.
7b. Close-up framing — a `close-up` frame ends at the waist. Name any page with `shot: close-up` whose prose, poses, or interactions stage below-waist action — kneeling, crouching, sitting, stepping, or feet-on-ground contact. A garment list naming footwear does not count. Rewrite that page's `shot` to `medium` and change nothing else.
7c. Themed-world setting — a page set in a fantastical world, or a real place the story's premise has transformed into one, dresses its environment with a lot of that world's characteristic elements. Name any such page whose setting is bare and rewrite it, adding elements to the environment and background only — never characters, never the action.

# OUTPUT FORMAT

Both blocks, in this order, nothing before or after.

---ANALYSIS---
<your answers to checks 1-7>
FAULTED PAGES: <the page numbers a check faulted, comma-separated, or NONE>

The FAULTED PAGES line is the last line of the analysis, and every page on it
must appear below. Naming a page's defect and not rewriting it leaves the
defect in the book.

---SCENES---
## Page <number>
<the corrected brief for that page, complete: prose then its JSON object, in the same shape it arrived>

Include a page only if you rewrote it. Use its real page number. When nothing needs rewriting, still output the analysis, then:

---SCENES---
NONE
