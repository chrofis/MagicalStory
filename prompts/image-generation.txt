{SCENE_INTENT}

Generate a SINGLE illustration filling the canvas, bleeding off all four edges — no borders, frames, margins, white edges, panel keylines, split screens, grids, or collages. No lettering on any surface — no words, names or letters anywhere in the image. The only exception is a REQUIRED TEXT block below; with no such block, nothing in the image is lettered. Bracketed names like `[CharacterA]` and object ids are identifiers only — never painted into the image. Draw exactly one of each named object.

When the FIRST reference photo shows a real location (architecture, landscape, no characters), render the scene IN it — copy its setting and geography, not its rendering: if it looks photographic, the ART STYLE below still governs. Its time of day and weather give way to the LIGHT line, when one is given. All OTHER reference photos define character IDENTITY only — face, hair, skin tone, build, clothing — never pose, framing, or composition. Each character's position, action, body direction and size come from the scene description and the COMPOSITION rules below.

{CHARACTER_REFERENCE_LIST}

{SCENE_DESCRIPTION}

{COMPOSITION}

{REQUIRED_OBJECTS}

{VISUAL_BIBLE}

{ERA_GUARD}

{SEASON_NOTE}

{LIGHT_NOTE}

{TEXT_AREA_INSTRUCTION}

{COVER_COMPOSITION}

**ART STYLE:** {STYLE_DESCRIPTION}

{REQUIRED_CAST}

**DEPTH AND SIZE:** Depth is distance from the camera, not size: `foreground` is nearest and most prominent, low in the frame; `midground` sits in the middle band, further back; `background` is furthest and smallest. The same element is drawn larger in the foreground than it would be deeper in the frame — but a size stated for an element always wins over its depth: a small object in the foreground is drawn close and sharp at its true size against the figures near it, never enlarged to fill the lower half. An element with no stated size that is genuinely large — a vessel, a building, a vehicle, a big animal — does dominate the foreground.

{COUNTS}

{NO_CHARACTER_MARKING}

{REQUIRED_TEXT}

{HANDS_HOLD_ONLY_NAMED}

{SHOT_DEFINITIONS}
