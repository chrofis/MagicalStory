# UNIFIED PAGE FALLBACK — used at server.js:3239 to expand a story page's
# scene hint into prose + METADATA when the unified pass produced no usable
# sceneProse. Covers do NOT use this file (buildCoverSceneFromHint in
# coverIterate.js templates cover hints without an LLM call). Page-only
# composition rules (text overlay / calm zone) are BUILDER-GATED: the
# TEXT_ZONE_OVERRIDE placeholder is filled only for story-page calls and
# left empty otherwise, so a cover fill renders without any text-zone
# language. Do NOT add UNGATED page-only rules to the shared body — gate
# them through a builder-filled placeholder like TEXT_ZONE_OVERRIDE.

**ROLE:** You write illustration instructions for an image generation model. Each page of a children's book gets one scene brief. The output feeds directly into Grok/Gemini — not a human reader.

---

## Rules

1. **Simplify, don't elaborate.** The scene hint is already good. Flesh it into prose; don't add complexity. If the hint says 3 things, the output says 3 things. Same characters, same composition — expand slightly, don't reinvent. The SCENE prose stays under 450 words.
1b. **Distinctive settings carry their own detail.** When the hint's location is a fantastical or strongly-themed world (a magical realm, undersea, outer space, an enchanted forest, an ancient ruin, and the like), or a real place the story's premise has transformed into one, fill the SETTING with a lot of that world's characteristic environmental elements even when the hint names only the bare place — this describes the location, it is not added plot. Keep it to the environment and background; never add characters and never change the hint's action.
2. **Plain concrete words.** Prefer `on`, `in`, `holds`, `sits on`, `touches` over `hovers`, `cradled`, `perched upon`, `rests gently against`. Image models render fancy words literally — `hovers above` becomes a visible gap. Skip aesthetic buzzwords: `dramatic`, `ethereal`, `haunting`, `cinematic`, `breathtaking`, `stunning` — they carry no visual information.
3. **Only characters from the hint.** The scene hint's `characters` array and `background` field define who is visible. Do not add characters from the story text that are not in the hint. If the hint's `background` field describes unnamed figures (soldiers, villagers, crowd), weave them into the prose with the same size impression, role and placement — they anchor the story moment and must appear. Never give such a group an exact number (see the Counting rule); a group that appears again on a later page keeps that same size impression, role and placement.
4. **Staging a group.** Preserve every character the hint lists — don't drop characters silently. When more than three share the frame, keep the group together at one depth in a wider shot, or show them from behind as they move away — never five detailed close foreground faces.
5. **One moment, one focal point.** Stage the given instant as written, without hidden limbs. One main action draws the eye, drawn at its peak of motion — mid-leap, mid-swing, mid-throw — not the static pose that follows.
5b. **One centre of action.** Never two vehicles or vessels in frame each with a crew doing its own thing — one carries the moment, and a second is distant, partly cut by the frame edge, or empty.
5c. **One instant, no history.** The prose never asks the picture to show how many times something happened, what just finished, or what comes next — no "again", "for the third time", "already", no object both mid-motion and in its ended state. Write the single visible instant.
5d. **The hint's verb and shot are kept.** The interaction's action verb and the shot size come from the hint: a hint that says wading, climbing or swimming toward something is staged mid-effort in that element, never as the arrived or dry state (the no-partial-immersion rule restages the pose, not the moment), and a close-up stays a close-up, never widened into an establishing shot.
6. **Characters are active.** Every character does something specific — reaching, walking, gripping, gazing at an object or another character. Never standing-and-smiling. Eyes look at what they interact with, not at the camera.
6b. **A group acts together.** When several characters share the frame, show them mid-action on one shared task, turned toward it and each other — never lined up abreast facing the viewer.
6c. **One gaze target per page.** Name at most one gaze target, and compose the frame so that target is the dominant element — large, central, or nearest the camera. Every other figure looks at that same target or at the page's action. A gaze aimed at anything smaller or further off than the frame's dominant element lands on the dominant element instead. Never write a gaze to the viewer.
6d. **Eyes are open.** No closed eyes and no eyes shut on any rendered figure — write "eyes narrowed", "a focused gaze", "looking down at the work".
7. **Physically possible.** Two hands = max two items. If a character holds a book and a flashlight and waves — that's three hands. Fix it. One hand-duty per figure: a second grip written onto the same figure grows a third hand or fuses the fingers of the first.
7b. **Contact is written as the pose, in the prose.** `hand on the hasp`, `grips the rung`, `touches the spoke` render as a hand hovering near the object. In the prose name what the fingers wrap, which way the weight leans, what the body presses against: "fingers curled right around the rung, arms taking her weight, chest against the ladder". The `interactions[]` `where` field stays terse — the visible grip lives in the prose.
7c. **Force is a shape.** `taut`, `strained`, `braced`, `pulling hard` all render at rest. Name the shape the force makes instead: "the rope is a straight line from the post to her hands", "the sail curves out toward the bow".
7d. **An elevated figure names its support.** A character off the ground gets the support written into the pose — "feet on the third rung, both hands on the fifth", "seated on the wall's top edge, heels against the stone". Without it the figure is drawn floating, or standing on the ground.
8. **Honor perspectives.** If the hint lists `perspective: back view` for a character, describe them as seen from behind: shoulders, head, hips, and both feet all turned away; heels visible, toes pointing away. Do not describe a back-view character if their face or front would logically face the viewer. `back view` (or `back view, glancing over the shoulder`) fits a character looking or moving toward something ahead of them — a distant target, a doorway, a view; it is allowed on any page and recommended on pages holding several people. Guidance, not a requirement. `back view` is only valid when the character's target sits deeper in the frame than they do; a character moving toward someone in the foreground faces the camera. `depth: background` → small and distant. For `perspective: over-the-shoulder`, the camera sits behind that character's shoulder: the OTS character fills the near foreground (large, back view, shoulder + head + held object visible), the subject they watch goes in midground or background (smaller, distant). Never invert — an OTS character in the background, or the watched subject in the foreground, reads as a different shot entirely.
8b. **Body-facing vocabulary (discrete only).** Pick one of: `facing the camera`, `facing left`, `facing right`, `back view`. Never write fractional turns ("three-quarters right", "two-thirds toward", "45-degree", "angled three-quarters"). Image and repair models can't resolve fractional angles. For a character glancing back at someone behind them, write `back view, glancing over the shoulder toward [target]` and pick the shoulder closer to the target.
8c. **Shared-action facing consistency.** Characters moving to the same destination or sharing one activity get the SAME facing treatment — all back view when the group heads for a background target, never one companion facing the camera while the others walk away. A look-down task (reading, examining a held object) cannot combine with `facing the camera`. Never describe front-only details (visor front, chest emblem, garment print) as visible on a back-view figure.
8d. **Age = apparent-age category.** Use the `Looks:` category from CHARACTER DETAILS verbatim (`school-age girl`, `teenager`, `middle-aged man`, `kindergartner`). Never use a numeric age in years and never substitute a different bucket.
8e. **Scale is anchored in frame.** `small in the distance`, `far off`, `tiny` render at foreground size. Measure the distant thing against something visible beside it: "the far ship spans less than the main character's hand", "the tower is a thumb's width beside the near roof".
8f. **True relative size.** A vessel, building or vehicle holds its real size against the figures near it — a person reaches about to a boat's rail, a doorway lintel or a wheel hub, never eye-level with a masthead, a rooftop or a chimney. When such an element shares the frame with a figure, name the ratio in the prose ("the mast rises five times her height").
8g. **An everyday prop carries a size anchor.** A prop on the ground, at a character's feet, in hand or on a surface gets a familiar-size term whenever its size matters — "a fist-sized apple", "a forearm-length crossbow", "a palm-sized cup". Those terms are for props only: a garment is sized by where it falls on the body ("knee-length", "falls to mid-back"), never by a prop calibration term.
8h. **One scale per page.** A page is at HUMAN scale, figures large enough to read an expression, or at OBJECT scale, a building, tower or vessel whole in frame — never both. A page that needs an expression shows the structure only in part.
9. **No age collectives.** Never write "the children", "the kids", "the boys", "the girls". Use names or group size ("all three", "the group").
10. **Prose construction.** Output one continuous paragraph followed by the metadata block — no headers, labels, bullet points, or section breaks. Open with the main subject and main action. Weave each character's physical description on first mention from CHARACTER DETAILS — age look, build, hair, eyes, distinctive features, clothing — as flowing language, not a labeled list. Clothing means the outfit itself; `standard`, `summer`, `winter` and `costumed` are metadata keys and never appear in prose. A character wears the outfit CHARACTER DETAILS gives them; never move a garment onto a character of the other sex, and never give a male character a garment made for girls or women, or the reverse. Name the TOP, the BOTTOM and the FOOTWEAR for every character on every page (a character whose outfit replaces the legs has no footwear — name the tail or fin instead, and never give them feet or shoes) — the image model draws only what the prose names, so a character given a hat and a shirt is drawn in a hat and a shirt and nothing else. Same for recurring Visual Bible elements: name them and interleave their identifying detail naturally.
10b. **Subject page.** When a beat stages the subject alone — a creature, an artifact, a place — describe it at full physical detail and name its size against a familiar object, since no character is there to give scale. Leave `characters[]` empty and name no character in the prose.
10c. **Traits are copied, not paraphrased.** Copy the `Hair:` value word for word — colour, type, length, styling, parting — and the `Looks:` bucket verbatim (rule 8d). A listed `Facial hair:` negation is part of the description: a clean-shaven character stays clean-shaven on every page, never stubbled, weathered or grizzled. Name `Glasses:` exactly as listed, untranslated, on every page; distinctive marks likewise. When the page's mood pulls toward grizzled, weathered or aged, suppress the pull — mood lives in the action, the light and the setting.
10d. **A garment the page removes.** When the page takes a normally-worn item off — coat off, cape down, hat in hand — the prose and `sceneIntent` both state the character is WITHOUT it and name where it now lies or is held, and the item gets an `interactions[]` entry for that place. The avatar reference wears the full outfit, so without that statement the item is painted on the character and on the ground at once.
11. **Framing pattern.** If the scene hint sets `framingPattern`, compose the prose as:
    - `solo-before`: close-up on the acting character, weapon drawn and aimed forward if applicable, gaze following the weapon's direction. No opponent or target in the frame.
    - `solo-after`: close-up on the character after the moment, no opponent, strong emotion.
    - `over-the-shoulder`: the acting character's back in one front corner (front-left or front-right), target small and soft in the opposite back corner (diagonal composition), 5–10% image height, weapon aim line runs diagonally from foreground to target.
    - `side`: profile view, character's body axis perpendicular to the camera; the action's target/destination visible on one side of the frame.
11b. **Camera-action consistency.** When the hint shows a character moving INTO, descending INTO, climbing INTO, or stepping THROUGH a space whose interior or far-side carries plot meaning (descending stairs, doorway into another room, tunnel, cave, threshold), expand the prose so the camera sits OUTSIDE that space and the character is seen from `back view` or in profile — never facing the camera with the interior behind them. Name the camera POV in the prose ("camera positioned outside the cellar door looking past the threshold; steep stairs descend into the dark beyond").
11c. **A close-up carries no environment.** A `close-up` brief describes only what that frame holds — the character, what their hands touch, the surface behind them. A long setting paragraph overrides the close-up tag and the wide shot is drawn instead; move the setting into `emptyScenePrompt`, or make the page a `medium` shot.
11d. **Aboard is not alongside.** When the camera is aboard a vessel or inside a structure, that vessel or structure is the ground the shot stands on — deck, railing, mast base, planking, walls, floor — and only those near surfaces are described. Its exterior (hull, figurehead, name, complete silhouette) never appears as an object in the frame, and no second copy of it stands in the background. The `emptyScenePrompt` for such a page is the view FROM that position — the planking, rail, mast base and the water or sky beyond, or the interior walls and floor — never the vessel or structure seen from outside. Set `aboard` to that element's id.
12b. **Never name what must be absent.** "no glow", "bare rail", "no other figures in the room", "does not wave" each paint the named thing into the picture. Leave it unwritten and describe what does occupy that space instead ("the rail runs smooth grey iron", "the far wall is plain plaster"). This covers props, people and the medium alike — you are not shown the art style, and a ban on glow, colour, text, reflections or weather can contradict the style the picture is drawn in.
12e. **A named prop is held, or it is the subject.** Props in a character's hands survive; props merely placed in the scene vanish. A plot-critical object goes into a named character's hands, or becomes the page's subject filling the foreground at full detail — never set dressing. At most two named props stand unheld on a page.
12f. **A real place in another era is described, not named.** Naming a real location pulls in how it looks today — modern seating, signage, fittings — into a historical or fantastical story. When the page's `era` or genre differs from the place as it stands, write the landmark's shape, materials and surroundings into the prose and leave its name out; its LOC id still goes in `objects[]`.

12c. **No lettering on any surface.** No words, names or letters anywhere in the image; the only exception is text the Visual Bible explicitly specifies for an element, painted exactly as specified. Applies to `sceneIntent` and every metadata field, not only the prose.

12g. **Build descriptions are visual, not nominal.** Describe an object by its shape and parts ("two-masted with square topsails"), never by a style name ("brigantine-style") — style words are lettering bait the sanitizer can't catch.

12d. **A held document is read, not displayed.** A map, letter, note or book in a character's hands stays angled toward the character reading it, its face foreshortened or turned away from the camera — the shot shows the characters reading, never the document presented flat to the viewer. This holds even when the page's beat is what the document says: stage the readers' reactions and pointing hands from the reader's side, and never make the document the frame-filling focal object.
 A prop being carried rather than read stays folded, rolled or pocketed.

**A two-sided prop.** A map, letter, note, chart, book, page, sign, board or plaque has two visual bible entries, one turned away and one face to camera. Name exactly one of the pair in `objects[]`: the turned-away entry unless the page's beat is what the document itself says, and never both on one page. When the beat is what the document says, name the face entry and write what it shows, including only the exact words its Visual Bible entry specifies. Otherwise the brief names the document as an object (material, size, edge wear, how it folds or rolls), leaves its face unlettered, and never stages it lying face-up or spread open across a surface.

12. **Never describe** weapons touching a character's body, characters held or bound by another character, or wounds or blood on a character.

---

## INPUT

**SCENE HINT (Page {PAGE_NUMBER} — your starting point):**
{DRAFT_SCENE_DESCRIPTION}

**Story Text (for emotional context — do not add characters from here that are not in the hint):**
{PAGE_CONTENT}

**CHARACTER DETAILS (exactly {CHARACTER_COUNT} characters — weave each one's appearance into the prose):**
{CHARACTER_DESCRIPTIONS}

{HEIGHT_ORDER}

{AVAILABLE_AVATARS}
{LOCKED_PERSPECTIVES}
{TEXT_ZONE_OVERRIDE}

**RECURRING VISUAL ELEMENTS (when one appears in the scene, name it and weave the detail this shot actually shows into the prose — the image model reads only your prose, never this list):**
{RECURRING_ELEMENTS}

---

## OUTPUT

One continuous paragraph of prose, then the metadata block. Start immediately with the main subject and main action — no preamble, headers, or bullets.

The prose must include, interleaved naturally:
- the main action of this page, opening the prose
- each named character described on first mention (age look, build, hair, eyes, distinctive features, and the full outfit: top, bottom, footwear — or the tail/fin in place of bottom and footwear when the outfit replaces the legs)
- where each character is positioned (left/right/center, foreground/midground/background), what they hold, how they face
- the setting: location, time of day, weather, lighting, mood
- any recurring visual element in frame, named with the identifying detail this shot shows, at the size and visibility the shot gives it
- a short note on shot type (close-up / medium / wide / aerial) if not obvious from the action

**Distance rule:** when characters must be far apart, describe the size difference against something in the frame rather than calling it small: "the main character fills the centre foreground while the secondary character is a figure in the far distance on the right, roughly a sixth their size."

**Example prose:**

A young child — a slim seven-year-old with short straight dark hair, brown eyes, wearing a striped hooded sweatshirt and dark athletic pants — kneels in the threshold of the open kitchen doorway and leans forward to reach a brown paper package lying flat on the pale parquet floor, bright midday sunlight pouring through the doorway and warming the cream-coloured cabinets behind him. In the soft midground, an adult — slim build, neck-length brown hair tied in a small ponytail, grey zip-up hoodie over a black t-shirt and dark jeans — stands beside a large wooden kitchen table with slightly worn edges in the centre of the room, and watches the discovery with a tilted head and curious half-smile. Wide shot framed by the open doorway, calm weekday atmosphere, the rest of the room quiet and tidy.

---

## Metadata block

After the prose, add exactly:

---METADATA---

Then a single-line JSON object:

```
{
  "sceneIntent": "CharacterA kneels in the open doorway and reaches for a package on the floor. CharacterB stands beside the table and watches, head tilted. Bright midday light through the doorway, calm weekday mood.",
  "characters": [
    {"name": "CharacterA", "clothing": "standard", "position": "left foreground", "depth": "foreground", "expression": "wide-eyed, mouth half open, brows lifted"},
    {"name": "CharacterB", "clothing": "costumed", "position": "center background", "perspective": "back view", "depth": "background", "expression": "calm, faint smile"}
  ],
  "shot": "medium",
  "aboard": "VEH001",
  "landmarkView": "exterior",
  "era": "present day",
  "objects": ["LOC001", "ART002"],
  "interactions": [
    {"character": "CharacterA", "object": "an item", "where": "carries the item in a pocket", "action": "standing", "storyRelevant": false, "priority": "low"},
    {"character": "CharacterB", "object": "wooden ladder", "where": "climbs the wooden ladder", "action": "climbing the ladder", "hands": true, "storyRelevant": true, "priority": "essential"}
  ],
<!-- TEXT_OVERLAY_BEGIN -->
  "textPosition": "top-left|top-right|bottom-left|bottom-right|top-full|bottom-full",
  "textZoneDescription": "5–15 words naming the saturated, high-contrast surface in that corner",
<!-- TEXT_OVERLAY_END -->
  "emptyScenePrompt": "..."
}
```

### Field rules

**`sceneIntent`** (required): 2-3 present-tense sentences naming the single moment the image depicts. Sentence 1: who does what to whom, where. Sentence 2: what characters hold or reach for, and the page's one gaze target (rule 6c) — never a second target, and never a character facing one person while gazing at another. Sentence 3: setting, lighting, mood. Name every character physically present. List the main and primary characters among them in `characters[]`; secondary characters stay in the prose and carry their CHR id in `objects[]`. One moment only — not cause plus effect.

**`characters[]`**
- One entry per main or primary character in the scene. Secondary characters never appear here — they have no avatar, so no clothing category can apply to them.
- Every character from CHARACTER DETAILS the prose describes as present gets an entry here, including a figure standing in the background or named only in passing. Never route one of them to `objects[]` instead.
- Always `name`, `clothing`, `position`.
- `clothing` is copied EXACTLY from the outline / AVAILABLE_AVATARS — `standard`, `winter`, `summer`, or `costumed:<costume>` as spelled there (one costume per character per story; the specific costume description is on the visual bible). Also describe the costume in the prose.
- `position` is a short phrase matching the prose ("right foreground", "center", "left background", "behind the desk") — the bbox detector reads it structurally.
- Only add `perspective` for back view or side profile.
- `era` is the story's time period in 2-4 words (medieval, 1920s, present day). It feeds the anachronism guard — omit only if the story has no defined era.
- `shot` is the page's camera framing: `close-up`, `medium`, or `wide`. Decide it once — the scene prose, the emptyScenePrompt, and the metadata all use this same framing. Omitting it makes the empty scene default to a wide shot.
- `landmarkView` is how the page sees a real landmark, when one is cited: `exterior` (from outside), `distant` (skyline or backdrop), `close` (a detail of the structure), `interior` (inside it), `view-from` (looking out from it, structure not visible), or `underwater` / `none` when no photograph of the place could show this scene. It selects the reference photo; a view with no matching photo attaches none and your prose carries the place.
- A `close-up` frame ends at the waist. Poses and interactions stay above it — no kneeling, crouching, sitting, stepping, or feet-on-ground contact, and nothing placed behind the character. The outfit list still names all garments including footwear. A beat that needs below-waist action is a `medium` shot, not a close-up.
- Only add `depth` when the character is in the background (default is foreground/midground).

**`objects[]`**
- String array of Visual Bible IDs only (`LOC001`, `ART002.2`, `CHR001`).
- Secondary characters visible in the scene go here by CHR id. Write their full appearance and their COMPLETE outfit into the prose on every page they appear on, copied from their Visual Bible entry — that entry is what their reference image is drawn from, so prose that disagrees with it fights the reference.
- Include every recurring visual element visible in the scene.
- Real-landmark LOCs are cited by their bare id (`LOC003`); the page's `landmarkView` decides which reference photo is attached.
- Invented LOCs (`isRealLandmark: false`) use the bare id `LOC001`, or the dotted form `LOC001.2` when the Visual Bible gives that location `vantages[]` — cite the vantage whose `pages` list contains this page.

**`depth` and `expression`** — required for every character. When the outline context already gives them, carry those values through unchanged; the outline is the source of truth and re-deciding them breaks continuity with the other pages. Only when the context does not supply them, write them yourself from the scene: `depth` is `foreground`, `midground` or `background`; `expression` is 3-8 words naming brows, eyes and mouth ("brows pulled tight, eyes narrowed, jaw set"). Never write a bare mood word — "happy" and "sad" cannot be drawn. An expression is one instant: never a transition ("changing from X to Y") — pick the single expression the picture shows. On a `wide` shot, faces are too small to read — describe body language in the prose instead of facial detail.

**Characters are solid.** Never `translucent`, `transparent`, `ghostly`, `see-through`, `spectral` — a character is always fully opaque, whatever the story calls them.

**`interactions[]`** — visible character-to-object and character-to-character contacts. The image generator reads all entries; the evaluator only enforces entries marked `storyRelevant: true`.

Mark `storyRelevant: true` only when the contact is the page's plot beat, identity marker, or dramatic action — typically 0–2 per page. Costume texture, pose detail, and secondary contact are `storyRelevant: false`. In doubt → `false`.

Omit entirely (do not include with `false`): pure ground/floor/path contact, leaning on walls, sitting on generic chairs.

Field shape: `{character, object, where, action, hands, storyRelevant, priority}`. `where` is a short sentence naming the object as the contact action ("holds the parchment map", "hands the letter to the messenger"). No body-part positioning, no pose detail: write "pushes the cart", not "presses both palms flat against the cart's side" — the visible grip belongs in the prose (rule 7b). Name which hand, how many hands, or where on the object they land only when the story turns on it.



`hands`: `true` when the character's hands are on the object. One object takes one pair of hands: when a second character reaches for the same object, one holds it out and the other reaches — their hands never share the grip.

`action`: required on every row, in English whatever the story language. Name the physical activity — the verb and what it acts on ("pushing the cart", "climbing the ladder") — never the purpose it serves. Rows performing the same activity carry the same label, however many characters that is. Use `"watching"` for anyone who only looks on and `"standing"` for anyone merely present; neither counts as the page action.

**Shared objects.** When several characters use ONE object at once, or act on ONE object together (carry the same basket, hold the same banner, pull the same rope, push the same cart), emit ONE entry: join the names with ` + ` in `character` and describe the shared action in the one `where` sentence. Separate entries with the same object render as duplicate objects. Characters each using their OWN identical object keep separate entries.

`priority`: `"essential"` for the page's central action (held prop, traced line, key gesture), `"normal"` for supporting action, `"low"` for incidental gaze/pose. Optional — defaults to `"normal"`. The image generator orders EXACT POSES with essentials first.

**Coverage:** every foreground/midground character should appear in `interactions[]` exactly once. If a character has no real action, give them a `priority: "low"` entry (e.g. `{character, object: "the campfire", where: "watches the campfire", storyRelevant: false, priority: "low"}`) — without it the image model defaults to a camera-facing portrait. Background characters exempt.

A vehicle under way MUST have exactly one character actively operating it — declare that operation as an interaction for that character, `essential` when the operating is the page's beat and `low` otherwise.

Cap: 1–6 entries, 0–2 with `storyRelevant: true`, and one `action` label per page beyond `"watching"` and `"standing"`.

Every entry must also appear in the prose as the same physical contact.

Back-view rule: for characters with `perspective: back view`, every held object must be visible from behind — peeking over shoulder, visible under arm, strapped to back, dangling at the side.

Pointing rule: no character points. Show which thing has their attention with gaze, body orientation, or an object held up — never an extended arm or index finger, in the prose or in an `action` label.

Counting rule: a group of like things is staged as a cluster, a row, a few or several — never with an exact number above two, in the prose, `sceneIntent` or `emptyScenePrompt`. A group that recurs across pages holds the same size impression, role and placement, never a count.

Distance rule: when the verb implies action across a gap ('shoots', 'aims at', 'throws at', 'calls to', 'gestures across'), `where` must include an explicit separation phrase ('across the room', 'a few paces apart', 'across the square', 'down the valley'). For these scenes `shot` MUST be `ultra-wide` so both endpoints fit visibly, and the prose must place each figure at the correct end of the frame.

<!-- TEXT_OVERLAY_BEGIN -->
**`textPosition` / `textZoneDescription`** (story pages only — when a TEXT ZONE POSITION FIXES block appears above, `textPosition` matches the position it names)
- `textPosition`: the corner reserved for the white story-text overlay. Even pages use a right corner (`top-right` / `bottom-right`), odd pages a left corner. `top-full` and `bottom-full` span both sides and are valid on any page.
- The surface behind the text naturally occupies that band in the scene's geometry: `top-*` takes sky, ceiling, cave roof, upper foliage, cliff face or calm distance; `bottom-*` takes ground, floor, water, cobblestones, grass, sand or low foliage. A vertical surface (wall, facade, door) fits `top-*` only when the camera puts it across the top of the frame — narrow alley, interior, against-the-wall framing.
- `textZoneDescription`: 5–15 words naming the saturated, high-contrast surface in that corner (e.g. "deep evening sky in rich indigo", "dark green pine foliage"). Describe what's there, not what it's for — never "rectangle", "blank area", "for text".
- `emptyScenePrompt` names that same surface filling that same band, as part of the scene.
<!-- TEXT_OVERLAY_END -->

**`aboard`**
- The Visual Bible id of the vehicle or structure the camera stands on or inside for this page (`VEH001`, `LOC004`). Omit it when the camera is outside the element looking at it, or when no such element is in the shot.
- It tells the plate builder to render the surfaces around the camera instead of the element's full exterior, so a page set on board does not gain a second copy of the vessel in its background.

**`emptyScenePrompt`**
- 2-3 sentences describing only the background.
- Same camera framing as the scene itself: a close-up scene gets a close-up stage (the wall, the doorway, the table corner), a wide scene gets the wide setting. Never an establishing wide shot for a close-up scene.
- The plate mirrors the scene's composition geometry instead of inventing its own: the same direction for any path, river, road, corridor, shoreline or horizon; the vanishing point or opening named as a frame position (upper-right, lower-centre); the same dominant diagonals; a lighting direction that matches the page's time and weather; and the same corners left uncluttered where the figures will be composited, written as the scene's own ground continuing through them. Read side by side, the prose and the plate describe one geometry twice, with the named characters absent from the plate. When the camera is aboard a vessel or inside a structure (rule 11d), it is that view's geometry that is mirrored — the rail's line, the deck's recession, the direction the light enters.
- The plate describes only what the camera sees from its vantage — never the exterior of the vehicle or structure the camera stands on.
- The scene geography must give every standing character footing at their declared position — a bank, path, floor, deck, or walkway at that depth, never open water or air under a standing figure. A beat that puts a figure where nothing standable exists moves the figure or the camera.
- When the page's beat is an obstacle, stage it blocking the route in frame and give one character an action against it — pushing, pointing, trying, being stopped. An obstacle nobody engages reads as scenery.
- A crowd the action needs — pushing through it, hiding in it, lost in it — is a required scene element with mass and closeness, never background atmosphere.
- No partial immersion: a character is either on standable ground or fully swimming. Wading, ankle-deep, and knee-deep poses render as standing on the water surface — restage them at the water's edge or as swimming.
- A real landmark is used as it is: characters interact with features the landmark actually has (its railing, its open side, its steps) — never with an invented gap, opening, ledge, or platform.
- No named characters, no tracked animals, no carried or handheld artifacts (swords, books, apples, crossbows — those get composited later with the character).
- Scene-anchor artifacts are allowed and, when the page text centers on them, must be described as the focal point — "prominent at the centre, dominating the foreground composition, readable and sharply defined," not "a pole stands somewhere." This covers stationary anchors (pole-with-banner, throne, monument, pedestal, altar, signpost) AND the vehicle or vessel when the scene takes place in or on one (boat, raft, wagon, carriage, cart, train compartment, sled). Name the key visual marker (the hat on top, the inscription, the crown, the boat's stern) so the model doesn't substitute a generic building.
- Never write hypothetical references like "where children would stand" — image models render those literally.
- When the scene goes down into a cellar, basement, cave, or other below-ground space, write the backdrop from inside at the top of the steps looking down — the stairs recede downward and away into the space below. Never put the camera outside looking at an exterior door, and never show steps rising up to a door.
- Use "open foreground", "uncluttered centre". Named characters will be composited later, so framing must leave enough physical space for them.
- When the scene's prose calls for a populated setting (crowd, villagers, market-goers, soldiers, guards, onlookers, distant workers, busy street), describe those anonymous extras here too — density, rough distance, silhouette only — so the backdrop already feels populated. Name a mix of several garment colours and more than one garment shape so the crowd reads as varied, not a uniform robed mass. Keep extras away from the spot reserved for named characters.

**Landmark accuracy:** Read all photo variant descriptions before writing. Use the most common structural features across variants. If characters are inside an enclosed landmark, describe the scene from inside.

**Prop placement vs landmark.** When a separate prop shares a depth band with the landmark, position the prop relative to the landmark (in front of, beside, between camera and landmark) — not by an unanchored frame slot.

---

**Self-check before returning:** the prose and the metadata describe the same single moment; every character the prose places in the scene has a `characters[]` entry and the same outfit in both; one gaze target, and a character gazing at a background-depth target is `back view`, never facing the camera; every demanded grip is written as a visible pose.

The self-check refers to each character by name only. Never re-describe a character inside it, and never state an age, a build or an outfit there — a description written in the check replaces the one in CHARACTER DETAILS and is drawn. Nothing from the check appears in the output.

All output in English. Prose and all metadata fields must be in English.
