Detect bounding boxes for the following issue in this storybook illustration:

ISSUE: {ISSUE_DESCRIPTION}

For each affected element, return BOTH:
- face_box: bounding box around the character's FACE only [y_min, x_min, y_max, x_max]
- body_box: bounding box around the character's FULL BODY [y_min, x_min, y_max, x_max]

COORDINATE SYSTEM:
- Values are 0-1000 (Gemini native format)
- Origin is top-left corner
- Format: [y_min, x_min, y_max, x_max]

DETECTION GUIDELINES:
1. face_box should tightly enclose the face (forehead to chin, ear to ear)
2. body_box should enclose the entire visible body (head to feet, or as much as visible)
3. If the issue affects an OBJECT (not a character), return only body_box around the object
4. If a face is not visible (back view), set face_box to null

Return ONLY valid JSON with no other text:
{
  "detections": [
    {
      "label": "description of affected element (e.g., 'boy in blue hoodie on the left')",
      "face_box": [y_min, x_min, y_max, x_max],
      "body_box": [y_min, x_min, y_max, x_max]
    }
  ]
}

EXAMPLES:

For a hand issue on a character:
{
  "detections": [
    {
      "label": "girl with red dress holding sword",
      "face_box": [120, 340, 220, 440],
      "body_box": [100, 280, 650, 520]
    }
  ]
}

For an object issue (no face):
{
  "detections": [
    {
      "label": "broken sword in foreground",
      "face_box": null,
      "body_box": [400, 200, 600, 400]
    }
  ]
}

For a face identity issue:
{
  "detections": [
    {
      "label": "boy with wrong hair color in center",
      "face_box": [150, 420, 280, 580],
      "body_box": [130, 350, 700, 650]
    }
  ]
}
