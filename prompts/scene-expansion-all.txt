#! ALL-PAGES SCENE EXPANSION — beats pipeline only (server/lib/beatsPipeline.js,
#! via buildSceneExpansionAllPrompt). ONE call writes the VISUAL BIBLE and every
#! page's brief — the book's covers included, as pages with their own plan lines
#! (server/lib/coverBeats.js, 2026-09-24) — with all the other pages in view, so
#! location, time of day, clothing and composition cannot drift between
#! neighbouring pages — and so no page can cite an element nobody declared.
#!
#! The bible used to be authored one stage earlier (story-bible-from-beats.txt),
#! which left it guessing which page used which element from plan-line prose.
#! That file now writes the CLOTHING REQUIREMENTS only; everything else is here.
#!
#! scene-expansion.txt is the PER-PAGE variant and is still the unified
#! pipeline's fallback (server.js). The two share their PAGE rule set; the bible
#! section has no per-page counterpart. Do not merge either file.
#!
#! Output is parsed by extractBibleSections(raw, AD_BIBLE_MARKERS) (the two
#! leading sections) and parseRefinedText(raw, expectedPages, 'SCENES') (the
#! pages) — the same page parser the scene review's response goes through.

**ROLE:** You write illustration instructions for an image generation model. Each page of a children's book gets one scene brief. The output feeds directly into Grok/Gemini — not a human reader. You write ALL {PAGE_COUNT} pages in this one response. Each page arrives as its PLAN line (shot — who is in frame — the instant the picture shows — what is true after); stage that instant.

---

## Per-page rules

1. **Simplify, don't elaborate.** The page's PLAN line is already good. Flesh it into prose; don't add complexity. If the plan line says 3 things, the output says 3 things. Same characters, same composition — expand slightly, don't reinvent. The SCENE prose stays under 450 words. Every element in the brief traces to the plan line or to a Visual Bible id the plan line implies; choose among and simplify what the plan line names, never add to it. Every physical fact the plan line states about the world — tracks in the dust, a torn rope, a dry crack — is in the brief. The page's action and its effect — a jet of water, a splash, a break, smoke, a fall — belong with the characters, in the prose and `sceneIntent`; the vantage's plate is the place without them.
1b. **A setting carries its own detail.** When the plan line's location is a fantastical or strongly-themed world (a magical realm, undersea, outer space, an enchanted forest, an ancient ruin, and the like), or a real place the story's premise has transformed into one, fill the SETTING with a lot of that world's characteristic environmental elements even when only the bare place is named. An everyday interior — a kitchen, a living room, a bedroom, a workshop, a classroom, a shop — does the same with its lived-in detail: worn surfaces, crockery and cookware, appliances and fittings, textiles, things standing on shelves and hanging on walls. An unfurnished box room is a fault. This describes the location, it is not added plot: keep it to the environment and background, never add characters and never change the plan line's action. Such furnishing stays ambient — unnamed and uncounted, outside rule 12e, which governs the page's named plot-critical props.
2. **Plain concrete words.** Prefer `on`, `in`, `holds`, `sits on`, `touches` over `hovers`, `cradled`, `perched upon`, `rests gently against`. Image models render fancy words literally — `hovers above` becomes a visible gap. Skip aesthetic buzzwords: `dramatic`, `ethereal`, `haunting`, `cinematic`, `breathtaking`, `stunning` — they carry no visual information.
3. **Only characters from the page's plan line.** The plan line defines who is visible on that page. Do not import characters from other pages, and never drop one they list. {PLAN_LINE_CAST} If the plan line describes unnamed figures (soldiers, villagers, crowd), weave them into the prose with the same size impression, role and placement — they anchor the story moment and must appear. Never give such a group an exact number above three (see the Counting rule); a group that appears again on a later page keeps that same size impression, role and placement. Unnamed figures get an appearance of their own: varied hair colours and lengths, more than one garment shape, several garment colours, and a mix of heights and ages across the group. `anonymous`, `generic` and `unnamed` name a role, never a look — a group given no appearance is drawn as copies of the nearest described character. A group in matching uniform, kit or livery keeps the shared garments and varies hair, height and age instead.
4. **Staging a group.** When more than three characters share the frame, keep the whole group together at one depth in a wider shot, or show them from behind as they move away — never five detailed close foreground faces. Push a figure into the background only when the plan line itself separates them (left behind, watching from afar, arriving in the distance) — never as a crowd-management default.
5. **One moment, one focal point.** Stage the given instant as written, without hidden limbs. One main action draws the eye, drawn at its peak of motion — mid-leap, mid-swing, mid-throw — not the static pose that follows.
5b. **One centre of action.** Never two vehicles or vessels in frame each with a crew doing its own thing — one carries the moment, and a second is distant, partly cut by the frame edge, or empty.
5c. **One instant, no history.** {ONE_INSTANT}
5d. **The plan line's verb and shot are the default.** Follow the plan line's action verb and shot size unless they cannot be staged. A plan line naming an in-progress movement — wading, climbing, swimming toward something — is staged mid-effort in that element, never as the arrived or dry state (the no-partial-immersion rule restages the pose, not the moment). A close-up is not widened into an establishing shot without need.
5e. **The plan line's stakes set the faces and the effort.** A plan line that carries threat, hurry or strain is drawn under it: every `expression` names the strain in brows, eyes and mouth, bodies brace, lean or hurry, and nothing in the frame rests. A calm plan line is drawn calm. Mood is shown through faces, bodies, light and weather — never named.
5g. **Content, not rendering.** {NO_LENS}
6. **Characters are active.** Every character does something specific — reaching, walking, gripping, gazing at an object or another character. Never standing-and-smiling. Eyes look at what they interact with, not at the camera.
6b. **A group acts together.** When several characters share the frame, show them mid-action on one shared task, turned toward it and each other — never lined up abreast facing the viewer.
6c. **One gaze target per page.** {GAZE_TARGET}
6d. **Eyes are open.** {EYES_OPEN}
7. **Physically possible.** Two hands = max two items. If a character holds a book and a flashlight and waves — that's three hands. Fix it. One hand-duty per figure: a second grip written onto the same figure grows a third hand or fuses the fingers of the first.
7b. **Contact is written as the pose, in the prose.** `hand on the hasp`, `grips the rung`, `touches the spoke` render as a hand hovering near the object. In the prose name what the fingers wrap, which way the weight leans, what the body presses against: "fingers curled right around the rung, arms taking her weight, chest against the ladder". The `interactions[]` `where` field stays terse — the visible grip lives in the prose.
7c. **Force is a shape.** `taut`, `strained`, `braced`, `pulling hard` all render at rest. Name the shape the force makes instead: "the rope is a straight line from the post to her hands", "the sail curves out toward the bow".
7d. **An elevated figure names its support.** A character off the ground gets the support written into the pose — "feet on the third rung, both hands on the fifth", "seated on the wall's top edge, heels against the stone". Without it the figure is drawn floating, or standing on the ground.
7e. **Two body actions that cannot co-occur.** A pose that holds part of the body against a surface rules out an action needing that same part elsewhere — head resting on a surface and eye contact with someone off to the side cannot both be drawn. Keep the one the page's declared interaction names and leave the other out.
7f. **A held prop keeps a hand.** Never write an action that occupies both hands while an essential prop is declared as held — one hand stays on the prop and the other does the action, or the action changes to one the free hand can do. When the plan line's verb is movement, the pose carries that movement — a stride mid-step with the weight forward, not a figure standing still performing the gesture.
7g. **Tip on the target, receiver clear.** When a tool acts on one thing and a second thing receives the result, the prose says where the tip touches the target, and the receiver stands clear of the tool — never between the actor and the target. The result appears where the tool touches, not where it ends up.
8. **Honor perspectives.** `depth: background` → small and distant. `depth` is distance from the camera and never states a size: a stated size holds at every depth, and a small thing in the foreground stays small. {OTS_NEAR_FIGURE} The subject they watch goes in midground or background (smaller, distant). Never invert — an OTS character in the background, or the watched subject in the foreground, reads as a different shot entirely. Describe a back-view character as seen from behind: shoulders, head, hips, and both feet all turned away; heels visible, toes pointing away. `back view` (or `back view, glancing over the shoulder`) fits a character looking or moving toward something ahead of them — a distant target, a doorway, a view; it is allowed on any page and recommended on pages holding several people. Guidance, not a requirement. `back view` is only valid when the target sits deeper in the frame than the character; a character moving toward someone in the foreground faces the camera. Thin a crowd by depth and size, never by turning characters away from the person they are approaching.
8b. **Body-facing vocabulary (discrete only).** Pick one of: `facing the camera`, `facing left`, `facing right`, `back view`. Never write fractional turns ("three-quarters right", "two-thirds toward", "45-degree", "angled three-quarters", "three-quarter back view", "three-quarter rear view"). "rear view" means write `back view`. Image and repair models can't resolve fractional angles. For a character glancing back at someone behind them, write `back view, glancing over the shoulder toward [target]` and pick the shoulder closer to the target.
8i. **A back-view character with a meaningful expression turns the head.** Write `back view, head turned toward [left|right] shoulder`, describing only what that turn reveals — a cheek, one eye, a brow — and give every reacting figure in a shared reaction the same treatment.
8c. **Shared-action facing consistency.** Characters moving to the same destination or sharing one activity get the SAME facing treatment — all back view when the group heads for a background target, never one companion facing the camera while the others walk away. A character's facing is written inside that character's own clause ("…, seen from behind, walking away from the viewer, face not visible"); one collective sentence for the group does not bind. A look-down task (reading, examining a held object) cannot combine with `facing the camera`. Never describe front-only details (visor front, chest emblem, garment print) as visible on a back-view figure.
8j. **Eyes are a field.** {LOOKS_AT_FIELD}
8k. **A face is a field.** {EXPRESSION_FIELD}
8d. **Age = apparent-age category.** Use the `Looks:` category from CHARACTER DETAILS verbatim (`school-age girl`, `teenager`, `middle-aged man`, `kindergartner`). Never use a numeric age in years and never substitute a different bucket.
8e. **Scale is anchored in frame.** `small in the distance`, `far off`, `tiny` render at foreground size. Measure the distant thing against something visible beside it: "the far ship spans less than the main character's hand", "the tower is a thumb's width beside the near roof".
8f. **True relative size.** {TRUE_RELATIVE_SIZE}
8g. **An everyday prop carries a size anchor.** A prop on the ground, at a character's feet, in hand or on a surface gets a familiar-size term whenever its size matters — "a fist-sized apple", "a forearm-length crossbow", "a palm-sized cup". Those terms are for props only: a garment is sized by where it falls on the body ("knee-length", "falls to mid-back"), never by a prop calibration term.
8h. **One scale per page.** A page is at HUMAN scale, figures large enough to read an expression, or at OBJECT scale, a building, tower or vessel whole in frame — never both. A page that needs an expression shows the structure only in part.
8l. **A creature in frame has a written face.** {CREATURE_FACE} Write it in the terms of the creature-tone block below.
{CREATURE_TONE}
9. **No age collectives.** Never write "the children", "the kids", "the boys", "the girls". Use names or group size ("all three", "the group").
10. **Prose construction.** Each page is one continuous paragraph followed by its metadata block — no headers, labels, bullet points, or section breaks inside a page. Open with the main subject and main action. Weave each character's physical description on first mention from CHARACTER DETAILS — age look, build, hair, eyes, distinctive features, clothing — as flowing language, not a labeled list. Clothing means the outfit itself; `standard`, `summer`, `winter` and `costumed` are metadata keys and never appear in prose. A character wears the outfit CHARACTER DETAILS gives them; never move a garment onto a character of the other sex, and never give a male character a garment made for girls or women, or the reverse. Name the TOP, the BOTTOM and the FOOTWEAR for every character on every page (a character whose outfit replaces the legs has no footwear — name the tail or fin instead, and never give them feet or shoes) — the image model draws only what the prose names, so a character given a hat and a shirt is drawn in a hat and a shirt and nothing else. Every page repeats the full physical description: each brief is read on its own by the image model. Same for recurring Visual Bible elements: name them and interleave their identifying detail naturally.
10b. **Subject page.** When a plan line stages the subject alone — a creature, an artifact, a place — describe it at full physical detail and name its size against a familiar object, since no character is there to give scale. Leave `characters[]` empty and name no character in the prose.
10c. **Traits are copied, not paraphrased.** Copy the `Hair:` value word for word — colour, type, length, styling, parting — and the `Looks:` bucket verbatim (rule 8d). A listed `Facial hair:` negation is part of the description: a clean-shaven character stays clean-shaven on every page, never stubbled, weathered or grizzled. Name `Glasses:` exactly as listed, untranslated, on every page; distinctive marks likewise. When the page's mood pulls toward grizzled, weathered or aged, suppress the pull — mood lives in the action, the light and the setting.
10d. **A garment the page removes.** {GARMENT_REMOVED}

10e. **A worn item on someone else.** {WORN_ON_OTHER}
11. **One figure acting on another across a gap.** {GAP_ACTION_FRAMING}
11b. **Camera-action consistency.** When a character moves INTO, descends INTO, climbs INTO, or steps THROUGH a space whose interior or far-side carries plot meaning (descending stairs, doorway into another room, tunnel, cave, threshold), expand the prose so the camera sits OUTSIDE that space and the character is seen in profile or over the shoulder, the interior visible past them — never facing the camera with the interior behind them, and never straight behind with their body hiding it. Name the camera POV in the prose ("camera positioned outside the cellar door looking past the threshold; steep stairs descend into the dark beyond").
11c. **A close-up carries no environment.** A `close-up` brief describes only what that frame holds — the character, what their hands touch, the surface behind them. A long setting paragraph overrides the close-up tag and the wide shot is drawn instead; leave the setting to the vantage's plate. {CLOSEUP_KEPT}
11d. **Aboard is not alongside.** When the camera is aboard a vessel or inside a structure, that vessel or structure is the ground the shot stands on — deck, railing, mast base, planking, walls, floor — and only those near surfaces are described. Its exterior (hull, figurehead, name, complete silhouette) never appears as an object in the frame, and no second copy of it stands in the background. Such a page cites a vantage whose plate is the view FROM that position — the planking, rail, mast base and the water or sky beyond, or the interior walls and floor — never the vessel or structure seen from outside. Set `aboard` to that element's id.
11e. **Inside is not outside.** A character inside, below or at the bottom of something the camera sees from outside — a shaft, a pit, a hollow trunk, a tank — is a fault. Three things change together: `shot` looks into it (high angle down into it, or from inside looking up), the cited vantage's plate is the interior the character occupies (its floor, its walls rising, the opening above), and the character's `depth` is `foreground` or `midground` — never `background` inside their own container. `depth` is distance from the camera, never elevation.
11f. **One level per frame.** Two named figures on different levels — one on a deck, floor, bank, wall or roof, the other on the water, ground or stair below — cannot be drawn facing each other at equal size across the edge: the renderer puts every figure on one plane and raises the lower surface to do it, so a boat alongside ends up sitting on the deck. Stage such a page from one level. Camera on the upper level looking down over the edge: the lower figure is small and far below, `depth: background`, and the surface they are on stays visibly below the edge. Or camera on the lower level looking up: the upper figure stands at the edge above, `depth: background`. Never both at full size on one plane. Figures on one surface — a deck, a quay level with it, one flight of stairs — are one level. Figures on one small shared surface — a boat, a raft, a cart, a wagon, a sled — are one level and carry the same `depth`, whatever their pose.
12. **Never describe** weapons touching a character's body, characters held or bound by another character, or wounds or blood on a character.
12b. **Never name what must be absent.** {NEVER_NAME_ABSENT}
12e. **A named prop is held, or it is the subject.** Props in a character's hands survive; props merely placed in the scene vanish. A plot-critical object goes into a named character's hands, or becomes the page's subject filling the foreground at full detail — never set dressing. At most two named props stand unheld on a page. This rule governs named plot-critical props only; the ambient furnishing of rule 1b is unnamed, uncounted, and not covered by it.
12f. **A real place in another era is described, not named.** Naming a real location pulls in how it looks today — modern seating, signage, fittings — into a historical or fantastical story. When the page's `era` or genre differs from the place as it stands, write the landmark's shape, materials and surroundings into the prose and leave its name out; its LOC id still goes in `objects[]`.

12c. **No lettering on any surface.** No words, names or letters anywhere in the image; the only exception is text the Visual Bible explicitly specifies for an element, painted exactly as specified. Applies to `sceneIntent` and every metadata field, not only the prose.

12g. **Build descriptions are visual, not nominal.** Describe an object by its shape and parts ("two-masted with square topsails"), never by a style name ("brigantine-style") — style words are lettering bait the sanitizer can't catch.

12h. **An object out of sight.** {CONCEALED_OBJECT}

12k. **An object several characters touch.** {REACHABLE_CONTACT}

12d. **A held document is read, not displayed.** A map, letter, note or book in a character's hands stays angled toward the character reading it, its face foreshortened or turned away from the camera — the shot shows the characters reading, never the document presented flat to the viewer. This holds even when the page's plan line is what the document says: stage the readers' reactions and pointing hands from the reader's side, and never make the document the frame-filling focal object.
 A prop being carried rather than read stays folded, rolled or pocketed.

**A two-sided prop.** A map, letter, note, chart, book, page, sign, board or plaque is one visual bible entry with a state per side, turned away and face to camera. {MULTI_PICTURE_PROP} Name exactly one of its state ids in `objects[]`: the turned-away state unless the page's plan line is what the document itself says, and never two states of it on one page. When the plan line is what the document says, name the face state and write what it shows, including only the exact words its Visual Bible entry specifies. Otherwise the brief names the document as an object (material, size, edge wear, how it folds or rolls), leaves its face unlettered, and never stages it lying face-up or spread open across a surface.

---

## Cross-page rules (why this is one call)

C1. **Location continuity.** Consecutive pages stay in the same place unless a plan line states the move. Never jump from an exterior to an interior — or between two unrelated locations — on a page whose plan line says nothing about travelling or entering. If a plan line implies a move, the arriving page's prose must name the transition ("steps in through the farmhouse door"). Reuse the same Visual Bible location id on every page set there.
C2. **Monotonic time of day.** Time moves forward across the book, never backwards. Pick the lighting of page 1, then hold or advance it: morning → midday → afternoon → dusk → night. Two consecutive pages in the same scene share the same light. Only a plan line that states a jump (next morning, days later) resets it. Weather holds the same across every outdoor page unless a plan line changes it; an indoor page has no weather, and shows it only through a window.
C3. **Clothing consistency.** A character wears the same clothing category on every page unless a plan line changes it (weather, costume, sleep). The category must come from AVAILABLE CLOTHING PER CHARACTER. Once a character changes, later pages keep the new one.
C4. **No two consecutive pages with near-identical composition.** Neighbouring pages must differ in at least one of: camera distance ({DISTANCE_SHOTS}), where the camera stands ({SHOT_POSITIONS}), or character grouping. Same location on two pages is fine; the same shot twice is not.
C5. **Recurring elements stay identical.** A location, animal, vehicle or artifact keeps its description across pages — same colours, same shape, same markings. Take them from the Visual Bible you write below and do not re-invent details per page.
C6. **Visual arc.** Framing builds with the plot: the widest establishing shot near the start, the tightest and most intense framing on the page whose plan line carries the climax.
C7. **An element left at a place stays there.** When a plan line sets an element down at a location — put down, dropped, hidden, left behind — the next two or three pages set at that same location still hold it: cite its id in `objects[]` and stage it in the prose where it lies, at the visibility the shot gives it — the place, never a character the plan line does not put in this frame. It counts against the page's element budget like any other. The window ends at the first plan line that moves, takes or breaks it, at the first page set somewhere else, or three pages on. A page that takes in the whole place from a distance, or points its camera away from where the element lies, inherits nothing — and a plan line's closing "what is true after" clause is not a placement: an element named only there sits wherever the story last put it.
C8. **One season for the book.** The season is **{SEASON}** on every page: foliage, ground cover, sky and daylight colour are that season's, and stay identical from page to page for the same place. No plan line changes it, and an interior shows it only through a window.

---

## INPUT

# THE STORY (for judgment — the plan lines divide it into pictures; stage only what this page's plan line carries)

{FINAL_ARC}

**ALL LOCKED PLAN LINES ({PAGE_COUNT} pages — write one brief per page, in order):**
{ALL_PLAN_LINES}

**CHARACTER DETAILS (exactly {CHARACTER_COUNT} characters — weave each one's appearance into the prose):**
{CHARACTER_DESCRIPTIONS}

{HEIGHT_ORDER}

**AVAILABLE CLOTHING PER CHARACTER (the `clothing` value must come from this list; each outfit is what that category's avatar wears):**
{AVAILABLE_AVATARS}

{AVAILABLE_LANDMARKS_SECTION}

---

## Visual Bible — you write it, before the pages

The Visual Bible lists everything that must look the same across illustrations. You write it first, then stage each page citing the ids you declared. A page's `objects[]` may only name an id that stands in the Visual Bible you emitted.

Work from the plan lines: an element a plan line only implies still needs an entry, because a page can reference only what is listed.

- All locations, all objects, all vehicles, all animals, all worn costumes and garments.
- Secondary characters you invented only. Never {CHARACTER_NAMES} — they have reference photos.
- Every secondary-character entry carries `age` as a NUMBER of years (`8`, `34`), never prose, and opens `build` with the character's sex ("a boy, slightly tall for his age", "a woman, broad-shouldered"). Jaw and facial-hair notes carry neither.
{CHILD_AGE_BAND}
- An entry shaped like a person — an invented figure, a small winged being, a doll, a figurine, a statue, a puppet — states its HAIR in `description`: colour, length and how it is worn, or plainly that it has none. Such an entry reaches the image model as its description and nothing else, and a head described only by its eyes is drawn bald. State the head the same way a face is stated: hair, then whatever else the figure's head carries.
- Every named or story-significant vehicle, vessel or structure gets its own `vehicles` entry — the antagonist's and the rival's included, never folded into `locations`. When two of them can share one frame, both need entries.
- Every Visual Bible element carries `scaleClass`, and it is the only place an element’s size is stated: {SCALE_CLASS_SPEC} An adjective is not a band — massive, tiny, huge name none. No entry gives a size of its own, in words or in units.
- `generic: true` only when any instance of the thing would serve: swap it between two pages and no reader could tell. If the story ever refers back to THIS one — it is found, lost, given, marked, recognised, or a character owns it — it is not generic. A generic element gets no entry beyond that flag, no id and no reference image: it is named in the page's scene prose like any other physical fact and never appears in `objects[]`. Omit the field on every other element.
- {REQUIRED_TEXT_AUTHORING}
- A name the story gives an element lives in `properName` and nowhere else — never in `label`, `name`, `description` or the page prose. One `properName` belongs to one id: two entries never share a name. An object that transforms later in the story and the creature it becomes are separate ids, and only one of them carries the name.
- `label` is the one English name every prompt uses for an element: one to four words ending in the noun, unique in this bible, never a bare category, never a proper noun, never a word from the story language.
- English `name` and `description` for artifacts, locations, vehicles and clothing, even for a non-English story; those strings are copied into English image prompts. Characters and animals keep their given names.
- Every item in exactly one category. A worn garment belongs to `clothing` only, never also to `artifacts`. Exception: a prop the story turns into costume (a hat found in a chest, a cloak handed over) is ONE `artifacts` entry with `wornAs`: `"<CharacterName>.<slot>"`. Every garment a listed character wears is one their outfits in AVAILABLE CLOTHING PER CHARACTER name, and its entry's `description` repeats the outfit's words for it. Never dress a listed character in a garment their outfits do not name, and never reword one; a page without a garment says so in a `wornItems` row. The slot is one of `headwear`, `top`, `bottom`, `footwear`, `belt/waist`, `outer layer`, `accessories` and never another word; neckwear, handwear and eyewear are `accessories`. `wornAs` is REQUIRED on any entry whose `type` is an outfit slot or whose item an outfit above also describes; without it no page can say who wears it. `wornAs` names where the item lives and never changes — which character wears it on a given page is that page's `wornItems` row.
- A change to an object, an animal or an invented figure is a state, not a second entry. One physical thing is ONE entry however the story alters it — broken, mended, opened, filled; a wing broken then whole; a coat lost then found. Almost every entry has no states at all: the field exists for the rare thing the story itself changes, and an entry without one is the normal case. `description` holds how it is BUILT — the material, shape and size that survive every state. `states[]` lists every look it wears, each with its own `pages`, because each state is drawn as its own reference cell and there is no separate cell for the object at rest. The FIRST state is therefore not optional and not implied by `description`: it is the object before the story changes anything, and it carries every page up to the first change. Leave it out and those pages are drawn in the first CHANGE instead — an object already broken, muddied or emptied on the page it is found. Two entries for one object give it two canonical descriptions and two reference renders that disagree on how it is built.
- The bible shows the OBJECT and nothing else. The test for a state: take away everything that is not the object — the hands, the hole, the wing, the shelf — and if what is left looks the same, it is not a state. Held, set down, carried, pressed against something, standing in front of something: always the same object, no state. How the WORLD lights it is not a state — a lamp falling on it, a shadow crossing it, dusk: the scene decides that, page by page. Its OWN light is a state: a thing that glows and goes dark, lights up and goes out, changes what it gives off. Then the emission lives in the states, never in `description`, which carries only what holds on every page. A state is only when the thing itself changes — a flower wilts, a bottle breaks, a canvas gets painted, a lid is torn off, a jar is filled — or when a face prop turns its other side to us. A state's cell draws the object alone; a state defined by contact with another element cannot be drawn without that element and drags it into the cell.
- A `delta` describes the object's own look and nothing else: no place, no figure, no body part, no other element. On the first state it is how the object looks before anything happens to it; on every later state it is what changed.
- A state describes the object's OWN look — never what arrives at it from elsewhere on that page (water jetting in, a shadow falling on it, a thing thrown toward it). The arrival is the action's result and belongs with the characters in that page's prose.
- A part added to an object — a filling, an insert, a fitting, a lid, a cover — is a state of it. It earns its own entry only when BOTH hold: some page stages it on its own, away from the thing it belongs to, and it has a look worth specifying once separated.
- An object's `description` is drawn literally. Material, colour, size and shape in plain terms, the colour named with a plain colour word and a material-colour compound only where no plain word fits; never compare its SHAPE to another object ("curved like a shallow bowl") and never say who holds, carries or wears it.
- As many entries as the story needs, no more. Include an element even if it appears once.
- `pages` is earned: a page belongs to an element only when your brief for that page puts it in frame, never a blanket 1-{PAGE_COUNT} range. Being physically present is not earning: a worn satchel, a leash on a wrist, the room they stand in are on many pages and earn only the pages that make something of them. At most {VB_ELEMENT_BUDGET} elements claim any one page (locations do not count); when more could, the ones the plan line names come first and the rest leave that page. An element the story leaves behind ends its range there. A character's or a tracked animal's entry claims only pages whose plan line names it — a brief never earns one for it (rule 3). {ELEMENT_ENTRY_PAGE} An entry's `pages` and the `objects[]` of the page briefs below say the same thing.
- Empty categories are `[]`.
- One entry per secondary character, no matter how the plan lines refer to them; a person named both by relation and by attribute is one entry. Never reuse an id. Every character the story names is declared exactly once: a name that is neither in {CHARACTER_NAMES} nor a `secondaryCharacters` entry is a fault, and so are two entries for the same person under different names.

Every entry needs specific colours, one signature element, and a description that works without seeing the other scenes.

Two or more beings of one kind — two cats, two horses, a grown creature and its young — are written against each other: each entry names the dimensions they share and the dimensions that tell them apart. Unrelated ones differ in at least two a child reads at a glance: colour, markings, coat or scale texture, build, ear or horn or tail shape. Ones the story makes kin share the family look — palette, markings, eye colour, head and snout shape — and part only on age and size.

The `description` is copied verbatim into image prompts, so write it as image-prompt text:
- **It must read as that thing at a glance.** Its first noun is the noun its `label` ends in — the word that names the thing, never the family in `type` ("a brass lantern with…", never "a brass light source with…"). Then name the one feature that separates it from the everyday object its bare geometry would otherwise describe — what it attaches to, what it is part of, what it does. Naming that thing identifies the object and never puts it in the picture. Geometry serves recognition; it is never the whole description.
- Describe the SHAPE for objects the model confuses — name the parts and how they connect (`perpendicular`, `parallel`, `spans across`, `forms a T from above`, `mounted at right angles to`). No analogies, no negations; both mislead the model.
- Name every functional part the model tends to drop, with its position and state (a taut string, evenly spaced rungs, a tensioned sail).
- A two-sided prop (rule above) lists the side away from the viewer first (the back of the sheet, the closed cover, the outer face of the roll), then `"face to camera"`, each with its own `pages`. Its `description` gives material, edge wear and dominant colour and says nothing about what is drawn or written on the face; words the reader must be able to read go in `text`.

Ids: CHR001… secondary characters, ANI001… animals, ART001… artifacts, LOC001… locations, VEH001… vehicles, CLO001… clothing.

Only a place listed in the REAL LANDMARKS section above gets `isRealLandmark: true` and `landmarkQuery` set to its exact name as listed. Every other location — a town, a street, a square, a building, any place that exists in reality but is not on that list — stays `isRealLandmark: false` with `landmarkQuery: null`. No REAL LANDMARKS section means no landmarks are available: every location is `false` and `null`.

{PLACE_SEPARATE} {PLACE_INSIDE_OUTSIDE}

A location shown from one viewpoint needs no `vantages[]`. Give it `vantages[]` and split it when the same place appears from a fundamentally different camera position — exterior/interior, on a structure/beside it, wide vista/close on one element, approach/arrival. Never split for a different cast, time of day or weather: each page declares its light in `timeOfDay` and `weather`, and the plate is painted, or re-lit, in that page's light. An object with `states[]` is cited by a page as `ART###.N`, the state whose `pages` include that page; otherwise the bare `ART###`. Vantage ids are `LOC###.N` from 1; each carries `name` (3-8 words), `shot` (one of {SHOT_ENUM}; {VANTAGE_SHOT}), a 2-3 sentence background-only `description` of what the place looks like from here, an `emptyScenePrompt` (the plate, rules below), and `pages`. Every page in the location's `pages` appears in exactly one vantage.

One backdrop plate is painted per vantage, and every page of that vantage is drawn on it. A location with no `vantages[]` carries its single plate in its own `emptyScenePrompt`. A page whose backdrop the shared plate cannot hold — a different camera position, a different part of the place, a framing the plate has no room for — takes a vantage of its own, with its own plate. Decide that split here, from the plan lines: a plate is written once for the pages that share it, never once per page.

**`emptyScenePrompt`** — one plate per vantage
- 2-3 sentences describing only the background. Every page of the vantage is drawn on this one plate; you write it once, here, and never again on a page.
- The framing the vantage's `shot` names: a close vantage gets a close stage (the wall, the doorway, the table corner), a wide vantage the wide setting. Never an establishing wide shot for a close vantage.
- The plate carries this vantage's geometry: the same direction and the same gradient for any path, river, road, corridor, shoreline or horizon — a route the pages climb rises away from the camera, one they descend falls away; the vanishing point or opening named as a frame position (upper-right, lower-centre); the dominant diagonals; the direction the light comes from. The plate names no time of day and no weather — the pages declare those. When the camera is aboard a vessel or inside a structure (rule 11d), it is that view's geometry — the rail's line, the deck's recession, the direction the light enters.
- The plate describes only what the camera sees from this vantage — never the exterior of the vehicle or structure the camera stands on.
- Foreground, midground and background bands show the place's own ground, floor, deck or water continuing unbroken — figures are composited into them on every page that uses this plate, so those bands are written as the scene's own surface and never as a reference to the figures themselves. Everywhere else the room or landscape keeps its furnishing; never clear the whole frame.
- The geography gives a standing character footing at every depth the pages on this vantage place one — a bank, path, floor, deck or walkway, never open water or air under a standing figure. A plan line that puts a figure where nothing standable exists moves the figure or takes another vantage. A character inside a shaft, pit or hollow stands on its floor: that floor is the standable surface, shown from within (rule 11e).
- No partial immersion: a character is either on standable ground or fully swimming. Wading, ankle-deep and knee-deep poses render as standing on the water surface — the plate carries a water's edge to stand at instead.
- An obstacle a plan line on this vantage puts in the way stands on the plate, blocking the route in frame; the character's action against it — pushing, pointing, trying, being stopped — belongs with the characters on the page. An obstacle nobody engages reads as scenery.
- A crowd the pages need — pushing through it, hiding in it, lost in it — is a required element of the plate with mass and closeness, never background atmosphere. When a plan line on this vantage calls for a populated setting (crowd, villagers, market-goers, soldiers, guards, onlookers, distant workers, busy street), describe those anonymous extras here — density, rough distance, silhouette only — so the backdrop already feels populated. Name a mix of several garment colours and more than one garment shape so the crowd reads as varied, not a uniform robed mass. Keep extras away from the bands the figures are composited into.
- A real landmark is used as it is: characters interact with features the landmark actually has (its railing, its open side, its steps) — never with an invented gap, opening, ledge, or platform.
- The plate is the place without the story's actions: its static props present and in place — an object lying in a basin, a rope hanging from a beam, a lid on its chest — named by presence and silhouette (a broken object by its outline: "its narrow end is missing, it ends in a straight cut"), never by damage texture. An action and its effect — a jet of water, a splash, a break, smoke, a fall — belong with the characters, in the page's prose and `sceneIntent`, and never reach the plate. A space a plan line puts something in is never called empty — name what its floor and walls hold.
- The plate holds only what looks the same on every page of the vantage. A prop the story changes at this place — opened, broken, emptied, moved — either stays in the state it holds across those pages, with the change written on the page that makes it, or the pages after the change take a vantage of their own.
- No named characters, no tracked animals, no carried or handheld artifacts (swords, books, apples, crossbows — those get composited later with the character).
- Scene-anchor artifacts are allowed and, when the pages on this vantage centre on them, must be described as the focal point — "prominent at the centre, dominating the foreground composition, readable and sharply defined," not "a pole stands somewhere." This covers stationary anchors (pole-with-banner, throne, monument, pedestal, altar, signpost) AND the vehicle or vessel when the pages take place in or on one (boat, raft, wagon, carriage, cart, train compartment, sled). Name the key visual marker (the hat on top, the inscription, the crown, the boat's stern) so the model doesn't substitute a generic building.
- Never write hypothetical references like "where children would stand" — image models render those literally.
- When the vantage goes down into a cellar, basement, cave, or other below-ground space, write the backdrop from inside at the top of the steps looking down — the stairs recede downward and away into the space below. Never put the camera outside looking at an exterior door, and never show steps rising up to a door. When a character is at the bottom of a shaft, pit or hollow, the backdrop is its interior: from the rim looking down onto its floor, or from the floor looking up at the opening.

---

## OUTPUT

Two parts, in this order: the Visual Bible, then the {PAGE_COUNT} page briefs. Declaring the cast and the props first is what lets each page cite ids that already exist.

First, exactly:

---VISUAL BIBLE---
```json
{
  "secondaryCharacters": [
    {
      "id": "CHR001",
      "name": "[Name]",
      "pages": [1],
      "scaleClass": "[one band from the scaleClass rule]",
      "age": [age in years as a number],
      "peer": "[yes|no — yes only for an invented child who plays, competes or shares a group with the commissioned children]",
      "build": "[height, body type]",
      "hair": "[colour, style, length]",
      "face": "[eye colour, distinctive features; for adults state facial hair]",
      "signatureLook": "[one memorable visual element]",
      "clothing": "[specific colours and details]",
      "states": "[OMIT unless the story changes this one's own look — a wing broken then whole, a coat lost then found. Almost never present. When present, the COMPLETE set of looks, at most 4. The FIRST row is how they look before the story changes anything, and every page up to the first change belongs to it: {\"name\": \"unaltered\", \"delta\": \"[how they look before any change, one clause]\", \"pages\": [X, Y]}. Then the rest, in the order the story reaches them: {\"name\": \"[one or two words]\", \"delta\": \"[the change to it, one clause, at most 15 words — never a place, a holder or another element]\", \"pages\": [X, Y]}. Every page in this entry's `pages` falls in exactly one state's `pages`.]"
    }
  ],
  "animals": [
    {
      "id": "ANI001",
      "name": "[Name]",
      "pages": [1],
      "scaleClass": "[one band from the scaleClass rule]",
      "generic": "[OMIT unless generic — see the generic rule]",
      "species": "[type and breed]",
      "coloring": "[exact colours and full pattern]",
      "features": "[collar, markings, accessories]",
      "states": "[OMIT unless the story changes this one's own look. Same shape and limits as above.]"
    }
  ],
  "artifacts": [
    {
      "id": "ART001",
      "label": "[see the label rule]",
      "name": "[plain English noun phrase for the thing, e.g. 'brass telescope', 'oak sea chest' — English even for a non-English story; never a proper noun, and never a bare category]",
      "properName": "[OMIT unless the story gives this object a name of its own]",
      "pages": [1],
      "scaleClass": "[one band from the scaleClass rule]",
      "generic": "[OMIT unless generic — see the generic rule]",
      "type": "[the category the reference-sheet gate checks the cell against, in English: 'hand tool', 'sea chest']",
      "text": "[OMIT unless the story asks a reader to read words on this object; then the exact characters, in the order they must appear — a few words, or a run of letters or digits]",
      "states": "[OMIT unless the story alters this object or turns a face prop's two sides to us. Otherwise the COMPLETE set of looks it wears, at most 4 in all. The FIRST row is the object before the story changes anything, and every page up to the first change belongs to it: {\"name\": \"unaltered\", \"delta\": \"[how it looks before any change, one clause]\", \"pages\": [X, Y]}. Then the rest, in the order the story reaches them: {\"name\": \"[one or two words]\", \"delta\": \"[the change to the object itself, one clause, at most 15 words — never material, colour or scale, those are the entry’s own; the object's own light belongs here when the story turns it on or off, how the scene lights it never does; never a place, a holder or another element]\", \"pages\": [X, Y]}. Every page in this entry's `pages` falls in exactly one state's `pages`.]",
      "description": "[what it is, named with the word that names it, and the one feature that separates it from the everyday object its bare geometry would otherwise describe — what it attaches to, what it is part of, what it does; then colour, material, geometry, described by shape and parts, never by a style name. No lettering unless this entry names the exact words it carries in `text`; otherwise 'illegible' / 'weathered' / 'smudged'. A prop whose interest lies on its face is one entry, with a state per side.]"
    }
  ],
  "locations": [
    {
      "id": "LOC001",
      "label": "[see the label rule]",
      "name": "[Name]",
      "pages": [1],
      "scaleClass": "[one band from the scaleClass rule]",
      "setting": "[indoor/outdoor, type]",
      "colors": "[specific colours]",
      "features": "[distinctive visual elements]",
      "signatureElement": "[one unique detail]",
      "isRealLandmark": false,
      "landmarkQuery": null,
      "emptyScenePrompt": "[the backdrop plate for this place; OMIT when this entry has vantages[], which carry one each]",
      "vantages": "[OMIT unless the place appears from more than one camera position. Otherwise one row per viewpoint, and every page of this entry in exactly one of them: {\"id\": \"LOC001.1\", \"name\": \"[3-8 words]\", \"shot\": \"wide\", \"description\": \"[2-3 sentences, background only]\", \"emptyScenePrompt\": \"[this viewpoint's plate]\", \"pages\": [X, Y]}]"
    }
  ],
  "vehicles": [
    {
      "id": "VEH001",
      "label": "[see the label rule]",
      "name": "[plain English noun phrase; a name the story gives it goes in properName]",
      "properName": "[OMIT unless the story names it]",
      "pages": [1],
      "scaleClass": "[one band from the scaleClass rule]",
      "generic": "[OMIT unless generic — see the generic rule]",
      "colorAndDetails": "[appearance: shape, colour, material, size, distinctive features]",
      "signatureElement": "[one unique detail]"
    }
  ],
  "clothing": [
    {
      "id": "CLO001",
      "label": "[see the label rule]",
      "name": "[plain English noun phrase; a name the story gives it goes in properName]",
      "properName": "[OMIT unless the story names it]",
      "pages": [1],
      "wornBy": "[character name]",
      "description": "[appearance: shape, colour, material, size, distinctive features]",
      "howWorn": "[how it sits on the wearer]"
    }
  ]
}
```

Then, for each plan line, in the order the plan lines are listed, exactly:

```
## Page <number>
<one continuous paragraph of prose>

---METADATA---
<single-line JSON object>
```

Nothing before the `---VISUAL BIBLE---` marker, nothing between the Visual Bible and the first page's heading, nothing after the last page's metadata. Every one of the {PAGE_COUNT} pages must appear exactly once — a missing page has no illustration.

Each paragraph starts immediately with the main subject and main action — no preamble, headers, or bullets — and must include, interleaved naturally:
- the main action of this page, opening the prose
- each named character described on first mention (age look, build, hair, eyes, distinctive features, and the full outfit: top, bottom, footwear — or the tail/fin in place of bottom and footwear when the outfit replaces the legs)
- where each character is positioned (left/right/center, foreground/midground/background), what they hold, how they face
- the setting: location, time of day, weather, lighting, mood — and the season, which is **{SEASON}**
- any recurring visual element in frame, named with the identifying detail this shot shows, at the size and visibility the shot gives it
- a short note on shot type ({SHOT_ENUM}) if not obvious from the action

**Distance rule:** when characters must be far apart, describe the size difference against something in the frame rather than calling it small: "the main character fills the centre foreground while the secondary character is a figure in the far distance on the right, roughly a sixth their size."

**Example prose (one page):**

A young child — a slim seven-year-old with short straight dark hair, brown eyes, wearing a striped hooded sweatshirt and dark athletic pants — kneels in the threshold of the open kitchen doorway and leans forward to reach a brown paper package lying flat on the pale parquet floor, bright midday sunlight pouring through the doorway and warming the cream-coloured cabinets behind him. In the soft midground, an adult — slim build, neck-length brown hair tied in a small ponytail, grey zip-up hoodie over a black t-shirt and dark jeans — stands beside a large wooden kitchen table with slightly worn edges in the centre of the room, and watches the discovery with a tilted head and curious half-smile. Wide shot framed by the open doorway, calm weekday atmosphere, the rest of the room quiet and tidy.

---

## Metadata block

After each page's prose, add exactly:

---METADATA---

Then a single-line JSON object:

```
{
  "sceneIntent": "CharacterA kneels in the open doorway and reaches for a package on the floor. CharacterB stands beside the table and watches, head tilted. Bright midday light falls through the doorway, and CharacterB's shoulders hang loose.",
  "characters": [
    {"name": "CharacterA", "clothing": "standard", "position": "left foreground", "depth": "foreground", "looksAt": "CharacterB", "expression": "wide-eyed, mouth half open, brows lifted", "emotion": "surprised"},
    {"name": "CharacterB", "clothing": "costumed", "position": "center background", "perspective": "back view, head turned toward right shoulder", "depth": "background", "looksAt": "ART001", "expression": "one eye and cheek visible, calm, faint smile", "emotion": "happy"}
  ],
  "shot": "medium",
  "population": "cast_only",
  "aboard": "VEH001",
  "landmarkView": "exterior",
  "era": "present day",
  "timeOfDay": "midday",
  "weather": "none",
  "objects": ["LOC001", "ART002"],
<!-- TEXT_OVERLAY_BEGIN -->
  "textPosition": "top-left|top-right|bottom-left|bottom-right|top-full|bottom-full",
  "textZoneDescription": "5–15 words naming the saturated, high-contrast surface in that corner",
<!-- TEXT_OVERLAY_END -->
  "interactions": [
    {"character": "CharacterA", "object": "an item", "where": "carries the item in a pocket", "action": "standing", "storyRelevant": false, "priority": "low"},
    {"character": "CharacterB", "object": "wooden ladder", "where": "climbs the wooden ladder", "action": "climbing the ladder", "hands": true, "storyRelevant": true, "priority": "essential"}
  ],
  "wornItems": [
    {"id": "ART001", "owner": "CharacterA", "state": "worn"},
    {"id": "ART003", "owner": "CharacterB", "state": "off", "location": "lies on the ground beside the ladder", "redressNote": "<which garments stay, named by colour + noun only; which item is off, with nothing in its place; and the garment the removal leaves outermost, described in full>"},
    {"id": "ART005", "owner": "CharacterA", "state": "worn", "wearer": "CharacterB"}
  ]
}
```

### Field rules

**`sceneIntent`** (required): {SCENE_INTENT_FIELD}

**`timeOfDay` / `weather`** (required): {SCENE_LIGHT_FIELD}

**`characters[]`**
- One entry per main or primary character in the scene. Secondary characters never appear here.
- Every character from CHARACTER DETAILS the prose describes as present gets an entry here, including a figure standing in the background or named only in passing. Never route one of them to `objects[]` instead.
- Always `name`, `clothing`, `position`.
- `clothing` is copied EXACTLY from AVAILABLE CLOTHING PER CHARACTER — `standard`, `winter`, `summer`, or `costumed:<costume>` as that list spells it. That list covers main and primary characters only — a secondary character never carries a clothing category, because they have no avatar for a category to select. One costume per character per story; when a character is `costumed`, also describe the costume in the prose.
- `position` is a short phrase matching the prose ("right foreground", "center", "left background", "behind the desk") — the bbox detector reads it structurally.
- Only add `perspective` for back view, side profile, or the near figure of an `over-the-shoulder` page (`over-the-shoulder`).
- `looksAt` on every foreground/midground character: another character's name, a Visual Bible id (`ART001`, `CHR001`), or `away`. Eyes only — see rule 8j. Never the object a character merely holds, and never the viewer — a book cover page excepted (rule 8j).
- `era` is the story's time period in 2-4 words (medieval, 1920s, present day). It feeds the anachronism guard — omit only if the story has no defined era.
- `shot` is the page's camera framing: {SHOT_ENUM}. Decide it once — the scene prose and the metadata use this same framing, and it is a framing the plate of the vantage this page cites can hold. Omitting it makes the plate default to a wide shot.
- `population` says how populated the page's setting is, in one of three values, and it is about the SETTING, not about who matters to the story.
  - `"crowd"` — the page is written around unnamed background people: a market, a busy street, a gathering, a body of troops.
  - `"ambient"` — a public place that simply has people in it: a city square, a park, a promenade, a quay, a playground, a station concourse, a shop. Distant passers-by, someone on a bench, a figure by a wall belong in the picture and are not cast.
  - `"cast_only"` — nobody but the listed figures: a private room, a garden, a forest clearing, an empty night street.
  Omit it and the page is read as `"cast_only"`. Choose `"ambient"` over `"cast_only"` whenever the location is one the public can walk into; a square with nobody on it reads as abandoned. The prose then must NOT claim the place is empty — never write "no other people are present" about an `ambient` or `crowd` setting. Describe background people the way the setting holds them ("a few distant passers-by cross the far side of the square"), and keep them unnamed, distant and small. The rule runs both ways: prose that puts unnamed figures in the frame — people crossing the square, riders passing, adults at the tables, onlookers — is a setting with people in it, so `population` is `ambient` or `crowd` on that page, never `cast_only`.
- `landmarkView` is required on every page citing a real landmark — how the page sees it: `exterior` (from outside), `distant` (skyline or backdrop), `close` (a detail of the structure), `interior` (inside it), `view-from` (looking out from it, structure not visible), or `underwater` / `none` when no photograph of the place could show this scene. It selects the reference photo; a view with no matching photo attaches none and your prose carries the place.
- A `close-up` frame ends at the waist. A pose the frame simply crops — sitting, kneeling, crouching — is fine there: the legs fall below the bottom edge, which is what a close-up IS, and a crouch or a kneel brings the head DOWN to what it is looking at. What a close-up cannot show is an action whose subject lies below the frame line — no {CLOSEUP_BELOW_WAIST}, and nothing placed behind the character. The outfit list still names all garments including footwear. A plan line whose point is below the waist is a `medium` shot, not a close-up.

**`objects[]`**
- String array of Visual Bible IDs only (`LOC001`, `ART002.2`, `CHR001`).
- Secondary characters visible in the scene go here by CHR id. Write their full appearance and their COMPLETE outfit into the prose on every page they appear on, copied from their Visual Bible entry — that entry is what their reference image is drawn from, so prose that disagrees with it fights the reference.
- Include every recurring visual element visible in the scene.
- A NAME IS NOT A PRESENCE. An element named only to say whose it is — a character's bag, a creature's egg — puts THAT element in the scene, not its owner. Cite the owner only on a page where the owner is in the picture. On the page an owner arrives, or a creature comes out of its egg, both are in the picture and both are cited.
- At most {VB_ELEMENT_BUDGET} Visual Bible elements per page — secondary characters, animals, artifacts and vehicles count; a LOC does not, real or invented. Keep the ones the page is about and leave the rest out of `objects[]` and out of the prose.
- An object listed with `States:` is cited with the dotted form `ART###.N` — the state whose `pages` include this page. An object with no states keeps the bare `ART###`. An object WITH states is always cited dotted, the pages before its first change included — those pages are its first state. Never two states of one object on one page.
- Real-landmark LOCs are cited by their bare id (`LOC003`); the page's `landmarkView` decides which reference photo is attached.
- Invented LOCs (`isRealLandmark: false`) use the bare id `LOC001`, or the dotted form `LOC001.2` when the Visual Bible gives that location `vantages[]` — cite the vantage whose `pages` list contains this page.

**`depth`, `expression` and `emotion`** — required for every character. `depth` is `foreground`, `midground` or `background`; `expression` is 3-8 words naming brows, eyes and mouth ("brows pulled tight, eyes narrowed, jaw set"). Never write a bare mood word in `expression` — "happy" and "sad" cannot be drawn; the one word goes in `emotion`, from rule 8k's list. A character's expression may change page to page with the story; their depth follows the composition. On a `wide` shot, or a plain `back view` with no head turn, faces are too small or hidden to read — describe body language in the prose instead of facial detail (rule 8i covers a back-view character whose expression must read).

**Characters are solid.** Never `translucent`, `transparent`, `ghostly`, `see-through`, `spectral` — a character is always fully opaque, whatever the story calls them.

**`interactions[]`** — visible character-to-object and character-to-character contacts. The image generator reads all entries; the evaluator only enforces entries marked `storyRelevant: true`.

Mark `storyRelevant: true` only when the contact is the page's plot moment, identity marker, or dramatic action — typically 0–2 per page. Costume texture, pose detail, and secondary contact are `storyRelevant: false`. In doubt → `false`.

Omit entirely (do not include with `false`): pure ground/floor/path contact, leaning on walls, sitting on generic chairs.

Field shape: `{character, object, where, action, hands, storyRelevant, priority}`. {CONTACT_VERB} `where` is a short sentence naming the object as the contact action ("holds the parchment map", "hands the letter to the messenger"). No body-part positioning, no pose detail: write "pushes the cart", not "presses both palms flat against the cart's side" — the visible grip belongs in the prose (rule 7b). Name which hand, how many hands, or where on the object they land only when the story turns on it.

`receiver` (optional): when the action's result later arrives at a second object — a basin under a spout, a bucket under a pump, a pool below — its VB id or plain name. `target` (optional): what the tool acts on, as a short noun phrase ("the crack in the rock face"). The page shows the result at the contact; the builder places the receiver clear of the tool.

An actor in the `character` slot is a name listed in `characters[]` or the id of a visual bible animal; an object, a vehicle or a place is never an actor, and a figure written by role rather than by its id resolves to nobody.

`hands`: `true` when the character's hands are on the object. {SHARED_GRIP}

`action`: required on every row, in English whatever the story language. Name the physical activity — the verb and what it acts on ("pushing the cart", "climbing the ladder") — never the purpose it serves. Rows performing the same activity carry the same label, however many characters that is. Use `"watching"` for anyone who only looks on and `"standing"` for anyone merely present; neither counts as the page action.

**Shared objects.** When several characters use ONE object at once, or act on ONE object together (carry the same basket, hold the same banner, pull the same rope, push the same cart), emit ONE entry: join the names with ` + ` in `character` and describe the shared action in the one `where` sentence. Separate entries with the same object render as duplicate objects. Characters each using their OWN identical object keep separate entries.

`priority`: `"essential"` for the page's central action (held prop, traced line, key gesture), `"normal"` for supporting action, `"low"` for incidental gaze/pose. Optional — defaults to `"normal"`. The image generator orders EXACT POSES with essentials first.

**Coverage:** every foreground/midground character should appear in `interactions[]` exactly once. If a character has no real action, give them a `priority: "low"` entry (e.g. `{character, object: "the campfire", where: "watches the campfire", storyRelevant: false, priority: "low"}`) — without it the image model defaults to a camera-facing portrait. Background characters exempt.

**`wornItems[]`** (required whenever it applies) — {WORN_ITEMS_ROW}

A vehicle under way MUST have exactly one character actively operating it — declare that operation as an interaction for that character, `essential` when the operating is the page's plan line and `low` otherwise.

Cap: 1–6 entries, 0–2 with `storyRelevant: true`, and one `action` label per page beyond `"watching"` and `"standing"`.

Every entry must also appear in the prose as the same physical contact.

Back-view rule: for characters with `perspective: back view`, every held object must be visible from behind — peeking over shoulder, visible under arm, strapped to back, dangling at the side.

Pointing rule: no character points. Show which thing has their attention with gaze, body orientation, or an object held up — never an extended arm or index finger, in the prose or in an `action` label.

{COUNTING_RULE}

Distance rule: an action across a gap is framed by rule 11 and its `where` carries the separation phrase it names.

<!-- TEXT_OVERLAY_BEGIN -->
**`textPosition` / `textZoneDescription`**
- `textPosition`: the corner reserved for the white story-text overlay. Even pages use a right corner (`top-right` / `bottom-right`), odd pages a left corner (`top-left` / `bottom-left`). `top-full` and `bottom-full` span both sides and are valid on any page.
- Pick the half against the figures' declared `depth`, not only the lateral side: a `foreground` character fills the bottom band across its whole width and a `background` one the top band, so the text goes in the other half.
- Keep that side clear: no character, no character body part and no shadow of a character on the text side of an even page's right half / an odd page's left half. Shift the whole composition one zone away from it rather than re-arranging who stands where.
- The surface behind the text naturally occupies that band in the scene's geometry: `top-*` takes sky, ceiling, cave roof, upper foliage, cliff face or calm distance; `bottom-*` takes ground, floor, water, cobblestones, grass, sand or low foliage. A vertical surface (wall, facade, door) fits `top-*` only when the camera puts it across the top of the frame — narrow alley, interior, against-the-wall framing.
- `textZoneDescription`: 5–15 words naming the saturated, high-contrast surface in that corner (e.g. "deep evening sky in rich indigo", "dark green pine foliage"). Describe what's there, not what it's for — never "rectangle", "blank area", "for text".
- The vantage's plate reserves no band — it serves pages whose `textPosition` differs — so name a surface the plate already puts in that band.
- Across the book: at least 30% of pages put the text in a `top-*` position and at least 30% in a `bottom-*`, never more than 3 consecutive pages in the same half, and 30-50% of pages use `top-full` or `bottom-full`. The full-width band goes to the pages whose scene already carries an edge-to-edge calm surface — wide and ultra-wide shots, aerial shots, landscape-heavy scenes, a clean sky band above or a smooth ground band below.
<!-- TEXT_OVERLAY_END -->

**`aboard`**
- The Visual Bible id of the vehicle or structure the camera stands on or inside for this page (`VEH001`, `LOC004`). Omit it when the camera is outside the element looking at it, or when no such element is in the shot.
- It tells the plate builder to render the surfaces around the camera instead of the element's full exterior, so a page set on board does not gain a second copy of the vessel in its background.

**Landmark accuracy:** Read all photo variant descriptions before writing. Use the most common structural features across variants. If characters are inside an enclosed landmark, describe the scene from inside.

**Prop placement vs landmark.** When a separate prop shares a depth band with the landmark, position the prop relative to the landmark (in front of, beside, between camera and landmark) — not by an unanchored frame slot.

**Self-check per page before moving on:** the prose and the metadata describe the same single moment; every character the prose places in the scene has a `characters[]` entry and the same outfit in both; one gaze target, and a character gazing at a background-depth target is `back view`, never facing the camera; every demanded grip is written as a visible pose.

The self-check refers to each character by name only. Never re-describe a character inside it, and never state an age, a build or an outfit there — a description written in the check replaces the one in CHARACTER DETAILS and is drawn. Nothing from the check appears in the output.

---

All output in English. Prose and all metadata fields must be in English.
